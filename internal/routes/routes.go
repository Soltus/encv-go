package routes

const (
	Root          = "/openlist/sites" // 根路由暂时没做，先用openlist管理代替
	Admin         = "/admin"
	Login         = "/login"
	Logout        = "/logout"
	FSProxy       = "/p"
	FSProxyAPI    = "/p-api"
	OpenListProxy = "/openlist"
)
