让我重新审视这个问题，用不同的角度思考。现在我有了更全面的视图。基于代码审视，我提出一个**架构层面的更好思路**：

## 🎯 根本问题 vs 现有方案的局限

### 当前方案的困境

你现在的实现（`MainActivity.kt`）采用：
1. **Assets 提取 → 本地文件系统** （copyBinaryFromAssets）
2. **权限检查 + HTTP 轮询** （checkHealth + waitForBackendAndNotify）
3. **noexec 绕过** （多目录回退：filesDir → cacheDir → externalFilesDir）

这些都是**被动防守**，每次都在解决 Android 系统限制带来的副作用。

---

## 💡 更好的思路：**架构转变**（3 个方向）

### **方案 A：原生服务化（推荐稳定性）**

不再让 APP 管理后端进程生命周期，而是：

```
┌─────────────────────────────────────────┐
│ ENCV-go 独立后台服务 (Foreground Service) │
│  - AndroidManifest 注册为 Service          │
│  - 系统级进程管理（不被杀掉）             │
│  - 独立于 MainActivity 生命周期           │
└─────────────────────────────────────────┘
         ↓ IPC (Intent/Broadcast)
┌─────────────────────────────────────────┐
│ Ionic Vue App (WebView)                  │
│  - 仅负责 UI + HTTP 调用                 │
│  - 启动/监听 Service，无需轮询          │
└─────────────────────────────────────────┘
```

**优势：**
- ✅ 系统会自动拉起服务（即使 APP 被杀）
- ✅ 避免 `noexec` 问题（native code 在 lib 目录）
- ✅ 不需要复杂的目录回退逻辑
- ✅ 清晰的进程边界

**实现：**
```kotlin
// EncvGoService.kt
class EncvGoService : Service() {
    override fun onStartCommand(intent: Intent?, flags: Int, startId: Int): Int {
        // 启动 Go 进程（使用 native library，不是 assets）
        startForegroundNotification()
        startGoProcess()
        return START_STICKY  // 系统杀掉后自动重启
    }
}

// MainActivity 仅负责绑定
bindService(Intent(this, EncvGoService::class.java), connection, BIND_AUTO_CREATE)
```

---

### **方案 B：Native Library + JNI（更加整洁）**

Go 二进制编译成 **`.so` 库** 而非可执行文件：

```
android/app/src/main/jniLibs/arm64-v8a/
├── liblibencv.so          # Go runtime
└── liblibencvgo.so        # Business logic
```

**优势：**
- ✅ APK 打包透明（Gradle 自动处理）
- ✅ Android 官方方式（无黑科技）
- ✅ 系统自动提供执行环境（无 noexec 问题）
- ✅ 进程崩溃时系统能捕获 JNI 信号

**权衡：**
- ❌ 需要修改 Go 编译为动态库（complex）
- ❌ 需要 JNI 胶水代码

---

### **方案 C：混合模式（实用折衷）** ⭐ **推荐**

保留 Assets 方案的简单性，但改进**可靠性**：

```kotlin
// 1. 预编译时放在 lib/ 而非 assets
android/app/src/main/jniLibs/arm64-v8a/libencvgo_main.so

// 2. MainActivity 中用 Runtime.exec() 执行
private fun startGoProcess() {
    val binary = "/data/data/${context.packageName}/lib/libencvgo_main.so"
    Runtime.getRuntime().exec(binary)
}

// 3. 改为 Broadcast 通知而非 HTTP 轮询
// Go 进程启动成功 → 发送 Broadcast → MainActivity 接收
private fun registerBackendBroadcast() {
    LocalBroadcastManager.getInstance(this).registerReceiver(
        backendReceiver, 
        IntentFilter("com.encvgo.BACKEND_READY")
    )
}
```

**优势：**
- ✅ 保留你现有大部分 Kotlin 代码
- ✅ 避免 noexec 问题
- ✅ 相比 HTTP 轮询，Broadcast 更快更可靠
- ✅ 实现简单（改动最小）

---

## 🔴 当前代码的**具体瓶颈**

### 1. **HTTP 轮询延迟**
```kotlin
// 每 500ms 轮询一次，最多 30 秒 = 最坏情况 30 秒才能连上
for (attempt in 1..60) {
    if (checkHealth(port)) { ... }
    Thread.sleep(500)  // ⚠️ 浪费时间
}
```

❌ **问题**：启动慢，用户体验差

✅ **改进**：Go 进程成功启动时主动通知

```kotlin
// Go 端
println("ENCV_BACKEND_READY:2025")  // 或 broadcast

// Kotlin 端监听 stdout 或 broadcast
private val backendReadyPattern = Regex("ENCV_BACKEND_READY:(\\d+)")
```

### 2. **noexec 多目录回退复杂**
```kotlin
// 试 filesDir → cacheDir → externalFilesDir
// 但每次都可能失败，诊断信息复杂
val candidateDirs = listOf(...)
for ((dir, name) in candidateDirs) {
    if (binary.canExecute()) { ✅ }
}
```

❌ **真正原因**：filesDir/cacheDir 挂载时就带了 noexec  
✅ **根本解决**：直接用 `/data/data/{package}/lib/` → 系统保证可执行

### 3. **没有健康检查的二级检验**
```kotlin
// 只检查 HTTP /health endpoint
if (code == 200) {
    backendReady = true
}
```

❌ **问题**：HTTP 200 不代表 Go 业务逻辑就准备好了

✅ **改进**：检查 Go 日志中的 "ready to serve" 或类似标记

---

## 📋 具体改进清单

### **短期（修复现在的问题）**
```kotlin
// MainActivity.kt 改进

// 1. 改成事件驱动而非 HTTP 轮询
private val backendReadyThread = Thread {
    val reader = BufferedReader(InputStreamReader(goProcess?.inputStream))
    reader.lines().forEach { line ->
        if (line.contains("listening on", ignoreCase = true) || 
            line.contains("ready", ignoreCase = true)) {
            backendReady = true
            notifyFrontend(backendPort, null)
            readyCallback?.invoke(backendPort)
        }
    }
}

// 2. 添加启动超时时的诊断
waitForBackendAndNotify()  // 改成最多 10 秒，而非 30 秒
// 超时后打印完整的 Go 输出而非截断

// 3. 启用 Go 的详细日志
environment()["ENCV_DEBUG"] = "1"
environment()["RUST_LOG"] = "debug"  // 如果有 Rust 组件
```

### **长期（架构改进）**
- [ ] 迁移 Go 二进制到 `jniLibs/arm64-v8a/lib*.so`
- [ ] 改为 Foreground Service + LocalBroadcaster 通知
- [ ] 移除 HTTP 轮询，用事件驱动

---

## 🚀 立即可做的改进（最小改动）

直接修改 `post-cap-sync.mjs` + `MainActivity.kt`：

```kotlin
// MainActivity.kt 第 147-177 行替换为：
private fun waitForBackendAndNotify() {
    val startTime = System.currentTimeMillis()
    val timeoutMs = 10000  // 10 秒超时
    
    while (System.currentTimeMillis() - startTime < timeoutMs) {
        if (intentionallyStopped) return
        
        for (offset in 0..MAX_PORT_SCAN) {
            val port = configPort + offset
            if (checkHealth(port)) {
                backendPort = port
                backendReady = true
                Log.i(TAG, "Backend ready on port $port")
                notifyFrontend(port, null)
                readyCallback?.invoke(port)
                readyCallback = null
                return
            }
        }
        Thread.sleep(200)  // 改成 200ms，降低延迟
    }
    
    // 超时详细诊断
    val output = goProcessOutput.toString()
    Log.e(TAG, "Backend startup timeout:\n$output")
    lastStartError = "timeout:${output.takeLast(500)}"
    notifyFrontend(0, "timeout")
    readyCallback?.invoke(-1)
    readyCallback = null
}
```

---

**你觉得哪个方向更合适？** 我可以详细展开任一方案的实现代码。