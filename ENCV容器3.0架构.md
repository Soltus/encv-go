# ENCV 容器架构 v3: 分层堡垒 + L1.5 元数据层

以 SCCGV 视频容器为例

## 1. 设计哲学演进

ENCV v3 架构在 v2.1 "分层堡垒" 的基础上，引入了 **L1.5 元数据层**，通过大幅扩展信封头部（从 16B → 2048B）来支持高密度的元数据内嵌和哈希路由。

### v3 新增核心目标

*   **高密度元数据**：头部支持 2000B 的 SpecialID，可直接存储许可号、证书、多级分类等复杂元数据，无需依赖 Manifest 解析。
*   **哈希路由友好**：SpecialID 可作为分布式存储的路由键，文件物理移动不影响逻辑 ID。
*   **向后兼容**：通过版本号自动识别 v2/v3 头部，旧版本读取器可跳过 v3 头部。
*   **占位符生成**：当未提供 ID 时，系统自动生成 2000B 的加密级随机占位符，确保哈希分布均匀且绝对唯一。

--- 

## 2. 整体文件布局（v3 更新）

```flowchart LR
  A[Container Header] --> B[Salt and IV]
  B --> C[Encrypted Data Block 1]
  C --> D[Encrypted Data Block 2]
  D --> E[Manifest Block]
  E --> F[Footer]
  F --- G[Manifest Offset]
  F --- H[Manifest Length]
  F --- I[Manifest CRC32]
  F --- J[Global CRC32]
```

±-----------------------------------------------------------------------------------+
| ENCV 容器文件 (.sccgv) |
| |
| ±------------------------------------------------------------------------- <-- L1: Envelope (信封层, v3 扩展)
| | L1: Envelope Header (2048 bytes) |
| | - Magic "ENCV" (4) - Version 0x03 (2) - IDType (4) - IDLength (4) |
| | - SpecialID [2000 bytes] - CBOR/Raw 元数据 - HeaderCRC32 (4) |
| ±-------------------------------------------------------------------------+ |
| | | |
| | ±------------------------------------------------------------------- <-- L1.5: 元数据层（v3 新增）
| | | L1.5: SpecialID (可内嵌) |
| | | - 类型：CBOR 结构化数据 OR 原始哈希 |
| | | - 内容：许可号、厂牌、编号、密级、证书链等 |
| | | - 用途：哈希路由、快速元数据查询、许可证验证 |
| | ±-------------------------------------------------------------------+ |
| | | |
| | ±------------------------------------------------------------------- <-- L2: Manifest (清单层)
| | | L2: Manifest Block |
| | | - Block Header (Type, Len, CRC32) |
| | | - Manifest Data (JSON) |
| | | { “version”: “…”, |
| | | “kvi”: { … }, |
| | | “fragments”: [ … ] } |
| | ±-------------------------------------------------------------------+ |
| | | |
| | ±------------------------------------------------------------------- <-- L3: Data Streams (数据层)
| | | L3: Fragment “kvi” (KVI) |
| | | - Block Header (Type, Len, CRC32) |
| | | - KVI Data (JSON) |
| | ±-------------------------------------------------------------------+ |
| | | L3: Fragment “main_data” (Data) |
| | | - Block Header (Type, Len, CRC32) |
| | | - Encrypted Data Stream |
| | ±-------------------------------------------------------------------+ |
| | | |
| | ±------------------------------------------------------------------- <-- L4: Recovery (恢复层, 可选)
| | | L4: Recovery Block |
| | | - Block Header (Type, Len, CRC32) |
| | | - Reed-Solomon Parity Data / KVI Backup |
| | ±-------------------------------------------------------------------+ |
| | | |
| ±-------------------------------------------------------------------------+ |
| |
| ±------------------------------------------------------------------------- <-- L1: Envelope (信封层)
| | L1: Envelope Footer (32 bytes) |
| | - Magic "ENCV" (4) - Manifest Offset (8) - Manifest Len (8) |
| | - Manifest CRC32 (4) - Global CRC32 (4) - Reserved (4) |
| ±-------------------------------------------------------------------------+ |
| |
±-----------------------------------------------------------------------------------+


---

## 3. 分层详解（v3 更新）

### L1: Envelope (信封层) - v3 扩展

*   **职责**: 快速识别、精确定位、全局校验 + **元数据内嵌**。
*   **结构**: v3 Header（2048 字节） + Footer（32 字节，不变）。
*   **v3 新增字段**:
    *   `IDType`: ID 编码类型（0=Raw, 1=CBOR, 2=Reserved）。
    *   `IDLength`: SpecialID 的实际有效长度（≤ 2000）。
    *   `SpecialID`: 2000 字节的固定数组，用于存储高密度元数据。
    *   `HeaderCRC32`: 头部自身的 CRC32 校验。
*   **工作流程**:
    1.  读取文件头部前 4 字节，识别 Magic "ENCV"。
    2.  读取 Version 字段：
        *   `0x02`: v2 头部（16 字节），跳过 L1.5 解析。
        *   `0x03`: v3 头部（2048 字节），解析 L1.5 SpecialID。
    3.  读取文件末尾 Footer，获取 Manifest 位置和完整性信息。

### L1.5: 元数据层（v3 新增）

*   **职责**: 提供高密度的、可直接访问的元数据，支持哈希路由和快速许可证验证。
*   **编码**: 推荐 CBOR（高密度、自描述、可逆），也支持原始二进制（如占位符）。
*   **内容示例（CBOR）**:

json
{
“t”: 1, // Type: 枚举值 (1=视频, 2=文档)
“b”: “SOLTUS”, // Brand: 厂牌
“s”: “SN-9901”, // Serial: 编号
“l”: “LIC-2024”, // License: 许可号（可长达 600+ 字节）
“c”: 3 // Class: 密级 (0=公开, 3=绝密)
}

*   **占位符处理**:
    *   当加密时未提供 ID，系统生成 2000 字节的加密随机数。
    *   用于哈希路由，确保分布均匀且绝对唯一。
    *   可通过 SHA-256 计算指纹作为路由键。

### L2: Manifest (清单层)

*   **职责**: 元数据中心（补充 L1.5）、版本控制、冗余备份。
*   **结构**: 独立 Block，JSON 格式（兼容 v2）。
*   **与 L1.5 的关系**:
    *   L1.5 用于快速路由和简单查询。
    *   L2 用于完整的、结构化的文件索引和元数据。
    *   两者的内容可以重叠，但不强求一致（允许业务逻辑灵活使用）。

### L3: Data Streams (数据层) & L4: Recovery (恢复层)

*   **职责**: 保持与 v2 完全一致，不变。

---

## 4. 核心工作流程（v3 更新）

```
//
/* 1. 初始化（v3 增强版） */
//
±----------------------+
| 用户请求播放 |
| open-stream movie.sccgv |
±----------±----------+
|
v
±----------------------+
| 1. 读取文件头部 4B Magic |
±----------±----------+
|
±-----------------+
| Magic = “ENCV”? |
±-----------------+
| YES | NO
v v
±----------------------+ ±----------------------+
| 2. 读取 Version 字段 | | 拒绝非 ENCV 文件 |
±----------±----------+ ±----------------------+
|
±--------------------+
| Version == 0x03? |
±--------------------+
| YES (v3) | NO (v2)
v v
±----------------------+ ±----------------------+
| 3. 读取 v3 Header | | 3. 跳过 v3 头部解析 |
| 解析 SpecialID | ±----------------------+
±----------±----------+
|
v
±----------------------+
| 4. 读取文件末尾 Footer | (L1)
| 获取 Manifest 偏移 |
±----------±----------+
|
v
±----------------------+
| 5. 跳转并读取 Manifest | (L2)
| 校验 CRC32 |
±----------±----------+
|
v
//
/* 2. 处理播放器请求（核心循环，不变） */
//
（以下流程与 v2 完全一致，省略）
```

---

## 5. 架构优势总结（v3 vs v2）

| 特性                 | v2 架构                          | v3 架构                                                               |
| :------------------- | :------------------------------- | :-------------------------------------------------------------------- |
| **头部大小**         | 16 字节，固定                   | **2048 字节（2KB），可容纳复杂元数据**                         |
| **元数据存储**       | 仅依赖 Manifest（需解析 JSON）   | **L1.5 头部内嵌（CBOR），快速访问，支持大许可号**                  |
| **哈希路由**         | 依赖文件路径或全文件 Hash        | **依赖 SpecialID，文件移动不影响 ID，分布式存储友好**           |
| **占位符生成**       | 无                               | **2000B 加密随机数，哈希分布均匀，绝对唯一**                  |
| **向后兼容**         | -                                | **通过 Version 字段自动识别，v2 读取器可跳过 v3 头部**           |
| **扩展性**           | 需修改 Manifest JSON 结构       | **CBOR 可灵活增减字段，头部结构固定，无需变更**                |
| **I/O 开销**         | 极小（16B）                      | **可接受（2KB vs 数 GB），一次性读取头部，无需二次 Manifest 解析** |

---

## 6. 兼容性策略

*   **版本识别**:
    *   读取文件头部前 4 字节 Magic "ENCV"。
    *   读取 Version 字段：
        *   `0x02`: v2 容器，使用 `EnvelopeHeader_v2`（16 字节）。
        *   `0x03`: v3 容器，使用 `EnvelopeHeaderV3`（2048 字节）。
*   **旧版本读取器**:
    *   读取到 Version=0x03 时，可视为未知版本，跳过头部解析，直接读取 Footer。
    *   由于 Header 长度变化，需根据 Version 计算 Header 偏移量。
*   **新版本读取器**:
    *   优先读取 L1.5 SpecialID，获取高密度元数据。
    *   仍需读取 L2 Manifest 获取完整文件索引。

---

## 结论

ENCV v3 架构通过大幅扩展信封头部并引入 L1.5 元数据层，在保持 v2 "分层堡垒" 所有优势的基础上，显著提升了元数据存储的灵活性、分布式存储的友好度以及整体架构的可扩展性。2KB 的头部开销在动辄数 GB 的加密容器文件面前完全可以忽略，而其带来的架构红利（如无需 Manifest 解析即可获取关键元数据）则是巨大的。

**建议：**
*   新项目直接使用 v3 头部。
*   旧项目可通过渐进式升级（同时支持 v2/v3 读写）。
*   占位符生成算法必须使用加密级随机源，确保哈希分布的均匀性和唯一性。

