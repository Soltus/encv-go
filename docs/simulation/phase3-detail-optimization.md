# Phase 3 Spec — 细节补充 + 性能优化

> **阶段目标**：填充世界细节、全面性能优化、达到 Android <100MB 目标
> **状态**：v0.1 — 规划中
> **前置依赖**：Phase 1（后端核心）+ Phase 2（前后端联通）
> **后续阶段**：Phase 4（前端交互 + 前端性能）

---

## 一、阶段目标

本阶段是"量变到质变"的关键阶段：

1. ✅ **填充细节**：NPC 个性 / 组织生态 / 区域经济 / 事件系统 深度化
2. ✅ **性能优化**：内存从 ~140MB → <100MB，CPU 占用降低 30%+
3. ✅ **Android 适配**：针对移动端 CPU/GPU/内存特性做专项优化
4. ✅ **长稳验证**：30 分钟连续运行无内存泄漏、无性能退化

## 二、细节补充清单

### 2.1 NPC 系统深度化

| 方向 | 内容 | 优先级 |
|------|------|--------|
| 个性系统 | 大五人格 / 价值观 / 兴趣爱好 / 习惯 | P0 |
| 记忆系统 | 短期记忆 / 长期记忆 / 遗忘曲线 | P0 |
| 情绪系统 | 基础情绪 / 心境 / 情绪传染 | P1 |
| 技能树 | 多路径成长 / 天赋 / 专长 | P1 |
| 健康系统 | 疾病 / 受伤 / 康复 / 体质 | P2 |
| 外貌系统 | 外貌特征 / 穿着 / 标识物 | P2 |

### 2.2 组织系统深度化

| 方向 | 内容 | 优先级 |
|------|------|--------|
| 组织类型 | 家族 / 商会 / 行会 / 帮派 / 政府 / 宗教 | P0 |
| 层级结构 | 首领 / 高层 / 中层 / 基层 / 外围 | P0 |
| 组织生态 | 合作 / 竞争 / 敌对 / 吞并 / 分裂 | P1 |
| 资源流动 | 税收 / 分配 / 投资 / 破产 | P1 |
| 组织文化 | 价值观 / 传统 / 仪式 / 禁忌 | P2 |

### 2.3 区域系统深度化

| 方向 | 内容 | 优先级 |
|------|------|--------|
| 经济系统 | 生产 / 消费 / 贸易 / 价格波动 | P0 |
| 资源分布 | 自然资源 / 人工设施 / 交通线 | P0 |
| 人口流动 | 出生 / 死亡 / 迁徙 / 城市化 | P1 |
| 文化区 | 语言 / 习俗 / 建筑风格 / 美食 | P2 |

### 2.4 事件系统深度化

| 方向 | 内容 | 优先级 |
|------|------|--------|
| 事件类型 | 个人 / 家庭 / 组织 / 区域 / 世界 | P0 |
| 事件因果 | 前置条件 / 连锁反应 / 历史影响 | P1 |
| 干预系统 | 玩家介入 / 干预结果 / 蝴蝶效应 | P1 |

### 2.5 编年史系统（多维度历史记录）

> **核心问题**：世界运行了一段时间后，玩家想知道"发生过什么"——没有历史记录的世界是"扁平"的，没有深度。
> **核心思路**：建立五级编年史体系，从个人到世界，每个维度都有可追溯的时间线，支持按时间/实体/类型/重要性多维查询。

#### 2.5.1 五级编年史架构

```
┌─────────────────────────────────────────────────────────┐
│  L0 个人编年史 (Personal Chronicle)                      │
│  每个 NPC 的生命事件时间线                                │
│  出生 / 成长 / 职业 / 婚姻 / 子女 / 成就 / 死亡           │
│  存储：短期记忆 + 长期记忆 + 死亡后归档                   │
├─────────────────────────────────────────────────────────┤
│  L1 家庭编年史 (Family Chronicle)                        │
│  家族谱系 + 重大家庭事件                                  │
│  联姻 / 分家 / 传承 / 家族荣耀 / 家族衰落                 │
│  存储：按家族组织归档，L2 冷存储                          │
├─────────────────────────────────────────────────────────┤
│  L2 组织编年史 (Organization Chronicle)                  │
│  每个组织的发展时间线                                     │
│  创立 / 扩张 / 分裂 / 战争 / 领袖更迭 / 重大决策          │
│  存储：组织元数据 + 事件索引，L1/L2 温冷存储              │
├─────────────────────────────────────────────────────────┤
│  L3 区域编年史 (Regional Chronicle)                      │
│  每个地区的发展时间线                                     │
│  建设 / 灾难 / 战争 / 经济波动 / 人口迁徙 / 文化变迁      │
│  存储：按区域分片，L2 冷存储                              │
├─────────────────────────────────────────────────────────┤
│  L4 世界编年史 (World Chronicle) — 时代之书               │
│  世界级别重大事件 + 时代划分                               │
│  时代开始/结束 / 世界大战 / 大灾难 / 文明兴衰 / 神降临     │
│  存储：L1 温存储（高频访问），永久保留                     │
└─────────────────────────────────────────────────────────┘
```

#### 2.5.2 事件通用结构

每个编年史事件包含：

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | uint64 | 全局唯一事件 ID |
| `tick` | uint32 | 发生时间（世界 tick） |
| `level` | uint8 | 编年史层级 (0-4) |
| `type` | uint16 | 事件类型枚举 |
| `importance` | uint8 | 重要性 0-5（影响留存时间） |
| `entity_id` | uint64 | 主实体 ID（NPC/组织/区域） |
| `target_id` | uint64 | 目标实体 ID（可选） |
| `data_tag` | uint16 | 事件参数标签（用于分类/筛选） |
| `data_payload` | []byte | 事件详情（可选，CBOR 编码） |

**存储优化**：
- 基础事件 = 32 字节（固定字段），不含 payload
- payload 只在重要事件中携带（不重要的事件只有标签）
- 相同类型事件批量存储时，type/tag 用 delta 编码进一步压缩

#### 2.5.3 事件类型体系

**L0 个人事件**（继承 LifeEventType）：
- 出生 / 童年 / 青春期 / 成年 / 第一份工作 / 晋升 / 转职
- 结婚 / 离婚 / 生育 / 退休 / 衰老 / 疾病 / 死亡

**L1 家庭事件**：
- 家族创立 / 联姻 / 分家 / 继承人诞生 / 族长更迭
- 家族崛起 / 家族衰落 / 家族灭绝 / 家族荣耀事件

**L2 组织事件**：
- 组织创立 / 扩张 / 收缩 / 分裂 / 合并 / 解散
- 领袖更迭 / 政策变化 / 重大胜利 / 重大失败
- 战争宣战 / 战争结束 / 结盟 / 背盟

**L3 区域事件**：
- 城市建立 / 繁荣 / 衰落 / 毁灭
- 自然灾害（地震/洪水/旱灾/瘟疫）
- 人口迁徙 / 经济繁荣 / 经济衰退
- 文化兴起 / 宗教传播 / 技术突破

**L4 世界事件**：
- 时代开始 / 时代结束
- 世界大战 / 世界和平
- 大灾难 / 大奇迹
- 文明兴衰 / 神之降临 / 魔法潮汐

#### 2.5.4 重要性分级与留存策略

| 重要性 | 级别 | L0热存储 | L1温存储 | L2冷存储 | 留存时间 |
|--------|------|----------|----------|----------|----------|
| 0 - 琐碎 | ⚪ | 30天 | - | - | 30天 |
| 1 - 次要 | 🟢 | 90天 | 1年 | - | 1年 |
| 2 - 中等 | 🔵 | 永久 | 永久 | - | 永久 |
| 3 - 重要 | 🟡 | 永久 | 永久 | - | 永久 |
| 4 - 重大 | 🟠 | 永久 | 永久 | 永久 | 永久 |
| 5 - 史诗 | 🔴 | 永久 | 永久 | 永久 | 永久 |

**自动升降级**：
- 低重要性事件随时间自动降级 → 最终删除
- 高重要性事件永久保留，但从热存储移到冷存储
- 「被引用」的事件提升重要性（成为后续事件的 cause）

#### 2.5.5 因果链系统

每个事件可以有 0-N 个前置事件（cause），形成有向无环图：

```
时代变迁 → 战争爆发 → 城市被围 → 饥荒 → 暴乱 → 政权更迭
                                  ↘ 难民潮 → 文化交融 → 技术传播
```

**因果链用途**：
- 点击事件可以看"为什么会发生"
- 可以追溯"一切的开端"
- 玩家干预后可以观察连锁反应
- 生成历史故事线（因果链 → 叙事）

**存储优化**：
- cause 字段只存 1-3 个直接原因（太多的话只存最重要的）
- 完整因果链通过反向遍历重建（BFS/DFS）
- 因果关系存储在事件 payload 里，不占主结构空间

#### 2.5.6 时间线查询 API

| 查询维度 | 参数 | 用途 |
|---------|------|------|
| 按时间 | `from_tick`, `to_tick` | 某段时间发生了什么 |
| 按实体 | `entity_id`, `level` | 某 NPC / 组织 / 区域的历史 |
| 按类型 | `type`, `data_tag` | 筛选特定类型事件 |
| 按重要性 | `min_importance` | 只看大事 |
| 因果链 | `event_id`, `direction` | 向前追溯原因 / 向后看影响 |
| 世界时间线 | `era`, `page` | 世界史浏览（分页） |

**前端展示**：
- 时间线组件（纵向时间轴，事件卡片）
- 事件详情弹窗（含因果链导航）
- 时代切换器（L4 世界史按时代分组）
- 实体历史 Tab（NPC/组织/区域详情页的"历史"标签）

#### 2.5.7 性能与存储估算

**写入性能**：
- 每 tick 产生事件数：~100-1000 个（2000 NPC，大部分是琐碎事件）
- 写入方式：批量写入 + 内存 buffer，每秒刷盘一次
- 写入开销：< 1ms/tick（热存储直接写，冷存储异步）

**存储估算**（游戏内 100 年 = ~3.6M tick）：

| 层级 | 事件数 | 平均大小 | 总大小 |
|------|--------|----------|--------|
| L0 个人（存活NPC） | 2000 × 50 = 100K | 32B | 3.2 MB |
| L1 家庭 | ~500 家族 × 100 = 50K | 40B | 2 MB |
| L2 组织 | ~100 组织 × 500 = 50K | 48B | 2.4 MB |
| L3 区域 | 100 区域 × 1000 = 100K | 48B | 4.8 MB |
| L4 世界 | ~500 件 | 64B | 32 KB |
| **热存储合计** | **~300K 事件** | - | **~12.4 MB** |
| L2 冷存储（历史归档） | ~10M 事件（死亡NPC+旧事件） | 平均 20B（压缩） | ~200 MB |

**结论**：100 年历史，热存储 ~12MB，冷存储 ~200MB（zstd 压缩后 ~50MB），完全可控。

## 三、性能优化清单

### 3.1 内存优化（目标：140MB → <100MB）

| 优化项 | 预期收益 | 难度 | 优先级 |
|--------|---------|------|--------|
| 格数据压缩（48B → 25-30B） | -30MB | 中 | P0 |
| NPC 名字池化/字符串去重 | -5MB | 低 | P0 |
| 按需脑区加载（8个→只激活2-3个） | -50MB | 高 | P0 |
| 焦点层级缓存大小动态调整 | -10MB | 中 | P1 |
| 关系数据压缩（邻接表 → 位图+delta） | -3MB | 高 | P1 |
| 事件日志滚动 + 量化存储 | -5MB | 中 | P1 |
| 内存守卫 + OOM 时自动降级 | 稳定性 | 高 | P1 |
| GC 调优（GOGC 调整） | -10MB 碎片 | 低 | P2 |

### 3.2 CPU 优化

| 优化项 | 预期收益 | 难度 | 优先级 |
|--------|---------|------|--------|
| Catch-up 向量化 / SIMD | +30-50% | 高 | P0 |
| 批量处理（batch 1000+ NPC 一次算） | +20% | 中 | P0 |
| 事件调度器时间轮优化 | +15% | 中 | P1 |
| 随机数生成器优化（xoshiro256） | +10% | 低 | P1 |
| 热数据预加载（基于访问模式预测） | 延迟 -40% | 高 | P2 |

### 3.3 I/O 优化

| 优化项 | 预期收益 | 难度 | 优先级 |
|--------|---------|------|--------|
| ObjectBox 热数据 + SQLite 冷数据分层 | 读 +200% | 中 | P0 |
| 批量写入（攒批 + 事务） | 写 +500% | 中 | P0 |
| 异步持久化（写后缓存） | 延迟 -80% | 高 | P1 |
| 增量快照（只存变化） | 磁盘 -90% | 高 | P2 |

### 3.4 存储分层优化（对抗无限膨胀）

> **核心问题**：世界系统随运行时间无限膨胀——事件日志、关系变化、生命事件、脑系统数据...永不删除的话，Android 设备存储迟早撑爆。
> **核心思路**：参考云厂商对象存储分层（S3 Standard → IA → Glacier → Deep Archive），按数据温度分层，牺牲冷数据读写性能换取存储大幅减少。

#### 3.4.1 四层存储架构

```
┌─────────────────────────────────────────────────────┐
│  L0 热存储 (Hot) — 内存 + 热数据库表                 │
│  目标：读写最快，容纳当前活跃数据                      │
│  内容：焦点NPC / 活跃组织 / 当前事件 / 近期关系       │
│  容量：< 100 MB（内存）+ < 500 MB（磁盘）            │
│  保留期：游戏内 30 天 / 现实 7 天                    │
├─────────────────────────────────────────────────────┤
│  L1 温存储 (Warm) — 数据库 + 轻量压缩                │
│  目标：平衡性能与空间，容纳近期历史                    │
│  内容：非焦点NPC / 历史事件(近30天) / 旧关系快照      │
│  容量：< 2 GB                                        │
│  保留期：游戏内 1 年 / 现实 30 天                    │
├─────────────────────────────────────────────────────┤
│  L2 冷存储 (Cold) — 磁盘文件 + 高压缩                │
│  目标：空间最小，可接受秒级读取延迟                    │
│  内容：死亡NPC档案 / 历史事件归档 / 旧快照            │
│  格式：zstd 压缩的二进制分片（每游戏年一个分片）      │
│  容量：< 5 GB（游戏内 100 年估算）                   │
│  保留期：永久（但可手动清理旧时代）                   │
├─────────────────────────────────────────────────────┤
│  L3 极冷 (Frozen) — 统计代理，无原始数据             │
│  目标：完全不存个体，只存分布参数                      │
│  内容：远古时代NPC → 只存人口统计/分布函数/时代特征   │
│  容量：< 1 MB / 时代                                  │
│  保留期：永久                                         │
└─────────────────────────────────────────────────────┘
```

#### 3.4.2 核心优化策略

**策略 1：事件溯源 + 周期快照（Event Sourcing + Periodic Snapshot）**

不存每 tick 的完整状态，只存事件流 + 每 N tick 存一次快照：
```
状态(t) = 最近快照 + Σ(从快照到t的事件增量)
```

| 快照层级 | 间隔 | 保留数量 | 用途 |
|---------|------|---------|------|
| Tick 快照 | 100 tick | 10 个 | 近期回滚 / 增量同步 |
| 日快照 | 1 游戏日 | 30 个 | 日级查询 / 对比 |
| 月快照 | 1 游戏月 | 12 个 | 月级统计 / 时代划分 |
| 年快照 | 1 游戏年 | 永久（L2） | 年级历史 / 时代特征 |

**收益**：磁盘占用减少 90%+（事件比状态小得多 + 快照稀疏）。

---

**策略 2：死亡 NPC 数据降级（Death Downgrade）**

NPC 死亡后立即从 L0 热存储移除，按以下分级处理：

| 死亡时长 | 存储层级 | 保留内容 | 可查询性 |
|---------|---------|---------|---------|
| < 30 天 | L1 | 完整属性 + 死亡前 100 条事件 | 完整查询 |
| 30 天 ~ 1 年 | L2 压缩 | 核心属性 + 生命事件摘要 + 关系统计 | 需解压（秒级） |
| > 1 年 / 无名 NPC | L3 统计 | 只计入死亡人口统计 / 物种分布 | 不可查个体 |

**特殊保留**：玩家相关 NPC、著名 NPC（有过重大事件的）永久保留在 L2。

**收益**：死亡 NPC 占总人口的 90%+（长时间运行后），降级后热存储压力锐减。

---

**策略 3：关系网稀疏化 + 折叠（Graph Sparsification）**

关系是 O(N²) 级别的存储炸弹，必须做稀疏化：

| 关系类型 | 存储策略 | 折叠方式 |
|---------|---------|---------|
| 直系亲属（父母/子女/配偶） | 永久存 L1 | 不折叠 |
| 朋友 / 同事 | 只存活跃的 top-20 | 其余折叠为「认识 N 人」统计 |
| 弱关系（一面之缘） | 只存计数，不存个体 | 直接折叠为数字 |
| 组织成员关系 | 存成员 ID 列表 | 超过 1000 人 → 存分页 + 统计 |
| 敌对 / 竞争 | 只存活跃冲突 | 历史冲突折叠为事件摘要 |

**查询时的透明降级**：
- 问「张三的朋友有哪些」→ 返回 top-20 + 「还有 347 个普通朋友」
- 问「张三和李四认识吗」→ 弱关系也能查（从计数反向 bloom filter 验证）

**收益**：关系存储量减少 95%+，从 O(N²) 降到 O(N)。

---

**策略 4：事件日志分层压缩**

事件是最大的存储消耗源，分层处理：

| 事件年龄 | 存储层级 | 编码方式 | 压缩比 |
|---------|---------|---------|--------|
| < 1 游戏日 | L0 内存环形缓冲 | 原生 struct | 1x |
| 1 日 ~ 30 日 | L1 数据库 | delta 编码 + 列存 | 3~5x |
| 30 日 ~ 1 年 | L2 分片文件 | zstd 压缩二进制 | 10~20x |
| > 1 年 | L3 统计聚合 | 只存事件类型分布 / 频率 | 100x+ |

**事件 delta 编码**：同一 NPC 的连续事件，只存变化字段（比如生命事件只存类型+时间，不重复存 ID/名字等）。

**按时间分片**：每游戏月一个分片文件，旧分片整体压缩，永不修改（append-only）。

---

**策略 5：脑系统按需生成（Stateless Brain）**

脑系统是存储的另一大巨头——100 个焦点 NPC × 1000 格 = 10 万格，如果 1 万 NPC 都有脑就是 1 亿格。

**优化思路**：
- **焦点 NPC（~100 个）**：完整脑 + 格缓存，存在 L0
- **非焦点 NPC**：脑是**无状态**的——需要时从 NPC 属性 + 随机种子**临时生成**，用完丢弃
- **格记忆**：只存焦点格的长期记忆，其余格的状态完全由种子 + 当前 tick 决定
- **重要记忆锚点**：比如「初恋」「丧亲」等重大事件锚点存在 NPC 属性里，作为脑生成的固定参数

**本质**：脑不是存出来的，是算出来的。NPC 的性格/记忆/偏好都编码在其属性 + 生命事件里，脑系统只是把这些属性「表现」出来。

**收益**：非焦点 NPC 的脑存储 = 0，减少 99% 的脑系统存储。

---

**策略 6：统计代理模式（Statistical Proxy）**

这是整个架构对抗规模的根本策略——**绝大多数实体根本不存**。

| 实体规模 | 存储方式 | 占总存储比 |
|---------|---------|-----------|
| 焦点 NPC（~100） | 全量属性 + 完整脑 + 完整关系 | ~50% |
| 热 NPC（~10K） | 全量属性 + 缓存 | ~30% |
| 温 NPC（~100K） | 数据库存储，按需加载 | ~15% |
| 冷 NPC（~10M-1） | 不存个体，只存统计分布 | ~5% |

**统计代理的查询模式**：
- 「查询 ID=9999999 的 NPC」→ 用 ID 做种子，**实时生成**属性（确定性算法）
- 「全城有多少铁匠？」→ 从职业分布统计直接返回
- 「给我找一个精灵族法师」→ 从分布 + 种子生成一个符合条件的虚拟 NPC

**一致性保证**：同一个 ID 每次生成的结果完全一致（确定性算法 + 固定种子），用户察觉不到区别。

**收益**：10M NPC 实际只存 100K，存储减少 99%。

#### 3.4.3 数据迁移流水线

```
L0 热 ──(老化策略)──> L1 温 ──(月度归档)──> L2 冷 ──(年度聚合)──> L3 统计
        ↑                    ↑                    ↑
    加载回热              解压查询              统计反推
    （重新活跃）          （用户查询）          （只能看趋势）
```

**迁移触发**：
- 按时间自动迁移（每日 cron 检查老化数据）
- 按存储容量触发（超过阈值强制迁移最老数据）
- 手动触发（用户「清理旧数据」功能）

#### 3.4.4 存储预算与自动治理

| 层级 | 硬预算 | 超出时行为 |
|------|--------|-----------|
| L0 内存 | 100 MB | 淘汰最久未用的 NPC / 格 |
| L0 磁盘（热库） | 500 MB | 迁移最老数据到 L1 |
| L1 温库 | 2 GB | 迁移最老分片到 L2 |
| L2 冷存储 | 5 GB | 提示用户清理，或自动聚合最老时代到 L3 |
| L3 统计 | 无上限（可忽略） | — |

**自动治理任务**（每日运行）：
1. 检查各层存储用量
2. 迁移超时数据到下一层
3. 超过硬预算 → 强制淘汰 / 压缩
4. VACUUM 数据库回收空间
5. 生成存储用量报告

#### 3.4.5 预期效果

| 指标 | 优化前 | 优化后 | 缩减比 |
|------|--------|--------|--------|
| 10 年运行总存储 | ~50 GB（估算） | < 3 GB | ~94% |
| 热存储（活跃） | 随时间增长 | < 500 MB | 有界 |
| NPC 人均存储 | ~200 B（全存） | ~2 B（统计代理） | ~99% |
| 事件存储/游戏年 | ~1 GB | ~50 MB（L2压缩） | ~95% |
| 关系存储 | O(N²) 爆炸 | O(N) 可控 | ~99% |

### 3.5 Android 专项优化

| 优化项 | 说明 | 优先级 |
|--------|------|--------|
| 后台 60% 降频 | Doze / App Standby 适配 | P0 |
| 前台/后台切换平滑过渡 | 避免切换时卡顿 | P0 |
| 温控调频 | 设备发热时自动降频 | P1 |
| 电量优化 | 低电量模式进一步降频 | P1 |
| 内存 Pressure 监听 | 内存紧张时自动降级 | P0 |
| Go runtime Android 调优 | GOGC / GOMEMLIMIT | P1 |

## 四、三级降级规范

### L1 降级（功能禁用）

| 触发条件 | 降级动作 | 用户感知 |
|---------|---------|---------|
| OOM 风险 | 释放非核心格缓存 | 部分 NPC 脑内数据加载变慢 |
| 严重温控 | 降到后台 0.6x 速率 | 世界变化变慢 |

### L2 降级（降级体验）

| 触发条件 | 降级动作 | 用户感知 |
|---------|---------|---------|
| 内存紧张 | 格模拟深度从 L3 → L2 → L1 | 脑内细节减少 |
| CPU 紧张 | 事件速率从 3x → 1x → 0.6x | 世界推进变慢 |
| 低电量 | 后台禁用格模拟 | 离开 App 时世界只推进宏观 |

### L3 降级（透明降级）

| 触发条件 | 降级动作 | 用户感知 |
|---------|---------|---------|
| 网络波动 | 本地缓存兜底 | 无感知 |
| 前端 FPS 下降 | 推送节流 | 几乎无感知 |

## 五、验证体系

### 5.1 性能基准测试

```bash
# 内存基准
go test -bench "BenchmarkMemory" -benchmem ./internal/simverse/

# CPU 基准
go test -bench "BenchmarkCatchUp|BenchmarkEvent" -benchmem ./internal/simverse/

# 数据库基准
go test -bench "BenchmarkSQLite|BenchmarkObjectBox" -benchmem ./internal/simverse/
```

### 5.2 长稳测试

长稳测试（Stability Test）是验证系统在持续运行下是否存在内存泄漏、性能退化、资源泄漏等问题的关键手段。

#### 5.2.1 测试分类

| 测试项 | 时长 | 指标阈值 | 触发方式 |
|--------|------|---------|---------|
| 内存泄漏检测 | 30 分钟 | HeapInuse 增长 < 5% | CI 定时 + 手动 |
| 性能退化检测 | 30 分钟 | tick 速率下降 < 10% | CI 定时 + 手动 |
| 数据库膨胀检测 | 30 分钟 | DB 文件增长 < 10% | CI 定时 + 手动 |
| Goroutine 泄漏检测 | 30 分钟 | goroutine 数波动 < 20% | CI 定时 + 手动 |
| 句柄泄漏检测 | 30 分钟 | 打开文件句柄稳定 | 手动 / 真机 |
| 长周期仿真 | 8 小时 | 世界生态不崩溃 | 手动 / 夜间 |

#### 5.2.2 测试方法

```bash
# 方式一：内置长稳测试（3 分钟，快速验证）
go test -run TestStability_3MinuteRun -timeout 5m ./internal/simverse/ -v

# 方式二：30 分钟完整长稳测试（CI 用）
ENCV_STABILITY_FULL=1 go test -run TestStability_Full30Min -timeout 45m ./internal/simverse/ -v

# 方式三：Go 内置 pprof 持续采样
go test -run TestStability_Full30Min -memprofile mem.prof -cpuprofile cpu.prof ./internal/simverse/
```

#### 5.2.3 观测指标

每 30 秒采样一次，记录以下指标：

| 指标类 | 具体指标 | 获取方式 |
|--------|---------|---------|
| 内存 | HeapInuse / HeapAlloc / HeapObjects | runtime.ReadMemStats |
| 内存 | GC 次数 / GC 停顿时间 | runtime.ReadMemStats |
| CPU | tick 耗时 / 每 tick 事件数 | 内部计时 |
| 协程 | goroutine 数量 | runtime.NumGoroutine |
| 缓存 | NPC 缓存命中率 / 大小 | cache.HitRate() / cache.Len() |
| 世界 | 存活 NPC 数 / 组织数 / 事件数 | world.Stats() |
| 数据库 | DB 文件大小 / 读写次数 | SQLite / ObjectBox stats |

#### 5.2.4 通过标准

1. **内存泄漏**：30 分钟内 HeapInuse 增长 < 5%（排除 GC 周期波动）
2. **性能退化**：前 5 分钟平均 tick 耗时 vs 最后 5 分钟，下降 < 10%
3. **Goroutine 泄漏**：稳定阶段 goroutine 数波动 < 20%
4. **数据一致性**：随机抽查 100 个 NPC，数据符合统计分布
5. **世界自洽**：人口/经济/组织数量不出现指数级爆炸或归零

#### 5.2.5 CI 工作流

独立工作流：`.github/workflows/simverse-stability.yml`

- **触发方式**：`schedule`（每日凌晨） + `workflow_dispatch`（手动） + `ci:stability` 标签
- **运行时长**：约 35 分钟（含准备 + 清理）
- **失败处理**：发 issue + 标记 `stability-fail` 标签
- **产物**：pprof 文件 + 测试报告 + 内存趋势图

详见 [simverse-stability.yml](file:///workspace/.github/workflows/simverse-stability.yml)

### 5.3 Android 真机验证

| 测试项 | 设备 | 指标 |
|--------|------|------|
| 前台内存 | 中端 Android | < 150 MB (PSS) |
| 后台内存 | 中端 Android | < 80 MB |
| 前台 CPU | 中端 Android | < 10% |
| 后台 CPU | 中端 Android | < 2% |
| 每小时耗电 | 中端 Android | < 1% / 小时 |
| 冷启动时间 | 中端 Android | < 3 秒 |

## 六、与其他阶段的关联

### 依赖 Phase 1 + 2
- 后端核心引擎
- 前后端 API 接口
- 性能基线数据

### 输出给 Phase 4
- 优化后的 API 性能数据
- Android 真机性能报告
- 降级规范 + 错误码
- 前端性能优化建议

## 七、验收标准

1. ✅ 所有细节系统可通过 API 查询
2. ✅ Android 前台内存 < 100 MB（优化目标）
3. ✅ 前台模式下前端 FPS > 55
4. ✅ 30 分钟长稳测试无内存泄漏
5. ✅ 三级降级机制全部可用
6. ✅ 事件系统可追溯（因果链完整）
7. ✅ 世界生态自洽（人口/经济不崩溃）

## 八、风险与缓解

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|---------|
| 细节爆炸导致维护困难 | 中 | 高 | 严格分形复用 + 统一抽象 |
| 内存优化后数据一致性问题 | 中 | 高 | 压缩/解压单元测试 + fuzz |
| Android 机型碎片化 | 高 | 中 | 分级适配 + 降级兜底 |
| 长稳测试暴露深层 bug | 高 | 中 | 分阶段验证 + 快速回滚 |
| 性能优化过度导致可读性下降 | 中 | 中 | 保留未优化版本作为对照 |
