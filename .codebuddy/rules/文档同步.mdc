---
description: 重大改动后必须同步更新相关文档（THEME_DEV.md / 方案文档 / 本仓库 rules），避免跨会话失联、上下文漂移
alwaysApply: true
enabled: true
updatedAt: 2026-07-16T05:53:00.000Z
provider: 
---

每次做完有架构/契约影响的改动，都要同步更新文档，避免下一会话（或用户）失联、拿不到最新事实。

- **触发条件**：动了公开 API / 主题契约 / 构建管线的依赖或架构（如增删 devDep、改 vite 配置、改 surface/ui-*/token 词汇、改 `@use/@forward` 模块边界、推翻既往决策）。
- **必须更新的文档**（按影响面选）：
  - 主题/表面相关改动 → `packages/shared-components/src/theme/THEME_DEV.md`（新增一节记录决策 + 推翻的旧结论，并显式标注「§X 已被 §Y 取代」）。
  - 依赖/构建改动 → 相应方案文档（如 `ENCV前端主题重构方案.md`）+ 必要时 `.codebuddy/rules`。
  - 推翻既往结论时，**不要只新增**，要在旧结论处加「已废除 / 已被 §Y 取代」标注，避免自相矛盾误导下一任。
- **纪律**：文档更新与代码改动同一次交付，不拖延到「以后」。门禁（`app_check_all`）只验代码不验文档——文档正确性由人工在本次提交前保证。
- **代价**：不更新文档会导致下一会话基于过时事实决策（如再次清理「已复活」的依赖、沿用已废弃的类词汇），造成反复返工。
