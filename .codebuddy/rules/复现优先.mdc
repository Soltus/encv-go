---
description: 先复现问题再修复问题（UI 问题必须真实重现），严禁猜测改、乐观测试、不固定经验
alwaysApply: true
enabled: true
updatedAt: 2026-07-16T22:44:00.000Z
provider: 
---

修 bug 的第一性原理：**没有可复现的失败，就没有可信的修复。** 任何「大概是这里」的直觉都必须先被一次真实重现证伪或证实，再动代码。

## 铁律

1. **先复现，再修复**
   - 动任何修复代码前，必须先拿到**可稳定重现的失败证据**（失败的测试 / 真实运行的错误日志 / 真机或浏览器里亲眼可见的现象）。
   - 复现不了，就先投资复现（写最小重现、加日志、起真实运行环境），**不要**跳过复现直接改。
   - 复现步骤与现象要写进交付说明（改了什么、怎么复现的、修好后同一步骤如何验证）。

2. **UI 问题必须真实重现**（不接受「读代码推断」代替）
   - UI/交互类 bug（空白页、错位、不响应、样式错、动效卡死等）**必须在真实渲染环境里亲眼重现**：起 dev server 用浏览器、或跑组件/E2E 测试断言真实 DOM/样式（如 `getComputedStyle` 的 `opacity`/`display`/`visibility`/位置），或在真机/APK 上重现。
   - **只读源码 + 逻辑推断 = 猜测，禁止据此直接改 UI**。源码分析只能用来**形成假设**，假设必须被一次真实重现验证后才算成立。
   - 修复后必须用**同一条真实路径**再验证一次现象消失（不是只看代码「应该好了」）。

3. **严禁猜测修改**
   - 不允许「改了再看看」「多半是这个」式提交。每一处改动都要能对应到一条被证实的根因。
   - 有多个候选根因时，先用复现/日志把范围收敛到唯一根因，再改；不要一次撒网改多处。

4. **严禁乐观测试**
   - 不允许「跑通了就默认覆盖到了 bug」。测试必须**先红后绿**：新增/修改的用例要能在修复前复现失败（红），修复后转绿——证明它真的守住了这个 bug。
   - 不允许用不相关/太弱的断言充数（如只断言「没抛异常」却不校验真实现象）。UI 修复的测试要断言可观测的真实状态。
   - 门禁（`app_check_all` 等）全绿是**必要非充分**条件；还要有针对本 bug 的、先红后绿的专项验证。

5. **严禁不固定经验**
   - 每次定位到的**真实根因 + 复现方法 + 修复方式 + 防复发手段**必须固化下来：
     - 写入工作记忆 `/.codebuddy/memory/YYYY-MM-DD.md`（当日）/ `MEMORY.md`（长期稳定结论）；
     - 有架构/契约影响的，按 `文档同步.mdc` 同步到对应文档；
     - 能加**契约锁/回归测试**的必须加（先红后绿那条即固化为回归测试），防止同类问题再犯。
   - 不允许「修完就忘」——下一会话必须能从记忆/文档/测试里拿到本次经验。

## 与其它规则的关系
- 所有实质命令（起 dev server、跑测试、构建）走 MCP（`app_exec` / `app_check_all` 等），不用裸终端（见 `环境保持.mdc`）。
- 修复带来的架构/契约变化按 `文档同步.mdc` 同步文档。
