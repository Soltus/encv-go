# 2026-07-15

## 并行推进：K9 / K15 / K41 三批同做（用户「并行推进」）

- **续6(主题 ACL 解耦，2026-07-15 20:40)**：用户要求「即使更换 gsap+daisyui 技术栈，下游应用/插件完全无感」。设计落地防腐层（ACL）：
  - **动效（gsap）**：新建 `packages/shared-components/src/motion/internal/` —— `types.ts`(引擎无关契约 `MotionEngine`/`MotionTarget`/`MotionVars`/`MotionTween`/`MotionContext`/`ScrollTriggerHandle`/`FlipState`/`ScrollTriggerConfig`，刻意不 import 任何动画库) + `gsap-engine.ts`(**全仓唯一 `import gsap` 处**，实现 `MotionEngine`，EASE_MAP 把语义缓动映射到 gsap 缓动) + `index.ts`(桶导出，注释标明「换技术栈唯一开关：改这一行导出」)。12 个 composable + `guard.ts` + `index.ts` 全部改为 `import { motion } from "./internal"`，移除直接 `import gsap` 与 `gsap.TweenVars`/`gsap.core.Tween`/`gsap.Context`/`ScrollTrigger`/`Flip` 类型泄漏（公共签名改用我们的类型）。`tokens.ts` 的 `EASE` 由 gsap 原生串(`power2.out`等)改为语义键(`out/inOut/back/linear/follow/float`)，由引擎映射。
  - **主题（daisyui）**：稳定契约 = CSS 变量(`palette.css` 纯 CSS 单一来源) + `.encv-*` 组件/工具类(`theme/components.css`，**纯 CSS、零 `@apply`、只吃 `--color-*`/`--radius-*` 令牌**)。新建 `components.css`，并在 `daisyui.css`、`theme-core.css` 顶部 `@import`。移除 `daisyui.css` 里泄漏 daisyui 类的 `@layer components{ .encv-card{@apply bg-base-100...} .encv-btn{@apply btn btn-primary} }` 块（换为 components.css 的令牌版）。换肤不变词汇成立：下游/组件用 `.encv-*` 与 CSS 变量，daisyui 的 `@plugin` 块是唯一切换点。
  - 已 grep 确认：下游(encv-mobile+各插件)零 `import gsap`、零 `bg-base-*`/`text-primary` 等 daisyui 工具类——消费边界本就干净，解耦纯内部 ACL。
  - **门禁**：`app_check_all` **9 PASS / 0 FAIL**(Biome/各包 typecheck/i18n/单测/encv-mobile vite build 全绿)。修正两处 TS：Flip.from 的 vars 需 cast 为 `Parameters<typeof Flip.from>[1]`（gsap.FromToVars 未导出）；format 经 `app_format` 修。
  - **结论/契约**：换 gsap→anime.js 或换 daisyui→其他，仅需 (a) 新增一个 `MotionEngine` 实现并改 `internal/index.ts` 一行；(b) 重写 `daisyui.css` 的 `@plugin` 块（palette.css/components.css 不动）。下游应用与各插件零改动。

用户一句「并行推进」→ 同时推进 3 个独立、低/中风险的 PURIFY backlog 项（文件互不重叠，可并行）。全部**代码 + 文档已落地**，仅门禁因终端再次不可用未跑。

### K9 · `runWithConcurrency` 提升为 lib 工具 ✅ 代码完成
- 新建 `packages/shared-components/src/lib/concurrency.ts`：`runWithConcurrency<T,R>(items, fn, max=5)`，签名/行为与原 `useBatchOperations.ts` 内联版**逐字一致**（含 `catch` 内 `{ok:false,error:String(err)} as any` 错误位填充，调用方 `r?.ok` 判成败不变）。
- `useBatchOperations.ts`：删内联定义，改 `import { runWithConcurrency } from "@encv/shared-components/lib/concurrency"`；`batchRetry/Cancel/Delete` 三处调用点不变。

### K15 · 模块级单例样板收敛 ✅ 代码完成
- 新建 `packages/shared-components/src/lib/singleton.ts`：`defineSingleton<T>(factory): { get, reset }`。
- 收敛 **3 个干净站点**（单 `_instance` + 独立 `__resetXForTests`）：`useRunSummaries.ts` / `runTasksStore.ts` / `useApiBaseProbe.ts` → `const _x = defineSingleton(factory)` + 导出 `useXSingleton()` 委托 `_x.get()` + `__resetXForTests()` 委托 `_x.reset()`。**导出名/调用方全不变**。
- **有意不动（避免错误抽象，呼应 K17/K22/K43 纪律）**：`useWorkflowTaskService.ts`（额外缓存 `_cachedOptions`）、`useRealtimeTransport.ts`（`_forcedMode` 与 `_instance` 同生命周期、reset 在实例方法内）、`useTasksList.ts`（双实例 `_viewInstance`+`_tasksListInstance`）——属「设计内差异」非简单单例，强行套用即错误抽象。

### K41 · 截断工具族收敛 ✅ 代码完成
- `lib/format.ts` 新增 `truncateText(text, max=60, ellipsis="...", mode:"tail"|"head"="tail")`（`tail`=保留末尾/省略符在前；`head`=保留开头/省略符在后）。
- `truncatePath` 改委托 `truncateText(p, max, ellipsis)`（tail，行为不变）。
- `buildReportZip.ts` 的 `truncateError` 改 `truncateText(firstLine, max, "...", "head")`——**精确还原原 head 保留语义**（`firstLine.slice(0,max-3)+"..."`），非盲目套 tail（错误截开头更有用）。

### K18 文档状态纠正
- `REFACTOR_PURIFY.md` K18 验证状态由「门禁待跑」改「已验证 PASS 8/FAIL 0」（2026-07-14 终端恢复后多次绿，含本批改动）。

### 门禁已跑通并验证（2026-07-15 续，用户「继续」）
- 终端恢复后，用本地 biome 二进制对 8 文件 `biome format --write`（EXIT=0，Formatted 8 files / Fixed 2 files）。
- `node scripts/check-all.mjs` → **PASS 8 / FAIL 0 全绿**（Biome CI + 6 处 typecheck + i18n lint + encv-mobile 单测全过）。
- `REFACTOR_PURIFY.md` 中 K9 / K15 / K41 三段「门禁待跑」已改「已验证 PASS 8/FAIL 0 全绿」；顺带把 K8 段落陈旧「门禁待跑」标记也改为已验证（K8 此前已绿）。
- **结论**：K9/K15/K41 三批代码 + 文档 + 门禁全部闭环。低/中风险并行推进完成。

### check:all 新增 build 套件（用户「check:all加上build验证并跑一次」）
- `scripts/check-all.mjs`：
  - 顶部文档注释「覆盖」列表补第 9 项：`encv-mobile 构建校验（vite build）`。
  - `SUITES` 数组末尾新增套件 `encv-mobile build (vite)`：`cwd=encv-mobile`，`cmd=pnpm`，`args=["exec","vite","build"]`，`timeoutMs=600_000`。
  - 取舍：只跑 `vite build`（纯 web 打包），**不**跑 `pnpm build`（后者含 `typecheck`，与套件 2 重复）。捕获「类型通过但构建期才报错」的回归（动态 import / 资源缺失 / 插件配置等）。build 套件默认常开（不随 `--no-tests` 跳过）。
- **运行受阻**：发出 `node scripts/check-all.mjs` 后 harness 再次报 10s 无法启动；连 `echo alive`/`ls`/`ps` 探针均返回 error → 确认是**终端真死/连接断开**（非 10s 误报）。按规则已停止发命令，待用户恢复终端后重跑。
- **待执行（用户侧恢复终端后）**：`cd /workspace/app && node scripts/check-all.mjs`（输出含第 9 项 build；报告写 `check-report.md`）。若仍 flaky，可 `node scripts/check-all.mjs > /tmp/cb.log 2>&1; echo EXIT=$? >> /tmp/cb.log` 后 `read_file /tmp/cb.log`。

### build 套件首跑即抓到真实回归（核心产出）
- 用户把默认终端改成 bash 后跑通：旧 `/tmp/check_build.log`（00:39）显示 `encv-mobile build (vite)` **FAIL 1**，其余 8 PASS。
- **报错**：`[UNLOADABLE_DEPENDENCY] Could not load src/api/encv` @ `src/stores/registerSharedTaskServices.ts:2` import from `@/api/encv`。
- **根因（不对称配置 = 批 9 重构没做完的断链）**：
  - `tsconfig.json:22-23`：`"@/*": ["./src/*", "../packages/shared-components/src/*"]` —— tsconfig `paths` **仍保留第二元素指向 shared**，故 `@/api/encv` 在 typecheck 里解析到 `shared-components/src/api/encv` → typecheck 全绿（蒙混过关）。
  - `vite.config.ts:278-280`：`encv-alias-fallback` 插件被批 9 **故意改成只解析本地 `src`**（注释："shared 兜底已成死代码，@/ 严格只解析本地"），删了 shared 兜底分支。
  - 批 9 删了 `src/api/encv.ts` 等实现文件，并**打算**把所有 `@/api/encv` 调用点改写成 `@encv/shared-components/api/encv`，但**漏了 39 处**（App.vue / Settings.vue / registerSharedTaskServices.ts / 多个 views / __tests__ 等）。vite/rolldown build 走严格本地解析 → 找不到文件 → 构建挂。
- **修复方向（待用户拍板，高爆破半径）**：
  - (B) 迁移 39 处 `@/api/encv` → `@encv/shared-components/api/encv`（批 9 的**预期终态**，但 39 处机械改动，需重跑 build+typecheck+单测）；
  - (A) 在 `src/api/encv.ts` 重新加 1 行 shim `export * from "@encv/shared-components/api/encv"`（最小改动、build 立刻绿，但**回退**了批 9"删 app 垫片"的意图）；
  - (C) vite alias 恢复 `@/api/*`→shared 兜底（直接**违背**批 9 显式决策，不推荐）。
- **状态**：已向用户报告根因并请其选择修复方向；未擅自做 39 处迁移或回退垫片（高爆破半径 + 涉及既有重构意图）。build 套件本身验证有效（成功捕获类型过/构建挂的回归）。

### 用户提议：给 codemogger-patch 加 MCP/HTTP API 以绕开终端（2026-07-15 01:04）
- 背景：用户被终端 flaky（10s 误报 / 真死）困扰，想"不依赖终端查询"。
- **前提纠正（已告知用户）**：agent 已有独立于终端的工具（read_file / search_content / search_file / list_dir / write_to_file / replace_in_file / delete_file），全部走文件系统，不经过 `execute_command`。flaky 的只有 `execute_command`（执行类：node/sed/git/biome）。查询类本就不碰终端；执行类用"输出重定向到日志 + read_file 验证"手法已能绕开大部分 flaky（build 验证即如此成功）。
- **MCP 可行性**：给 `codemogger-patch` 包 MCP server（把 `references`/`context`/`search`/`impact`/`leaks` 暴露为 tool），用户在 CodeBuddy MCP 设置注册后，agent 即可工具调用查代码图谱、不碰终端。但：① agent 不能自注册 MCP（需用户侧 Integration/设置）；② server 常驻需保活（规则禁止误杀）。HTTP API 对我帮助有限（无 localhost 调用工具，WebFetch 仅外部网页内容分析、且或拦 localhost）。
- **范围判断**：MCP 只能替代"查询"终端，替代不了"跑构建/测试"终端。codemogger 的 `impact`/`leaks` 恰是重构溯源利器（如本次 39 处 `@/api/encv` 溯源），MCP 化价值高、范围小。
- **状态**：用户批准（"直接写入/root/.codebuddy/mcp.json即可注册"）。**已实现并注册**：
  - 新建 `/workspace/app/codemogger-patch/mcp-server.mjs`：Node stdio JSON-RPC MCP server，包装 `./codemogger-shim`，暴露 7 个 tool：`codemogger_references`(mode symbol|module|file)、`codemogger_context`(expand)、`codemogger_search`、`codemogger_impact`、`codemogger_leaks`、`codemogger_index`、`codemogger_list`。每次调用都带 `--db <CODEMOGGER_ROOT>/.codemogger/index.db`（shim 据此推导 codebase dir）。默认 `CODEMOGGER_NO_AUTOINDEX=1`（避免 MCP 请求超时；需新鲜度时显式调 `codemogger_index`）。
  - 写入 `/root/.codebuddy/mcp.json`：`mcpServers.codemogger` = `{command:"node", args:["/workspace/app/codemogger-patch/mcp-server.mjs"], env:{CODEMOGGER_ROOT:"/workspace/app/encv-mobile"}}`。
  - **待验证**：终端 flaky 导致 `node --check` + 握手冒烟测试未跑成。语法/握手需 CodeBuddy 重启加载 MCP 后由用户侧验证；首次使用前应先调 `codemogger_index` 建索引（AUTOINDEX 默认关）。

### codemogger MCP 已写入 README + .cnb.yml，并实跑验证（2026-07-15 01:19+）
- README 新增 `## MCP server (terminal-free queries)` 章节：7 个 tool 表、`~/.codebuddy/mcp.json` 注册示例、idempotent one-liner、env 说明（CODEMOGGER_ROOT/SHIM/AUTOINDEX）。
- `.cnb.yml` 的 `vscode.stages` 新增 stage `构建 codemogger-patch 并注册 MCP`：
  - **自包含**：先 `npm install -g codemogger@0.1.5`（patch 基准版本，README 说 npm 现服 0.1.5 源码）→ `bash apply.sh` → 可选 context 补丁 → `printf` 写 `/root/.codebuddy/mcp.json`（**不用 heredoc**：YAML `script:|` 块缩进导致 heredoc 结束符无法顶格、bash 会吞掉后续脚本，改用 printf 逐行写 JSON）。
  - 关键修正：原 Dockerfile 只装 cypress/pnpm，**没装 codemogger**；旧版 stage 只在"已装"时 apply，CNB 构建里会跳过 → impact/references 废掉。现改为 stage 自带安装，MCP 工具在真实构建里全可用。
- **实跑验证（本会话 MCP 工具已可用）**：
  - `codemogger_leaks /workspace/app/packages/shared-components/src` → **成功返回**，证明 MCP 通道通、shim 执行正常。结果：shared 层**无真实反向 `@/` 依赖**（解耦不变量成立）。2 处"命中"是注释误报（`// 下方 re-export 以兼容现有 import ... from '@/api/...'`）——`leaks` 朴素正则把"re-export ... from '@/...'"注释也匹配，已知局限。
  - `codemogger_index` → 返回**干净的 codemogger 缺失错误**（`/usr/local/lib/node_modules/codemogger/dist/cli.mjs` 不存在）。即：**本沙箱没装 codemogger**，故 index/impact/references/search/context（都调真实 CLI）失败，**只有纯 grep 的 leaks 可用**。
  - 结论：39 处 `@/api/encv` 迁移完整性已通过 `search_content`（0 残留）+ `leaks`（shared 干净）双重确认；impact 交叉验证受限于本沙箱未装 codemogger，已由 .cnb.yml stage 自安装闭环。
- **未做（用户 01:46 批准"先做"）**：本沙箱 `npm i -g codemogger@0.1.5` + `bash apply.sh` + MCP `codemogger_impact` 完整演示。
  - **已先修一个真实 bug**：`codemogger-shim` 的 `impact` 分支内部调 `references` 时**没透传 `--db`**，而 MCP server 的 cwd 不固定 → `references` 默认按 cwd 找索引库，会读到错误库（我们建在 `/workspace/app/encv-mobile/.codemogger/index.db`）。已在 impact 分支加 `dbarg=(--db "$DB_PATH")` 透传。shim 每次调用从磁盘读取，修复对运行中 MCP server 即时生效。
  - **卡点**：终端 01:46+ 再次失联（`echo` 探测连失败 4 次，"Command failed to start within 10 seconds"）。npm 全局安装 + apply 必须走终端，无法在本端完成。已把安装/打补丁命令交给用户在自己终端跑，跑完说"done"我再走 MCP 跑 `codemogger_index` + `codemogger_impact @encv/shared-components/api/encv` 做 39 处迁移交叉验证。
  - **用户 01:52 回"跑完了，继续"** → 本端走 MCP 完成验证：
    - `codemogger_index` 首次报 `failed to open database .../.codemogger/index.db: I/O error (statfs shared WAL coordination path): entity not found` —— 根因是 `.codemogger` 目录不存在，SQLite 建库时 WAL 协调路径 statfs 失败。用 `write_to_file` 建 `/workspace/app/encv-mobile/.codemogger/.keep` 后重试，**成功**：Indexed 493 files → 3801 chunks, embedded 3738（53s）。
    - `codemogger_impact @encv/shared-components/api/encv` **成功返回权威导入图**：
      - 新模块 `@encv/shared-components/api/encv`：**132 处引用**（所有迁移后导入正确落到 shared 层）
      - 旧别名 `@/api/encv`（alias variant）：**`No imports found` — 0 处残留**
    - **结论**：`@/api/encv` → `@encv/shared-components/api/encv` 迁移**完整、无遗漏、无残留**；shared 层已是单一来源。同时证明 shim `--db` 透传修复生效（读到了正确索引）。MCP 通道 + codemogger 全工具链在本沙箱已闭环可用。
  - **待办（终端相关，flaky 未重跑）**：`node scripts/check-all.mjs` 确认 39 处迁移后 build 全绿——现可由新建的 `app-dev` MCP 的 `app_check_all` 工具跑（见下），不再依赖 flaky 终端。

### codemogger MCP 自完善 + 新增项目级 app-dev MCP（2026-07-15 01:56+）
- **修 `.codemogger` 路径问题（用户要求 MCP 自己处理）**：`app/codemogger-patch/mcp-server.mjs` 的 `runShim` 每次调用前 `mkdirSync(dirname(DB), {recursive:true})` 自动建 `<root>/.codemogger`。根因：codemogger 的 SQLite 开库带 shared WAL 协调路径，父目录不存在时 `statfs ... entity not found`。已删掉之前手动建的 `.keep` 兜底文件。
- **新增项目级 MCP `app-dev`（进一步减少终端依赖）**：
  - server 文件：`/workspace/scripts/app-dev-mcp.mjs`（stdio JSON-RPC，路径从 `__dirname` 推导 `REPO_ROOT=/workspace`、`APP_ROOT=/workspace/app`）。
  - 暴露 3 个 tool：
    - `app_check_all`：`node scripts/check-all.mjs`（cwd=`/workspace/app`），`noTests` 跳过单测；内部超时 900s；跑完附带读 `app/check-report.md` 摘要。= 全套门禁（biome CI / encv-mobile+shared+3 plugin typecheck / i18n lint --all / 单测 / vite build）。
    - `app_typecheck`：`pnpm exec vue-tsc --noEmit --incremental`（默认 encv-mobile，可指定 shared-components / plugin-*/web）；超时 300s。
    - `app_i18n`：`python3 scripts/i18n-tool.py <sub> --app encv-mobile`（cwd=`/workspace`），sub∈scan/lint/var-check/dup/stats/gen-types/db-init/find-key/en-check；find-key 接 key、dup 接 threshold。
  - ~~注册：`/workspace/.mcp.json`~~ **（02:09 已纠正）**：先前误把 app-dev 注册到项目根 `/workspace/.mcp.json`，理由是仓库里有 `.cloudbase-mcp/web-template/.mcp.json`——**但用户指出 `.cloudbase-mcp` 与 CodeBuddy 毫无关系，此推断错误**。CodeBuddy 实际读的是**用户级 `/root/.codebuddy/mcp.json`**（codemogger 就在那）。
- **纠正动作（02:09）**：
  - 把 `app-dev` 加进 `/root/.codebuddy/mcp.json`，与 `codemogger` 并列（`args: ["/workspace/scripts/app-dev-mcp.mjs"]`，无需 env，路径由 `__dirname` 推导）。
  - 删除错误的 `/workspace/.mcp.json`。
  - **`.cnb.yml` 同步**：`构建 codemogger-patch 并注册 MCP` stage 的 printf 块原本只写 codemogger，现补上 app-dev（`args` 用 `${REPO_ROOT}/scripts/app-dev-mcp.mjs`）。这样云原生开发环境每次启动都会自动注册两个 MCP。
- **教训**：CodeBuddy 的 MCP 注册位置是 `~/.codebuddy/mcp.json`（用户级），不要用 `.cloudbase-mcp` 等第三方生态的约定去推断。
- **状态（02:11 已闭环验证）**：用户确认「注册后可直接使用，无需重启」。直接调用 `app-dev` 工具成功：
  - `app_typecheck`（encv-mobile 默认）→ `exit=0`，工具链可用。
  - `app_check_all`（noTests=true）→ **8 PASS / 0 FAIL / 1 SKIP**：Biome CI、encv-mobile+shared+3 plugin typecheck 全 PASS、i18n lint --all PASS、encv-mobile vite build PASS；单测 SKIP。报告写入 `app/check-report.md`。
  - **结论**：39 处 `@/api/encv` → `@encv/shared-components/api/encv` 迁移后整套门禁（lint+typecheck+i18n+build）全绿、无回归。终端 flaky 问题已被 MCP 通道彻底绕开。
  - 教训修正：之前担心「CodeBuddy 不自动读项目级 .mcp.json」是多余的——app-dev 实际 `configSource: project, projectPath: /workspace` 已生效（即便我删了根 `.mcp.json`，用户级 `/root/.codebuddy/mcp.json` 注册即被加载）。MCP 工具注册后即时可用，不必重启 IDE。

### 剩余高价值待拍板项（高风险，未动，需用户点头）
- **K27** 导航约定被 god file 违反 + 抽象泄漏（先修 `getAppNavigation` classList bug 再收敛 `router.push` 13+ 处 + lint 固化边界）。
- ~~**K28** 双任务 store 重复「任务状态 reducer」（`createTaskCollection()` 共用）。~~ **（02:39 已完成并全绿）**
- ~~**K29** Modal/Overlay 两套互斥范式（`useModal` + `useDisclosure`）。~~ **（02:47 已完成安全切片并全绿）**
- 均「高风险、高价值」，文档标注需全回归 + 路由测试，未擅自推进。

### K28 完成记录（02:20–02:39）
- **新建** `packages/shared-components/src/lib/taskCollection.ts`：抽出两 store 重复的任务归约核心（`getTaskById`/`patchTaskById`/`rebuildIndex`/`appendTask`/`applyEvent` 4 件套 + `IDENTITY_FIELDS` 守卫 + `triggerRef`），用 hooks 参数化差异（`acceptCreated`/`onCreated`/`onPatched`）。
- **改写** `taskStore.ts` 与 `runTasksStore.ts`：内部归约逻辑委托 `createTaskCollection`，保留公开 API（`appendTask`/`applyEvent`/`getTaskById`/`patchTaskById`/`rebuildIndex`）为薄壳，避免改动其余调用点。
  - taskStore 差异：MAX_LOADED 守卫 + `afterAppend`（view-state + `persistence.putTask` 持久化）。
  - runTasksStore 差异：runId 守卫 + `some(id)` 去重 + `_totalCount++`。
- **WS 契约保持等价**：`registerSharedTaskServices.ts:51` 的 `taskServices.appendTask` 仍委托 `useTaskStore().appendTask`（薄壳保留）。
- **补单测** `taskCollection.test.ts`（vitest，聚焦归约核心：patch 守卫/getById/append prepend/applyEvent 4 件套/hooks 触发/rebuildIndex）。
- **新增 MCP 工具 `app_format`**（`scripts/app-dev-mcp.mjs`）：跑 `pnpm exec biome format --write`，绕开 flaky 终端做 biome 格式化。**注意**：运行中 server 不会热加载新增工具，需重启 IDE 才生效（用户 02:39 重启后 `app_format` 可用）。
- **验证**：`app_check_all` → **9 PASS / 0 FAIL**（Biome CI、encv-mobile+shared+3 plugin typecheck、i18n lint、encv-mobile 单测含新 taskCollection.test、vite build 全 PASS）。K28 零回归。
- **教训**：JSON reporter 的 biome 日志不含 diff 文本，纯肉眼难定位格式差异；加 `app_format` MCP 工具后可在 MCP 通道内直接格式化并看 diff。

### K30 + K29 安全切片完成记录（02:42–02:47）
- **K30（useDisclosure + useClickOutside）**：
  - 新建 `packages/shared-components/src/composables/useDisclosure.ts`（`isOpen`/`open`/`close`/`toggle` 最小抽象）。
  - 新建 `packages/shared-components/src/composables/useClickOutside.ts`（外部点击 + 可选 Escape 关闭 + `onMounted`/`onBeforeUnmount` 自动清理；`closeOnEscape` 默认 true）。
  - **改写** `packages/shared-components/src/components/shared/FilterDropdown.vue`：移除手搓 `isOpen`/`handleClickOutside`/`handleEscape`/`onMounted`/`onBeforeUnmount`，改用两个 composable（行为等价，保留 `toggleOpen` 的 open 后 focus 逻辑）。
  - 补单测 `useDisclosure.test.ts`（纯逻辑）+ `useClickOutside.test.ts`（happy-dom，含与 useDisclosure 集成 + 生命周期清理）。
- **K29 安全切片（useModal）**：
  - 新建 `packages/shared-components/src/composables/useModal.ts`：封装 `modalController.create + present + onDidDismiss` 为 `openModal()`，返回完整 `{data, role}`；`dismiss()` 透传。component 类型用 `Parameters<typeof modalController.create>[0]` 推导（避开 vue `ComponentRef` 在本版本未导出的坑）。JSDoc 注明 componentProps 浅快照坑（须传 reactive）。
  - 补单测 `useModal.test.ts`（mock `@ionic/vue` 的 modalController，`vi.hoisted` + `vi.clearAllMocks` 防跨用例累计）。
  - **迁移 4 个同构「文件选择器」`handleBrowsePath`**（WebdavServerDetail / HttpServerDetail / PluginSettings / Settings）到 `useModal`：纯样板缩减（`create+present+onDidDismiss` → `openModal`），语义不变（两种 dismiss 变体 `result.data` 字符串 / `{data, role}` 都原样保留）。`modalController` 导入改为 `useModal`。
- **关键教训（vitest 测试注册）**：`app/encv-mobile/vitest.config.ts` 用 projects，`FAST_INCLUDE` 只列具体文件，默认 `vitest run` **只跑 FAST_INCLUDE 里的文件**，未列出的新测试根本不执行（所以第一次 K30 跑"单测 PASS"其实没验证新测试）。**新建 composable 单测必须手动加进 `FAST_INCLUDE`** 才能真正被 `app_check_all` 验证。ISOLATED_INCLUDE 默认排除（仅 `ENCV_TEST_FULL=1` 跑）。
- **验证**：`app_check_all` → **9 PASS / 0 FAIL**（Biome CI、encv-mobile+shared+3 plugin typecheck、i18n lint、encv-mobile 单测含 3 个新测试、vite build 全 PASS）。`useModal.test` 此次**真正执行并抓出 2 个真实 bug**（mockCreate 跨用例累计、component 类型错误），修后全绿。
- **K29 剩余**：全仓仍有 10 处 `modalController.create` 未迁（useNewTaskModal 含 reactive 快照坑、AgentChat、ContextPopover、App.vue 等复杂场景）。文档标注 K29 需「逐处替换 + 交互测试」，当前缺交互测试，**未擅自全量迁移**，留待交互测试齐备再推进。

### 下一步候选（均「高风险/需用户拍板」，未动）
- **K27** 导航约定违反 + 抽象泄漏（先修 `getAppNavigation` classList bug 再收敛 `router.push` 13+ 处 + lint 固化边界）。
- **K15** 单例样板收敛（6+ 手搓单例 composable）。
- **K29 剩余 10 处** modalController 全量迁移（需交互测试）。
- **K35/K36/K38** 元范式（pinia vs 手搓单例 / 生命周期 init+refresh / 测试 mock fixture）。

### 插队任务：注册 daisyUI MCP（03:18–03:40）
- 在 `/root/.codebuddy/mcp.json` 的 `mcpServers` 新增 `daisyui-github`：`{"url":"https://gitmcp.io/saadeghi/daisyui","disabled":false,"autoApprove":[]}`（gitmcp.io 官方格式，仅 url，匹配 Cline/Cursor）。已确认 JSON 合法。
- **gitmcp.io 真实工具名（来自 idosal/git-mcp README，非 GitHub API 那套）**：`fetch__documentation` / `search__documentation` / `search__code` / `fetch_url_content`。⚠️ 之前猜的 `get_file_contents`/`list_commits`/`get_repository`/`create_issue` 全是另一个项目（modelcontextprotocol/github-mcp-server）的，猜错名字导致 not found。
- **关键机制（用户纠正）**：MCP 列表刷新靠**开新对话（同一会话）**，不是重启 IDE。`mcp_get_tool_description` 只能按名查单个工具、**无「列出全部」模式**，所以必须先知道名字；本注册所在对话的工具缓存早于注册，任何名字（含 README 正确名）都返回 not found 且 `mcpServerConfig` 为空——属「列表未刷新」，非配置错误。服务器本身连通（能解析 `daisyui-github` 并返回 server 级 not found，非连接失败）。
- **README 注册格式参考（idosal/git-mcp）**：VSCode 用 `{"type":"sse","url":...}`；Cursor 仅 `url`；Windsurf 用 `serverUrl`；Cline 用 `url`+`disabled`+`autoApprove`（即用户给的格式）。无 headers/auth（gitmcp 免鉴权）。若新对话仍 not found，可试补 `"type":"sse"`（VSCode 系客户端远程 MCP 常见要求）。
- **格式实测（03:53，5 种变体逐个探 `fetch__documentation`）**：
  - ❌ `serverUrl`（Windsurf 格式）→ CodeBuddy 报 "server not found or not connected"（不认该 key）。
  - ✅ `url` / `type:"url"` / `type:"sse"` / `type:"http"` → 全部被 CodeBuddy 识别并连通（server 解析成功），但本会话工具列表为空（not found + `mcpServerConfig` 空），4 种表现一致 → 排除是 `type` 字段问题，确认为**会话缓存未刷新**。
  - 结论：CodeBuddy 远程 MCP 接受 `url` 及 `type:url/sse/http`，拒绝 `serverUrl`。最终保留用户原始 `daisyui-github`（`url`+disabled+autoApprove）。
- **待办**：新对话里用 `mcp_call_tool` 调 `fetch__documentation`（取 daisyUI 的 README/llms.txt）做真实验证；若仍空，再补 `"type":"sse"` 试。

## 2026-07-15 晚：app_exec 门禁 + 项目 skill 注册
- app_exec 安全门禁(MCP)：规则抽到 scripts/app-dev-guard.mjs，server 用 fs.watch+动态 import 做 HMR(热更，无需重启/新对话)，fail-closed。自测 scripts/app-dev-guard.test.mjs 经 MCP 跑出 ALL PASS(9 条)。门禁拦截破坏性操作(递归删除、危险 git 改写、进程强杀、提权、下载直灌 shell、写块设备)，放行常见安全重定向。
- 项目 skill 注册：skills-lock.json 原 skillPath 指向不存在的 skills/，已修正为 .agents/skills/(.claude/ 归一)。另建 .codebuddy/skills/<name> 符号链接指向 .agents/.trae/agent 下真源(单一真源)。假设 CodeBuddy 扫描 .codebuddy/skills(与 rules/snippets 同体系)，但官方文档本环境被网络拦截未能确认；需重载 IDE 后查 use_skill 是否出现 capacitor-expert 等验证。

## 2026-07-15 晚（续）：HMR bug 修复 + 新增 web-fetch MCP
- app_exec 门禁 HMR bug 修复：原 `watch(GUARD_MODULE)` 监听文件 inode，而编辑器/write_to_file 是原子保存(temp+rename)，原 inode 被 unlink 后 inotify 静默停止 → 改规则不触发热更。改为 `watch(GUARD_DIR)` 按 basename 过滤，抗原子保存。另新增 `app_guard_reload` 工具手动重载并返回规则数，便于验证 HMR 是否生效(旧 server 进程仍无此工具，需重载 IDE 才加载新代码)。
- 新增 `scripts/web-fetch-mcp.mjs`（高级版 web_fetch 替代）：retry(指数退避+Retry-After)、内容嗅探(magic bytes 复核 server 声明的 content-type)、SPA 检测(React/Vue/Next/Nuxt 壳)+可选 headless 渲染(puppeteer/playwright 装了才用)、代理(HTTP/HTTPS 经 CONNECT 隧道，零依赖)。已注册到 /root/.codebuddy/mcp.json 的 `web-fetch` server。核心函数导出 + main 守卫启动 server，可 `node -e "import(...)"` 单测。
- 教训：记忆文件直接用 write_to_file/replace_in_file 写，不用 app_exec MCP 绕弯；验证命令才走 app_exec。

## 2026-07-15 晚（续2）：app-dev MCP 增 skill 管理 + MCP 注册单一真源
- app-dev MCP 新增 skill 管理（scripts/skill-manager.mjs，纯 Node 零依赖，核心函数导出可单测）：
  - 多 skill 路径：skillPaths 列表(默认 .codebuddy/skills/.agents/skills/.trae/skills)，持久化 .codebuddy/skill-registry.json；路径列表可增删(app_skill_path_add/remove)；扫描时**跟随符号链接**(因 .codebuddy/skills/* 是符号链接)，故能识别全部 31 个 skill(含仅经符号链接可达的 video-encrypt)。
  - CRUD 工具(9 个)：app_skill_list(可 filter)/get/add/update/remove、app_skill_paths/path_add/path_remove、app_skill_rescan。
  - add 三法：import=复制本地文件/目录；npx=`npm install <pkg> --prefix <tmp>` 后复制首个 SKILL.md；git=`git clone --depth 1 <url>`(可选 subdir/ref)后复制 skill(s)(subdir 自身是 skill 则拷一个，是 skill 集则拷全部)。update 无 method 时仅刷新 hash。
  - 监视：对每个 skill 路径目录 + 每个 skill 子目录 fs.watch(按 SKILL.md/basename 过滤，抗原子保存)，变更 debounce 后 rescan 并 resync 注册表(含 sha256 hash)。
  - 经 app_exec 单测：发现 30→31 skill、import/get/update/remove/路径增删全 OK；测试污染注册表已 rm，重新 loadRegistry 干净重建(默认三路径)。
- MCP 注册改单一真源：维护 /workspace/.codebuddy/mcp.json(权威，含 codemogger/app-dev/web-fetch，绝对 /workspace 路径)；.cnb.yml 的「构建 codemogger-patch 并注册 MCP」stage 第 2 步改为 `cp /workspace/.codebuddy/mcp.json /root/.codebuddy/mcp.json` 覆盖，去掉原内联 printf 块，消除两处不一致。改 MCP 只需改项目文件 + 重跑流水线。
- 用户已确认：.codebuddy 项目 skill 已被 CodeBuddy 正常识别(符号链接方案成立)。

## 2026-07-15 晚（续3）：用 skill 管理 MCP 安装 gsap-skills / daisyui
- **增强 installGit（scripts/skill-manager.mjs）**：原 git 方法只在仓库根目录找 SKILL.md，对 skill 嵌套在 `skills/` 子目录的仓库(`npx skills add` 约定)会失败。新增 `copySkillsFrom` 递归识别三种结构：① 根/SKILL.md=单 skill；② 直接子目录含 SKILL.md=skill 集合(各拷一个)；③ 含 `skills/` 子目录→递归(agentskills 集合约定，模拟 `npx skills add`)。**实测**：`npx skills add https://github.com/greensock/gsap-skills` 与 `saadeghi/daisyui` 此前失败(no SKILL.md at root)，增强后成功。
- **安装结果**：经 app_skill_add(method=git) 装到 .codebuddy/skills：gsap-skills 拆出 8 个 skill(gsap-core/frameworks/performance/plugins/react/scrolltrigger/timeline/utils)，daisyui 1 个(含嵌套子 skill：colors/config/install/usage)。总数 31→40。live MCP server 通过目录监视器自动发现全部 9 个新 skill(app_skill_list 已验证)。
- 注意：live server 启动时已 import 旧 skill-manager，本次用 `node /tmp/install-skills.mjs` 直接跑更新后模块安装(同一份代码)；改 skill-manager 后要让 live 的 app_skill_* 工具用新逻辑，需重载 IDE(server 不热加载子模块)。
- daisyui 仓库还有 `packages/docs/src/routes/SKILL.md`(无关文档 skill)，因自动识别走 `skills/` 子目录约定被正确忽略。

## 2026-07-15 晚（续4）：移除 gitmcp 远程 MCP + 写前端主题重构文档
- **移除 gitmcp**：`/root/.codebuddy/mcp.json` 与项目真源 `/workspace/.codebuddy/mcp.json` 经 grep 确认**均已无 `daisyui-github`/`gitmcp`**（此前「单一真源」流水线 `cp` 覆盖时已清掉）。实测不可用，现已确认移除完成。
- **当前主题架构关键事实（写重构文档用）**：
  - `encv-mobile/src/main.ts:34-36` 只引 `theme/variables.css`(Ionic 写死亮/暗变量, 主色 `#4f8cff` 蓝) + timeline 样式，**未引 `daisyui.css`**；只有 `plugin-*/web/src/main.ts`(simverse/openlist/mpv) 才引 `daisyui.css`(主色 `#8b5cf6` 紫) → 主应用与插件主色不一致。
  - `useTheme.ts` 是命令式补丁引擎：`document.documentElement.style.setProperty('--ion-color-primary',…)` 直接写根 + 自带 hexToRgb/lighter/darker 运算；与 CSS 变量体系割裂（这是"旧主题色实现"）。
  - `styles/daisyui.css` 已用 daisyUI v5(`@plugin "daisyui"`+`@plugin "daisyui/theme"`)，含 `encv`/`encv-dark` 两主题 + `.encv-card/.encv-panel/.encv-btn` 共享类；`vite-plugins/daisy-ui.ts` 仅返回 `tailwindcss()`。
  - `AppearanceDetail.vue` + `useTheme` 已有高级能力需保留：P3 色域、vivid 滤镜、背景模糊、渐变背景、护眼/暗/亮/渐变 4 类背景预设 + 自定义取色器。
- **产出**：`/workspace/ENCV前端主题重构方案.md` —— 以 daisyUI v5 + GSAP 重塑(共享包为主)，参考 Obsidian(变量覆盖+Snippets)与思源笔记(theme.json+css+可选 theme.js 钩子)主题架构。核心：三层令牌(primitive→semantic→component)、daisyUI 为事实源且 Ionic 向下桥接其 `--color-*`、主题=data-theme 切换 + 叠加层(主色/渐变/模糊/vivid/P3)、GSAP 动效层 `src/motion/`(遵守 reduced-motion/vivid)、Appearance 重组为「主题/主色/特效」三段、分阶段迁移(每阶段 app_check_all 门禁)。强调保留既有高级特效、主应用接入 daisyui.css 统一调色板。
- **续(用户反馈「gsap 动效太少了不够」)**：大幅扩充该文档 GSAP 动效层——2.5 由 5 条扩为「动效全集」9 类场景(路由转场/滚动揭示/微交互/浮层/导航/数据反馈/氛围视觉/引导空态/手势驱动)，新增 2.6 动效设计令牌(ease/dur/--motion-intensity CSS 变量 + MotionProfile 闸门)；3.4 `src/motion/` 由 5 文件扩为 14 模块(transition/reveal/flip/micro/magnetic/overlay/nav/data/ambient/guide/gesture/registry 等)含代码骨架；同步更新目录结构、Phase 3、验收清单。
- **续2(用户要求补 daisyui + 浏览器兼容性)**：用已装 daisyUI 5 skill(config/colors) 补充 `ENCV前端主题重构方案.md`——新增 2.7「daisyUI v5 主题与组件体系落地细节」(双插件结构/19 色必填令牌/语义色铁律/形状深度令牌/组件层/用户主题构建期拼接)；新增 2.8「浏览器兼容性 Chromium 114」(项目要求 114+，Tailwind v4 基线 Chrome 111+，114≥111 核心可用无需 polyfill；对照表列 OKLCH/color-mix/nesting/backdrop-filter 均 114✅，light-dark()/allow-discrete/field-sizing ❌113/117/123 但 daisyUI 不依赖；结论:只用 114 支持特性,encv 主题用 hex 零风险,Tailwind v4 不降级不引 autoprefixer)。同步更新 3.3/Phase0(加 browserslist "Chrome >= 114")/验收清单(加兼容性红线项)。**事实**:app 用 tailwindcss^4.1+daisyui^5+@tailwindcss/vite^4.1+vite^8,仓库无 browserslist 配置。
- **续3(simverse 横竖屏)**：先误把"横屏世界"理解为需做竖屏响应式重排，被用户纠正——**横屏世界=landscape-locked 游戏，不存在竖屏形态**（锁屏即可，不重排）；**竖屏才是所有应用/插件的主要形态**（encv-mobile 主应用 + simverse 除世界外的所有页面 + openlist/mpv 等插件），需 phone+pad 双屏适配。据此重写 §2.9：2.9.1 横屏世界特殊适配(锁横屏+沉浸+fg_idle+Phaser RESIZE+Manifest 不硬锁+无竖屏布局)；2.9.2 竖屏=主形态(phone 单栏+底部 Tab+抽屉 / pad master-detail 双栏 / 断点 container queries / 复用 .encv-card+base-* 令牌)；2.9.3 响应式令牌+useSimverseLayout(isWorld) 世界恒 landscape、其余恒 portrait-*；2.9.4 兼容性(114 全支持)。**关键事实**：plugin-simverse 现状——`SimverseWorld.vue` 仅 native 模式 `lockScreenOrientation("landscape-primary")`；`SimVerseActivity.kt` 仅 `configChanges="orientation..."` 未设 `screenOrientation`；世界 HUD 横屏形态(`.world-map{padding:60px 100px 70px}`、300px side-panel、居中 bottom-bar)；`RegionScene.ts` 用 `this.scale` 布局。
- **续4(Phase 0 落地脚手架，已做)**：按文档 Phase 0 真正落地共享包动效地基——`pnpm-workspace.yaml` catalog 加 `gsap: ^3.13.0`；`packages/shared-components/package.json` 加依赖 `gsap: "catalog:"` + 根 `browserslist: ["Chrome >= 114"]` + exports 加 `"./motion": "./src/motion/index.ts"` 与 `"./motion/*": "./src/motion/*"`；`src/styles/daisyui.css` 顶部增 `@import "../theme/tokens.css"`。新建 `src/theme/tokens.css`(§2.6 动效令牌：--motion-ease-*/--motion-dur-*/--motion-stagger/--motion-intensity；.encv-vivid/.encv-p3 上调 1.3；reduced-motion 媒体查询置 0)。新建 `src/motion/` 共 15 文件：`guard.ts`(MotionProfile + getMotionProfile + settleToFinalState)、`tokens.ts`(EASE/DUR/STAGGER)、`registry.ts`(命名动画热禁用)、`index.ts`(installMotion 注册 ScrollTrigger+Flip + 统一导出)、`transition/reveal/flip/micro/magnetic/overlay/nav/data/ambient/guide/gesture.ts`(§2.5 动效全集对应 composable，全部经 guard 闸门、reduced-motion 落终态)。**约束**：tsconfig strict + moduleResolution bundler；导出名无冲突；gsap 子路径 `gsap/ScrollTrigger`/`gsap/Flip` 已在 index 注册。
- **续4 修正(环境规则：MCP 可执行命令，不必交用户)**：用户纠正"环境规则该更新"——工作区规则里"长耗时命令(构建/check:all/下载)交用户本端跑"是过时理解。**正确做法**：app-dev MCP 工具(`app_exec`/`app_check_all`/`app_format`)运行在持久 MCP 进程、带安全闸门与超时，是跑命令/校验的正确通道，不应再用裸 `execute_command` 硬跑长命令，也不需交用户。本次实践：`app_exec` 跑 `pnpm install`(拉 gsap，4.1s) → `app_check_all` 首跑 Biome FAIL(7 个 motion 文件格式不符) → `app_format` 格式化 7 文件 → 重跑 `app_check_all` **9 PASS / 0 FAIL**(Biome/各包 typecheck/i18n/单测/vite build 全绿)。Phase 0 脚手架门禁全过。记忆结论：本环境**命令一律走 MCP 工具**；`execute_command` 仅用于极短、非阻塞的探测性命令，且仍严禁 kill 类危险操作。
- **环境保持.mdc 规则已就地更新（2026-07-15 20:14）**：`/workspace/.codebuddy/rules/环境保持.mdc` 第 16 行原"长耗时命令交用户在自己终端跑"已改写为「长耗时/阻塞命令一律通过 app-dev MCP 工具执行(app_exec/app_check_all/app_format)，不裸用 execute_command 硬跑、也不交用户」+ 补充 `execute_command` 仅用于极短探测。规则文本确在 workspace 内、可直接改（此前误以为 harness 注入不可改）。

- **续5(Phase 1 共享部分完成，2026-07-15 20:27)**：按 `ENCV前端主题重构方案.md` Phase 1「统一 daisyUI + 桥接令牌」落地共享侧。
  - 新建共享文件：`src/theme/bridge.css`(Ionic←daisyUI 桥接令牌，纯 CSS；`--ion-color-*` 重写为 `var(--color-*)` 别名，shade/tint/contrast 用 color-mix 派生(114✅)，`-rgb` 三元组因 114 无相对颜色语法仅对内置两主题硬编码；覆盖 `:root`/`[data-theme=encv]`/`[data-theme=encv-dark]`/`body.dark`)；`src/theme/palette.css`(daisyUI encv/encv-dark 的 `--color-*` 纯 CSS 等价，单一调色板来源，注释要求改动同步 daisyui.css 的 `@plugin "daisyui/theme"` 块)；`src/styles/theme-core.css`(`@import tokens+palette+bridge`，纯 CSS 不依赖 Tailwind)。
  - `daisyui.css` 顶部增 `@import "../theme/bridge.css"`(插件 web 也受益)。`encv-mobile/src/main.ts` 在 `variables.css` 之后 import `theme-core.css`(桥接覆盖 variables.css 蓝色字面量 → Ionic 跟 daisyUI 紫)。
  - **⚠️ vite 8 (rolldrop) interop 坑（关键长期事实）**：给 encv-mobile vite 接入 `@tailwindcss/vite`(`daisyUiPlugin()`) 时，rolldrop 打包 vite.config 把 `@tailwindcss/vite` 的默认导出 interop 弄坏 → `tailwindcss_vite_..._index_mjs.default is not a function`，`vite build` 失败。运行时 `import('@tailwindcss/vite').default` 确为 function(已 node 验证)，纯属配置打包 interop 问题。插件 web 用同 `daisyUiPlugin()` 但因门禁不构建插件、此前未暴露。**结论：encv-mobile 主应用暂不以 Tailwind 管线接入 daisyUI，改走纯 CSS `theme-core.css` 入口**；未把 `@tailwindcss/vite`/`tailwindcss`/`daisyui` 加为 encv-mobile 依赖(已撤回)。后续若需主应用用 Tailwind 工具类(Phase 4 迁移)，须先解决此 interop(如 daisy-ui.ts 做 default/interop 兜底，或 vite 配置 external 化)。
  - **门禁**：`app_check_all` **9 PASS / 0 FAIL**(Biome/encv-mobile+shared+3 plugin typecheck/i18n/单测/encv-mobile vite build 全绿)。
  - **待办**：人工核对暗/亮主色(蓝→紫属预期焕新)；Phase 3 动效全集试点。

### 续7(并行三批：B noop引擎+动效开关 / P2 useTheme解耦 / P3 reveal试点，2026-07-15 21:02)
- 门禁 `app_check_all` **9 PASS / 0 FAIL** 全绿。三批文件互不重叠，纯并行。
- **B(ACL 顺带收益 · noop 引擎 + 运行时动效开关)**：
  - 新增 `motion/internal/noop-engine.ts`：`NoopEngine` 实现 `MotionEngine` 全接口（所有补间 no-op、直接停自然态=终态），`export const noopMotion`。`internal/index.ts` 桶导出 `noopMotion`，注释标明「全局改 no-op 只需 `export { noopMotion as motion } from "./noop-engine"`」。
  - `motion/guard.ts` 新增全局总闸 `setMotionDisabled(disabled: boolean|null)`/`getMotionDisabled()`（null=跟随系统 reduced-motion；true=强制全关，composable 直接落终态；false=强制开）。`getMotionProfile().enabled` 据此计算。⚠️ 与 `registry.ts` 的 `setMotionEnabled(name, enabled)`（按命名动画开关）**同名冲突**，已改名 `setMotionDisabled` 规避（TS2308 已修）。
- **P2(useTheme 解耦到语义令牌)**：`useTheme.ts` 的 `applyColor` 原手搓 6 个 Ionic `--ion-color-primary*`(含 lighter/darker 数学)，现改为只写 daisyUI 语义令牌 `--color-primary` + 仍需 JS 补的 `--ion-color-primary`/`-rgb`/contrast/`-contrast-rgb`（bridge.css 对自定义色不负责 shade/tint，但 shade/tint 已由 bridge 的 `color-mix(var(--color-primary)...)` 自动派生）。`shade/tint` 手写逻辑删除 → useTheme 不再依赖 Ionic 内部明暗数学（ACL 解耦：业务只认 `--color-*`）。新增语义别名 `setPrimaryColor`(=setThemeColor) 并对 Appearance 等下游零改动。
- **P3(动效全集首个真实下游试点)**：`motion/reveal.ts` 的 `useScrollReveal` 重写——先 `motion.set` 落初始隐藏态再 `to` 入场（消除「滚动到才闪一下」跳变）；新增可选 `ready?: Ref<boolean>` 闸门（等待异步列表渲染出子元素再建触发器，解决挂载时子元素不存在不入场）。`useScrollReveal` 透传 `MotionTarget`/`MotionVars`/`ScrollTriggerHandle` 等引擎类型，公共签名零动画库泄漏。**首个真实消费方**：`encv-mobile/src/views/ExtensionsPage.vue` 引入 `useScrollReveal`（从 `@encv/shared-components/motion` 桶，顺带触发 `installMotion()` 注册 ScrollTrigger），对 `.extensions-list` 容器挂 `stagger:true` + `ready: computed(()=>!isLoading && extensions.length>0)`。下游仅 import 语义 API，gsap 经 ACL 全透明。
- **教训**：① `motion/index.ts` 用多个 `export *`，新加导出名须先 grep 全目录防与 `registry.ts` 等既有名碰撞（TS2308）；② 在 `<script setup>` 中引用尚未声明的 `ref`/常量时，要放在其声明之后（否则 TDZ/TS2448），pilot 代码最初放在 `extensions`/`isLoading` 声明前已后移修正。
- 下一步候选（未动）：Phase 4 组件迁移到 `.encv-*`/`bg-base-*` + Appearance 重组；Phase 5 用户主题/Snippets 闭环；K27 导航约定收敛；K29 剩余 10 处 modalController 迁移。
