# 2026-07-11 工作记忆

## check:all 第五次跑 + 报告脚本改造 + Biome 收尾（00:00）
- 用户重跑 check:all 后状态：Biome FAIL、shared typecheck FAIL，其余（encv-mobile typecheck/单测、i18n）PASS。
- **用户指出报告只截 40 行不合理**：要求「完整输出写入单独文件，报告中用相对路径引入」。
- 改造 `scripts/check-all.mjs`：
  1. 去掉 `onData` 的 4KB 截断（之前既截 4KB 又截 40 行，双截断丢上下文）。
  2. 新增 `LOGS_DIR = <app-root>/check-logs`，`mkdirSync(..., {recursive:true})`，`slugify()`。
  3. 每个套件跑完把 `stdout+stderr`（stripAnsi）完整写入 `check-logs/<slug>.log`。
  4. 报告「失败详情」不再内嵌输出，改为相对路径引用：`[\`check-logs/<slug>.log\`](./check-logs/<slug>.log)`。
  5. `check-logs/` 与 `check-report.md` 加入 `app/.gitignore`（生成物，避免污染 git 状态）。
- **Biome 最后一个真错误（被 40 行截断藏住路径）**：`packages/shared-components/src/composables/useI18n.ts:417` 的 `currentLocale.value = currentLocale.value;`（biome `noSelfAssign`）。`currentLocale` 是普通 `ref<Locale>`，Vue 3 给 ref 赋相同值不触发响应式（`hasChanged` 判断），故该行是 no-op → 直接删除，行为不变。删除后 Biome 应全绿（假设无其它 lint 点）。
- lint 校验：`check-all.mjs`、`useI18n.ts` 均 0 错。
- 预期本轮（第六次）重跑：**Biome CI ✅**（首次全绿）、encv-mobile typecheck ✅、单测 ✅、i18n ✅；**仅 shared typecheck 仍 FAIL**（315 既有错，非本次范围）。报告文件精简、完整日志在 check-logs/ 按需查看。

## 附：CodeBuddy 配置位置 & 漫游覆盖（用户上一问）
- CodeBuddy 全局配置在 **`/root/.codebuddy/`**：`models.json`、`mcp.json`、`expert-history.json`、`plugins/`（5243 文件市场缓存）、`skills/`。
- 用户列的漫游路径 `/root/.codebuddy/**/*.md` + `/root/.codebuddy/models.json` 仅覆盖 `.md` 与 `models.json`；**`mcp.json`/`expert-history.json`/技能里的非 md 文件均未覆盖** → 不完整。建议漫游整体 `/root/.codebuddy/**` 或至少补 `mcp.json`/`expert-history.json`/`**/*.json`/`**/*.js`。
- 每日记忆在 `/workspace/.codebuddy/memory/`（工作区有 git 不需漫游）；与 `/root/.codebuddy/` 是两码事。

## check:all 第六次跑 + 攻克 shared typecheck（00:14）
- 用户重跑后：**Biome CI ✅PASS**（首次全绿）、encv-mobile typecheck/单测/i18n 均 PASS；仅 **shared-components typecheck FAIL（308 行）**。报告正确引用 `check-logs/shared-components-typecheck.log`，用户正查看完整日志。
- **shared typecheck 308 错根因（三层，均已修）**：
  1. **自引用解析失败**：shared 的 `tsconfig.json` 只有 `"@/*"` 映射，没有自引用 `@encv/shared-components/*`。encv-mobile 能解析是因为它 tsconfig 有 `"@encv/shared-components/*": ["../packages/shared-components/src/*"]`。shared 自己源码（`IncrementalFilter.ts`、`src/components/shared/*.vue`）引用 `@encv/shared-components/...` 全部 `Cannot find module`，但目标文件（`useFrontendLogs.ts`、`lib/workflow/types.ts`）都存在。→ 给 shared tsconfig `paths` 加 `"@encv/shared-components": ["./src"]` 与 `"@encv/shared-components/*": ["./src/*"]`。
  2. **测试文件被纳入生产 typecheck**：`include: src/**/*.ts` 把 `*.test.ts`/`__tests__/**` 收进来，它们依赖 `vitest`/`@vue/test-utils`（shared devDeps 无）→ 大片 `Cannot find module` + `implicit any`(TS7006) + `unknown`(TS2345/TS18046)。这是 308 错的大头。→ `exclude` 加 `"**/*.test.ts", "**/__tests__/**"`（标准做法：测试由 vitest 自行类型检查）。
  3. **真实类型错**：`src/composables/useEventBus.ts:13` 从 `@/api/encv` 导入 `EncvTask`，但 `EncvTask` 实际定义在 `src/types/task.ts`（其它文件都从 `@/types/task` 导入），`@/api/encv` 无此导出 → TS2305。→ 改为 `import type { EncvTask } from "@/types/task";`。
- 校验：308 错中非测试文件仅 `IncrementalFilter.ts`(自引)、`useEventBus.ts`(EncvTask)、`PhaseBadge/PhaseIcon/UnifiedTimelineCard.vue`(自引) 五处，全部被上述修复覆盖；其余全在测试文件（已排除）。预期本轮（第七次）重跑 **shared typecheck ✅ → 5/5 全绿**。
- `useEventBus.ts` read_lints 0 错。
- 注意：排除测试后 shared 的 `*.test.ts` 不再被 `vue-tsc` 类型检查（运行时仍由 vitest 跑，但 vitest 用 esbuild 不类型检查）。如需测试也类型检查，需给 shared 加 vitest/@vue/test-utils devDeps + 独立 `tsconfig.spec.json`（更大改动，留待后续）。

## check:all 第八次跑 + 清理优化（00:35）
- 用户重跑：报告确认 **5/5 PASS**（Biome / encv-mobile typecheck / shared typecheck / i18n / encv-mobile 单测），耗时合计约 17.4s。
- **用户体验优化**：用户要求「运行第一步先清理 check-logs 并在终端输出清理情况（处理占用异常）」。已在 `scripts/check-all.mjs` 实现：
  - 新增 `cleanLogsDir()`，在 `main()` 开头、检查循环前调用。
  - 逻辑：目录不存在→提示「无需清理」并 `mkdirSync` 重建；存在→`readdirSync` 逐条 `rmSync(p,{recursive,force})` 删除，单条失败（占用/权限，捕获 `e.code`）不中断，最后汇总「已清理 N 个」+ 若有失败则黄色列出「⚠ 跳过 M 个（占用/异常）：- name (code)」；`readdirSync` 失败则红色提示并强制重建目录。
  - 去掉了原顶层 `mkdirSync`，改由 `cleanLogsDir` 按需重建；import 增 `rmSync/existsSync/readdirSync`。
  - lint 0 错。

## check:all 补全 plugin-* 类型检查（00:53）
- 用户指出 pnpm 工作区还有 `plugin-*` 项目（encv-mobile/plugin-{openlist,mpv-player,simverse}/web），原 `check:all` 只 typecheck `encv-mobile` + `shared-components`，**漏了三个插件**，全量检查不全。
- 三个插件 `package.json` 均有 `"typecheck": "vue-tsc --noEmit"`（无 test:unit）。已在 `scripts/check-all.mjs` 的 `SUITES` 新增 3 个套件：`plugin-openlist/web typecheck`、`plugin-mpv-player/web typecheck`、`plugin-simverse/web typecheck`（均 `pnpm typecheck`，timeout 300s），置于 encv-mobile typecheck 之后。脚本头注释同步更新为 8 项覆盖。
- 同步更新 `docs/migration-task-system.md` §6 门禁：补 plugin-* 三条 `vue-tsc --noEmit` 手动命令，并提示用 `pnpm check:all` 一键覆盖。
- 现 `check:all` 共 8 套件：Biome CI / encv-mobile typecheck / shared typecheck / 3×plugin typecheck / i18n lint / encv-mobile 单测。
- lint 0 错。注意：插件 typecheck 能否解析 `@encv/shared-components` 依赖 encv-mobile 同款 tsconfig 别名配置；若插件 tsconfig 缺映射，重跑可能 FAIL，需用户贴 `check-logs/plugin-*.log` 再修。

## 两项优化：i18n 纳入后端 + 类型检查加速（01:05）
- **i18n 需项目级覆盖后端（Go 根 + Kotlin）**：用户指出 i18n 应含后端。调研发现：
  - `scripts/i18n_lib/scanner_go.py` 已实现 Go 扫描（`i18n.T("key")`/`i18n.TWith`），`scanner_kotlin.py` 已实现 Kotlin 扫描（`R.string.xxx`→key `x.y`）；`lint.py` 的 `run_all_apps_checks` 遍历 `get_all_app_names()` 逐个 app 检查，Go/Kotlin key 并入 used_keys 后对共享字典做 `check_missing_keys`。
  - 原 `_discover_apps` 把 `go_dirs=["internal","pkg"]`（指向仓库根 `/workspace/internal`、`/workspace/pkg`）和 `kotlin_dirs`（android app main/java）**只挂在 `encv-mobile` 前端 app 名下**，且漏了根级 `cmd/agent` 与插件 Kotlin 目录；后端因此不能作为独立项显式出现在报告里，用户感知为"没包含"。
  - 实测：根 Go（`internal`/`pkg`/`cmd`/`agent`）与 Kotlin 当前**均无** `i18n.T`/`R.string` 调用（Kotlin 命中的是 `getStringExtra`/`Locale`/`SharedPreferences`，非 i18n），所以后端扫到也是 0 key——但基础设施必须正确以便后端一旦引入 i18n key 即被捕获。
- **改动 `scripts/i18n_lib/config.py` `_discover_apps`**：
  - 移除挂在 encv-mobile 上的 `go_dirs`/`kotlin_dirs` 特例。
  - 新增独立 `backend` i18n app：`go_dirs = [internal, pkg, cmd, agent]`（均存在），`kotlin_dirs = [android/app/src/main/java, android/combolite-host/src/main/java, plugin-{openlist,mpv-player,simverse}/src/main/java]`；`src_dirs/i18n_files` 初空，由下方 `shared_i18n` 合并块自动注入共享字典（与前端 app 同待遇）。
  - 验证：combolite/plugin Kotlin 目录 `R.string.` 引用数均为 0（search_content count=0），加入后端扫描**零误报风险**；android app main/java 原已被 encv-mobile 扫过且 i18n PASS，迁移到 backend 行为一致。
- **类型检查加速（目标 <5s，原 ~20s）**：
  - `scripts/check-all.mjs`：5 个 vue-tsc 套件由 `pnpm typecheck`（会触发 `pretypecheck` 的 i18n 钩子，与独立 i18n 套件重复）改为**直接 `pnpm exec vue-tsc --noEmit --incremental`**（shared 加 `-p tsconfig.json`）。跳过冗余 i18n 钩子 + 启用 tsc 增量缓存（热重跑 <5s）。
  - 验证：`vue-tsc/index.js` 经 `@volar/typescript` 的 `runTsc` 把全部 CLI 参数透传 tsc，`--incremental` 是 tsc 标准参数→不会报未知选项；tsbuildinfo 写在各项目根（如 `encv-mobile/tsconfig.tsbuildinfo`），不在 check-logs，不被 cleanLogsDir 删；已把 `*.tsbuildinfo` 加进 `app/.gitignore`。
  - `main()` 改为 `runAllConcurrent(SUITES, 4)` 并发执行（带并发上限 4，防 5+ vue-tsc 同时跑撑爆内存），结果按 SUITES 定义顺序收集保证报告表格顺序稳定；抽出 `runOne`/`runAllConcurrent` 辅助函数。串行 for 循环已移除。
  - lint：check-all.mjs 与 config.py 均 0 错。
  - 说明：冷启动（大量改动后）全量类型检查仍可能 ~15-20s；但**热重跑（无源码改动）因增量缓存可 <5s**，并发也缩短冷跑墙钟。

## 架构文档澄清：boundary-spec 过时，migration-task-system 才是权威（01:33）
- 用户指出的"拆分抽象与实现"重构**已落实到文档**：`app/docs/migration-task-system.md`（§0 定调"抽象=重写+DI 而非搬运"、§2 目标架构 shared=纯抽象层/库、encv-mobile=应用层注入上下文）。
- **`docs/shared-components-boundary-spec.md` 是过时稿**，主张"encv 业务全搬回 encv-mobile"，与已落地的 DI 共享抽象层**直接相反**——按它执行会摧毁 taskStore 等已提升进 shared 的抽象。之前我误把它当权威，导致误判"两份规范打架"。
- 已把此架构事实写入 `/workspace/.codebuddy/memory/MEMORY.md`（权威文档、boundary-spec 过时禁执行、已落地事实、Module G 去重）。

## 边修订边执行边事实核查（01:50-02:10）
- 用户指令"边修订边执行边事实核查review"：不盲信文档，所有结论以实际代码为准。
- **事实核查 Module G 去重声明（migration-task-system.md §8.1）**：文档称 encv-mobile 有 11 个与 shared 重复的 composables，但实测**仅 6 个还在**（useToast/useClipboard/useDateFormat/relativeTime/activeStatus 为 re-export 垫片 + useSearchInput 为与 shared 逐字节相同的完整副本），其余 7 个（useTheme/useErrorCapture/useFrontendLogs/useHighRefreshRate/useIonicAutoRegister/usePinchZoom/useDevTools）已被之前某轮删除。
- **执行 G-1**：删除上述 6 个冗余文件（共 ~520 行）。安全依据：① `encv-mobile/tsconfig.json` 的 `"@/*":["./src/*","../packages/shared-components/src/*"]` 二级回退已确认；② 全部 58 处引用均用 `@/composables/useX` 别名（grep 确认**无相对路径引用**），删除后自动回退 shared；③ useSearchInput 逐行比对与 shared 等价。
- **事实核查抓出文档反向错误（G-4 useEventBus）**：文档 §8.1.1 ② 称"保留 encv-mobile canonical、删 shared 孤儿"。实测 `encv-mobile/src/composables/useEventBus.ts` **已不存在**，11 处 `@/composables/useEventBus` 引用经别名回退到 **shared 副本 = 事实 canonical**。原"删 shared"指令会断 11 处引用 → 已修正文档为"保留 shared 为唯一真源，禁止删除"。
- **修订文档**：① `docs/shared-components-boundary-spec.md` 顶部加废弃/对齐头部（指向 migration-task-system.md，纠正"搬业务出 shared"错误前提）；② `app/docs/migration-task-system.md` §9 补 Module G 去重进度（2026-07-11）、§8.1 加"composables 重复已去重完成"标注、§8.1.1 ② 追加 useEventBus 事实核查修正。
- **shared/api 暂存残留（A-ext/Phase 6，未执行）**：`shared/api/` 仍含 13 个 encv_* 文件（encv_admin/files/openlist/perf/plugins/search/system/trash/webdav 等非任务域 + encv_tasks/core + encv.ts 垫片）；`encv-mobile/src/api/encv_tasks.ts`/`encv_core.ts` 已改用 `@encv/shared-components/api/core` 新基座。非任务 api 回退 shared 需逐文件 api/core 重写或回退 encv-mobile，风险较高，留待后续。
- **待用户验证**：执行侧无法自跑构建，请用户跑 `cd /workspace/app && pnpm check:all` 确认 encv-mobile typecheck + 单测仍绿（预期应更干净，单例分裂 bug 已随本地副本删除消除）。
- 待用户决定：① 修订/废弃 boundary-spec（加废弃头指向 migration-task-system）或 ② 继续代码执行（建议先 G-1 去重，低风险+修 errorStore 单例分裂 bug）。

## 首跑 check:all FAIL → 修复（02:17）
- **用户跑 `check:all` 报 FAIL**：encv-mobile typecheck + 单测 FAIL（3 suite）。日志实际运行 02:17（在我的 G-1 删除之后）。
- **根因（推翻上一轮错误假设）**：我曾以为"tsconfig `@/*` 二级回退到 shared 对运行时也生效"。**只对 TS 类型检查成立；Vite/vitest 运行时默认不读 tsconfig paths**。`vite.config.ts` 有 `encv-alias-fallback` 插件（本地优先+shared 次之）故 dev/build 正常；但 `vitest.config.ts` 的 `resolve.alias['@']` 仅指向本地 src、无 shared 回退，且 `plugins:[vue()]` 无该插件 → 测试环境 `@/composables/useToast` 等解析不到 shared。
- **两个具体错误**：① `activeStatus.test.ts` 在 `composables/` 内用相对 `./activeStatus` 导入（我之前 grep 模式漏了"同目录相对导入"），删本地副本后断链；② `useAgent.ts` 的 `@/composables/useToast` 在 vitest 解析失败，拖挂 renderTurnItems 两个测试 + activeStatus 测试。
- **修复（架构对齐，非回退垫片）**：① 提取共享插件 `encv-mobile/vite-plugins/encv-alias-fallback.ts`（逻辑同 vite.config.ts inline 版，加 `enforce:'pre'` 以便在 vitest 本地 `@` 别名前生效），加入 `vitest.config.ts` 的 `plugins`；② `activeStatus.test.ts` 的 `./activeStatus` → `@/composables/activeStatus`。`vite.config.ts` 的 inline 版未合并（避免改动无法被 check:all 验证的 dev 配置）。
- **待复验**：请用户重跑 `pnpm check:all` 确认转绿。
- **教训**：删除本地副本前，除"tsconfig 有回退 + 无相对路径引用"外，还必须确认 **Vite/vitest 运行时别名也有 shared 回退**（tsconfig paths ≠ 运行时解析）。测试文件若用相对路径导入被删模块，需一并改 `@/` 别名。

## Module G 全部闭环（02:30）
- **G-3 事实核查**：`VirtualLogList.vue` **只存在于 shared**，encv-mobile 本地副本早已不存在；`DevLogs.vue:248` 用 `@/components/VirtualLogList.vue` 经 `@/` fallback 解析到 shared。文档"删本地重复"前提不成立 → G-3 无需操作即完成。残留 `components.d.ts:101` 陈旧相对类型声明（生成文件，生产构建自愈，首跑 typecheck 未报错，不阻塞）。
- **Module G 状态：全部闭环 ✅**：G-1/G-2（composables 去重+修 vitest 回退）、G-3（无本地副本）、G-4（shared 为 canonical 保留）。encv-mobile 与 shared 通用 composables/components 重复已消除，统一经 `@/` 别名回退 shared。
- 后续唯一大块残留：`shared/api/` 非任务域 encv_*（files/admin/openlist/...）暂存残留，属 A-ext/Phase 6，需 api/core 重写或回退 encv-mobile，风险高，未启动。
- 文档 `migration-task-system.md` §9 已更新：G-3 标记完成、Module G 状态闭环、复验待用户跑 `pnpm check:all`。

## 三次迭代定位 vitest `@/` 解析失败（03:19 仍 FAIL → 真正根因）
- 02:57 复跑仍 3 suite FAIL（同错）。当时误判为"vitest 打包使插件内 `__dirname` 指向 config 目录"，改为显式传 `roots`——是稳妥的健壮性改进，但**不是决定性修复**，复跑仍 FAIL。
- **真正根因（03:18 复跑后定位）**：`vitest.config.ts` 的 `test.alias` 与 `resolve.alias` 都设了 `'@': SRC_DIR`，该别名把 `@/composables/useToast` **先解析成本地绝对路径**（已删除→不存在），fallback 插件来不及回退 shared 就报错；且 `enforce:'pre'` 的 `resolveId` 在 vitest transform 期 `this.resolve()` 里并不可靠被咨询。
- **对照 `vite.config.ts`**：其 `resolve.alias` **根本没有 `'@'` 别名**，仅靠 inline `encv-alias-fallback` 插件（normal 阶段、无 enforce）解析所有 `@/...` → dev/build 一直正常。vitest 缺的就是这点。
- **决定性修复（与 vite.config 完全对齐）**：① 从 `vitest.config.ts` 的 `test.alias` 和 `resolve.alias` **同时移除 `'@': SRC_DIR`**；② 插件去掉 `enforce:'pre'` 保持 normal 阶段；③ 保留显式传 `roots` 的健壮性。
- 已验证：`packages/shared-components/src/composables/useToast.ts`、`activeStatus.ts` 均存在；lint 0 错误；doc §9 + MEMORY.md 已据实更新（推翻了上一轮 __dirname 误判）。
- **待复验**：请用户第四次复跑 `pnpm check:all` 确认转绿。若仍 FAIL，需在插件 `resolveId` 加 `console.error` 打印 source/roots 看是否真被调用。

## 四次迭代最终定位（03:28 复跑仍 FAIL → 决定性根因）
- 03:28 复跑**仍 FAIL**，但现象变了：这次失败的不只是 shared 回退项，连**本地 src 真实存在的文件**（`@/composables/renderTurnItems`、`@/composables/appServerRealtimeReducer`、`@/lib/workflow/state-machine`、`@/lib/workflow/types`）也 `Failed to resolve`。已逐一确认这些本地文件都存在。
- **决定性根因**：测试跑在 `'fast'` **project** 下，而 `plugins` 只配在**根配置**。`vitest`/`vite 8` 的 **projects 模式不继承根配置的 `plugins`**——每个 project 是独立 Vite 配置，必须各自带 `plugins`。`'fast'` project 里没有任何插件 → `@/...` 无人解析 → 全部失败（连本地文件都解析不了是此根因的判定特征）。前三次（缺插件 / __dirname 误判 / `'@'` 别名抢先）全是干扰项、均非决定性。
- **对照 `vite.config.ts`**：单配置无 projects，根 `plugins` 直接生效 → dev/build 一直正常。vitest 用 projects 才暴露差异。
- **修复**：抽 `BASE_PLUGINS = [vue(), encvAliasFallback({ roots: [SRC_DIR, SHARED_SRC] })]`，**根配置 + 每个 project（fast/isolated）都引用同一份**。插件 `resolveId` 加"仅解析失败时 `console.error('[encv-alias-fallback] NOT FOUND', source, dirs)`"调试日志，便于下次一锤定音。
- 已验证：本地 4 个文件存在、lint 0 错误、doc §9 + MEMORY.md 已据实更新（推翻前三轮误判）。
- **待复验**：请用户第五次复跑 `pnpm check:all`。若仍 FAIL：日志**无** NOT FOUND → 插件仍未被调用（projects 继承问题未解决）；日志**有** NOT FOUND → 路径错（看 dirs 打印）。
