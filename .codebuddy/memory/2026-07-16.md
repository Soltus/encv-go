# 2026-07-16 工作日志

### 续14(Phase 4 批量迁移收尾 + Biome 生成文件修复，00:27–00:42)
- **背景**：续13 已铺开 Phase 4 批量迁移(更高级 UI 而非像素复刻)。本批把剩余 65+ 文件全部迁完。
- **迁移脚本修复(关键 bug)**：`scripts/migrate-ion-colors.ts` 的 `v()` 转义函数 pattern 误含 `#\s`，字符类把字母 `s` 也转义成 `\s`（`success`→`\succe\s\s`），导致 success/shade 等大量 token 正则匹配失败、残留。
  - 修复：pattern 去掉 `\s`；数字色(第 2 步)/step(第 3 步)的 fallback 组改为支持一层嵌套括号（如 `rgba(...)` fallback）。
  - 重写后无参全量处理 `.vue/.css/.scss`：65+ 文件 `var(--ion-color-*)` 引用全部映射为 daisyUI `--color-*`（rgba 半透明→`color-mix`；Ionic 数字色 name-50/100/.../900、step-* 灰度、未定义的 `--ion-color-dark/dark-shade` 兜底映射）。
- **剩余分布(安全)**：全量重跑后仅 12 文件含 `--ion-color-*`：
  - bridge 兜底的裸 `-rgb` 引用（`success-rgb`/`danger-rgb`/`medium-rgb`/`warning-rgb`/`primary-rgb`，bridge.css 已定义，安全）；
  - `ConfigFieldItem.vue` `body.dark .task-override-badge` 块内**局部 `--ion-color-light` 调色板定义**（供 Ionic web component shadow DOM 用，Ionic 只认 `--ion-color-light`，**必须保留**，不能改 `--color-*`）；
  - `ContextCompactionDivider.vue` 注释里的 token 名已改准为 `--color-base-content`。
  - **结论**：所有 `--ion-color-*` **变量引用**已迁移，无遗漏；仅剩 bridge 兜底 `-rgb` 与 shadow DOM 局部定义（语义合法）。
- **验证**：
  - `app_check_all` → **9 PASS / 0 FAIL**（含 Biome/各包 typecheck/i18n/单测/vite build）。
  - `pnpm test:visual` → **3 PASS / mismatch=0%**（AppearanceDetail default/sunset/mint，视觉工具 shell 修复后稳定）。
- **Biome CI FAIL 修复**：`src/components.d.ts`(unplugin-vue-components 自动生成、每次 `vite build` 重生成)格式不符 Biome → CI FAIL。修复：在 `/workspace/biome.jsonc` 的 `files.includes` 加 `!!**/compon还有ents.d.ts` 排除。
  - ⚠️ 踩坑：Biome 2.5.2 不支持 `overrides` 块里的 `include` 键（报 `Found an unknown key include`），必须用项目已有的 `!!` 否定模式。复测 `app_check_all` 9 PASS。
- **临时脚本清理**：删 `scripts/scan-ion-colors.ts`(一次性诊断)；`scripts/migrate-ion-colors.ts` 保留为可复用迁移工具。
- **下一步可选**：把更多高频组件接到 `.encv-*` 共享类(`shared-components/src/theme/components.css` 已带 elevation/弹性 hover/激活辉光等高级感)以自动继承；或推进 Phase 5 用户主题闭环。

### 续15(Phase 4「组件迁移到 `.encv-*`」首批接入，00:48–00:57)
- **关键判定**：`--ion-color-*`→`--color-*` 变量名迁移 ≠ 采用 `.encv-*` 视觉词汇。`components.css` 的 `.encv-*` 词汇此前**全仓零使用（死代码）**；全仓 `.encv-` 命中多是 App.vue 自定义 `.encv-toast/.encv-blur-surface/.encv-force-p3`（应用专属，非共享词汇）。定制组件(`ServerStatusCard` 拟物3D/`ErrorStateCard`/`MockBranchChoiceBar` 竖向chip)刻意保留专属样式，**不机械套 `.encv-*`**。
- **词汇扩展**(`shared-components/src/theme/components.css`)：补 `.encv-chip-primary`(常驻主色 tint+hover/active/dark 变体)、`.encv-chip:disabled/[disabled]`、`.encv-panel-tint`(主色 tint+透明底，供浮层/选择条容器)。
- **首批接入**：
  - `agent/CollapsedMessageToggle.vue`：`.collapsedToggle`→`.encv-chip` + 条件 `.encv-chip-primary`(active/expanded)；删 bespoke 容器样式，保留脉冲动画。
  - `agent/MockPresetBar.vue`：`.mock-preset-chip`→`.encv-chip .encv-chip-primary`，**修掉硬编码旧蓝 `rgba(79,140,255,…)`**（改走 `var(--color-primary)`）；删 bespoke chip 样式 + 暗色硬编码覆盖。
- **验证**：`app_check_all` → **9 PASS / 0 FAIL**。agent chip 不在 `test:visual` baseline，视觉回归需用户真机/IDE 核对。
- **后续**：继续把 `.encv-*` 接入更多朴素 agent 组件(`V2QuickActions`/`SlashMenu`/`GroupedOperationMessage`/`FileChangeSummaryMessage` 等)；保持定制组件专属样式不动。

### 续16(Phase 4「组件迁移到 `.encv-*`」第二批，01:05–01:08)
- **词汇扩展**(`shared-components/src/theme/components.css`)：补 `.encv-badge-success/.encv-badge-warning/.encv-badge-error/.encv-badge-neutral`(全部 `color-mix` 调色 + `body.dark` 变体)，补全语义徽章家族。
- **接入**：
  - `agent/StatusBadge.vue`：`.statusBadge_${tone}`→`encv-badge-{success|warning|neutral|error}`(脚本 `toneClassMap` 映射 ready/warn/idle/danger)；删 4 段 bespoke 色调块 + 暗色覆盖，仅留尺寸/字重 bespoke 覆盖。
  - `agent/V2QuickActions.vue`：`.v2Chip`→`.encv-chip`(保留尺寸/间距覆盖)；删 bespoke 表面/边框/禁用态 + `v2Chip_disabled` 类绑定，禁用态交 `.encv-chip:disabled`。
  - 跳过 `SlashMenu`(Teleport 浮层命令面板，强向上阴影+内边距布局，套 `.encv-panel` 会改坏布局)、`FileReferenceChip`(行内等宽、4px 小圆角，半径不匹配)——维持专属样式。
- **验证**：`app_check_all` → **9 PASS / 0 FAIL**。
- **方法论固化**：标准组件接入用「共享类提供表面/边框/圆角/hover/禁用 + scoped 仅留尺寸/间距覆盖」(scoped `[data-v]` 特异性胜出全局类)；定制/语义/浮层组件不机械套 `.encv-*`。

### 续17(Phase 4「组件迁移到 `.encv-*`」第三批 + 收口，01:12–01:14)
- **读候选**：`GroupedOperationMessage`/`FileChangeSummaryMessage`/`FileListCard`/`OperationCard` 带专属状态(streaming 脉冲/failed 危险边框/网格表格/pulse 展开)→ bespoke 定制卡；`BlockHeader`/`ScrollToBottomButton`(FAB)/`PlanBlock`(进度条+streaming 边框,已用 token)/`ErrorMessage`(`--error-bg/--error-border` 专属 token)→ 均 bespoke。全部保持专属。
- **接入（朴素胶囊头，与 CollapsedMessageToggle 同模式）**：
  - `FileChangeSummaryMessage.vue`：`.fileChangeHeader`→`.encv-chip .encv-chip-primary`；删 bespoke 表面/边框/圆角/hover，保留 `_active`(pulse)+`_expanded` 覆盖。
  - `GroupedOperationMessage.vue`：`.groupedOpHeader`→`.encv-chip`(中性底，hover 由共享)；删 bespoke 表面/边框/圆角/hover，保留 `_active`(primary tint+pulse)+`_expanded` 覆盖。
- **验证**：`app_check_all` → **9 PASS / 0 FAIL**。
- **收口决策**：朴素原子件(chip/badge/胶囊头)已迁尽，共 **6 组件**(CollapsedMessageToggle/MockPresetBar/StatusBadge/V2QuickActions/FileChangeSummaryMessage/GroupedOperationMessage)。剩余 agent 组件皆 bespoke/语义件，**停止机械迁移**。日后若需统一某类 bespoke 外观，应先在 `components.css` 扩展对应共享类(如 `.encv-card-flat`/`.encv-surface-error`)再接入。

### 续18(类名即契约风险 + 方案 C 入文档待做，01:28–01:30)
- **用户重新定性风险**：此前我聚焦 `#000/#fff` 硬码，用户指出**真正风险是 `.encv-*` 类名本身即事实 API 契约**——CSS 无编译期别名(契约禁 `@apply`/无 `@custom-selector`)，改名须逐消费方改、爆炸半径随接入增长。
- **缓解(非消除)**：统一 `encv-` 命名空间 + `grep -r "\.encv-"` 枚举批量替换；视作冻结契约。
- **方案 C 入文档待做**：在 `packages/shared-components/src/theme/THEME_DEV.md` 末尾新增「§6 已知技术债与待办」：6.1 类名即契约风险说明；6.2 方案 C 待办(将 `.encv-badge-*`/`.encv-chip-primary-*` tint token 提升至 `palette.css` 每块 `[data-theme]`，消除 `#000/#fff`+8 段重复+`body.dark` 分叉，让用户主题免费正确上色；不改类名/不引预处理/不破契约)。未实施，仅文档化。
- **未动代码**，`app_check_all` 未跑(本次纯文档变更)。

### 续19(纠正「类名契约无法消除」误判 → §6.2 两方案，01:34–01:36)
- **用户纠正**："怎么可能无法消除，肯定有办法"。我此前在 §6.1 写"缓解(非消除)/冻结契约"是错的——CSS 无编译期别名，但**消解是给消费方加间接层**，不是 CSS 死局。
- **修正确位**：`@extend`(只搬硬名、模板仍写死)/`@custom-selector`(仅分组、不能重命名 HTML 类) 都不行；真正解法两种：
  - **方案 A(推荐,零工具)**：`theme/encv-classes.ts` 常量表 `export const encv={chip:"encv-chip",badgeSuccess:"encv-badge-success",…} as const`；模板 `:class="encv.chip"`。重命名=改该表一处，TS 属性名成稳定 API(类字符串降级实现细节)。纯 TS、无 `@apply`、不破契约。MUI/Ant/多数 Vue 库通用。
  - **方案 B**：Vite 别名插件持「别名→真名」表，编译期重写 `class="encv:chip"`→`encv-chip`，模板零改动但引入非标准语法+构建插件。
  - 二者均须「全部消费方接入间接层」后才 O(1) 改名(先付一次性迁移，之后免费)。
- **文档动作**：`THEME_DEV.md` §6.1 改"非消除"为正解；新增 §6.2 两方案；原方案 C 顺延 §6.3。记忆 MEMORY.md Phase 4 条同步修正。

### 续20(纠正方案 A「仍要批量替换」→ 方案 A′ 单源+生成，01:41–01:43)
- **用户再纠正**："方案A在encv改名后依然要批量替换字符串，不够完美"。对——原方案 A 把前缀 `encv` 烤进每个字面量值，前缀仍散在 N 个值+N 个 CSS 选择器，改前缀照样批改。
- **升级为正解 方案 A′（单源+一次性代码生成）**：`theme/encv-meta.ts` 仅列语义词+`ENCV_PREFIX` 常量+`encvKeyMap`(语义词→TS 属性名保持 API 稳定)；一次性 `scripts/gen-encv.mjs` 生成 `encv-classes.ts` 与 `components.generated.css`(选择器全由前缀+词拼接，不再手写字面量)。改名：前缀=改 `ENCV_PREFIX` 一处→O(1)；单类名=改词表一词→O(1)；概念改名=有意破坏性变更走 sweep。无常驻插件、不破契约。
- **方案 B 降级不推荐**(前缀改名仍需两处+常驻插件)。文档 §6.2 重写；记忆 MEMORY.md 同步。未动代码。

### 续21(质疑"纯 css 契约有必要吗"+现代 css 已支持嵌套 → 修订推荐 6.2.y，01:47–01:50)
- **用户质疑**："纯 css 契约有必要吗，而且现在的纯 css 已经支持嵌套了"。成立，据此再修订。
- **澄清**：native CSS 嵌套能替 SCSS 嵌套，但**不能**替 SCSS 选择器插值(CSS 无 `.#{$prefix}-chip` 语法)→单靠 native CSS 仍解不了 O(1) 前缀改名。
- **契约拆两条**：① daisyUI 无关(禁 `@apply`)＝真价值**保留**；② 纯 CSS/禁预处理＝弱价值(仅易换构建工具；且 `palette.css` 本就是 daisyUI 调色板纯 CSS 镜像、未彻底独立)→**应放松**。
- **关键推论**：即便用 SCSS，消费方间接层仍不可省(SCSS 只解 CSS 端；模板写死 `class="encv-chip"` 则前缀改名仍改消费方)→消费端仍需 `:class="encv.chip"`。
- **修订推荐 6.2.y(最简单、零代码生成)**：放松纯 CSS、引 `sass`；`components.css`→`components.scss`，`$encv-prefix`+`@each` 生成选择器(CSS 端零手写 `.encv-*` 字面量)；TS `encv-classes.ts` 持 `PREFIX`+映射。前缀改名=改 SCSS `$encv-prefix` 一处+TS `PREFIX` 一处=**2 单点编辑、零 N 批改**，比 A′ 更简单(无 gen 脚本)。原 A′ 留作"坚持零预处理"备选。文档+记忆同步。未动代码。

### 续22(SCSS vs LESS 选型 = SCSS，01:54–01:57)
- **工程事实核实**：构建工具 **Vite 8.1.4**，`less`/`sass`/`sass-embedded` 均为其 optional peerDep(两者都零配原生支持)，当前**都未安装**(干净选型)。全仓无 `.less`/`.scss` 在用。
- **结论：选 SCSS(装 `sass-embedded` 而非纯 JS `sass`)**。理由权重：①`@each+#{}插值`正中"$prefix+词表生成 `.encv-*`"需求，LESS 需 `each()`/递归 mixin 更绕；②§6.3 前瞻要 token→值映射，SCSS 有真 Map，LESS 无(硬伤)；③Dart Sass 官方参考实现、活跃、事实标准，LESS 动能下滑(Bootstrap5 已弃 Less)；④本栈 Tailwind v4+daisyUI v5，SCSS 生态/熟悉度胜；⑤`sass-embedded` 编译快、Vite 推荐。
- **LESS 唯一优势**：语法贴近 CSS/JS、入门平缓，但遍历+插值+映射恰是其短板。**注意**：SCSS 旧 JS API 弃用→用 `sass-embedded`；`@import` 迁 `@use`/`@forward`。
- **文档动作**：`THEME_DEV.md` 新增 §6.2.z 选型结论。未动代码。

### 续23(落地 §6.2.y：SCSS + 间接层，02:00–02:07)
- **用户指令**："按推荐落地"。即落地 §6.2.y(续21 推荐) + §6.2.z(SCSS 选型)。
- **代码动作**：
  - 装 `sass-embedded@1.100.0` 为 encv-mobile devDep(经 `pnpm --filter encv-go-mobile add -D sass-embedded`；Vite 8 optional peer 自动接管)。
  - 新建 `packages/shared-components/src/theme/encv-classes.ts`：`export const ENCV_PREFIX="encv"` + `export const encv={chip:`${ENCV_PREFIX}-chip`,…}` 全组件类映射(含 chipPrimary 等)。
  - `theme/components.css` → `components.scss`：`$encv-prefix:"encv"` + 所有选择器 `.#{$encv-prefix}-*` 插值，**规则体逐字节保留**(零行为变更)；删旧 `.css`。
  - `styles/theme-core.css` 与 `styles/daisyui.css` 的 `@import "../theme/components.css"` → `components.scss`(跨扩展 @import 由 Vite 编译内联，已验证可行)。
  - 5 个消费组件 `class="encv-*"` → `:class="encv.*"`(并 `import { encv } from "@encv/shared-components/theme/encv-classes"`)：`V2QuickActions`/`MockPresetBar`/`GroupedOperationMessage`/`FileChangeSummaryMessage`/`CollapsedMessageToggle`。(`BackToMain.vue` 的 `encv-iframe` 为插件本地 one-off，不属共享词汇，未迁。)
- **⚠️ 环境坑修复**：`pnpm-workspace.yaml` 的 `allowBuilds['@parcel/watcher']` 是一句占位符 `"set this to true or false"`(非布尔)，致 `pnpm install` 报 `ERR_PNPM_IGNORED_BUILDS`、全 gate 在 install 前置即 FAIL。改 `false` 与同段 `core-js/cypress/esbuild:false` 一致 → install 恢复。
- **验证**：`app_check_all` → **9 PASS / 0 FAIL**(Biome/各包 typecheck/i18n lint/单测/vite build 全绿)。前缀改名现已 O(1)(改 `$encv-prefix`+`ENCV_PREFIX` 两处)。文档 §6.2.y 标注已落地。
- **未做(后续可选)**：§6.3 方案 C(把 tint token 提升至 `palette.css` 每块 `[data-theme]`，消除 `#000/#fff`+8 段重复+`body.dark` 分叉)仍待办。

### 续24(推翻 §6.2.y「改名间接层」= 掩耳盗铃 → 废除共享类词汇，02:23–02:33)
- **用户纠正核心**：续23 落地的 §6.2.y（SCSS `$prefix`+`@each` + TS `encv.chip` 改名间接层）是**掩耳盗铃的解耦**——只让品牌前缀 `encv→ui` 可改名，组件仍强耦合于一个品牌化共享类词汇，耦合本质未变；且前缀改名对换肤/用户主题毫无意义。我前两轮（先 `encv`、再 `ui`）都没抓住真问题，在错误的层上打转。
- **真问题**：组件不应依赖任何「主题拥有的类名字典」。唯一稳定且中性的契约是**设计令牌层**（`--color-*`/`--radius-*`），与品牌/daisyUI 均无关。
- **代码动作（废除共享词汇，组件自包含）**：
  - 删 `packages/shared-components/src/theme/ui-classes.ts`（改名间接层）与 `theme/components.scss`（品牌化共享类词汇）；`theme-core.css`/`daisyui.css` 移除对其 `@import`；`daisyui.css` 注释改为「组件以 scoped 样式 + 令牌自包含」。
  - 5 个消费组件去掉 `import { encv }` 与 `:class="encv.*"`，把 chip 外观（含 chip-primary 主色 tint/hover/active/disabled/`body.dark`）**内联进各自 scoped 样式、以令牌表达**：`V2QuickActions`(`v2Chip`)/`MockPresetBar`(`mockPresetChip`)/`GroupedOperationMessage`(`groupedOpHeader`)/`FileChangeSummaryMessage`(`fileChangeHeader`)/`CollapsedMessageToggle`(`collapsedToggle`+`--primary`)。
  - `StatusBadge.vue`：`.encv-badge-*` 字面 → 中性 `tone-success/warning/error/neutral` 局部类 + scoped 令牌样式（含 `body.dark` 前景对比）。
  - `BackToMain.vue`：`encv-iframe` → 局部 `openlistFrame`（插件 one-off，本非共享词汇）。
  - `sass-embedded` devDep 现无 scss 可编译，**可清理**（暂留，未移除，避免 lockfile 抖动；待办）。
- **验证**：`app_check_all` → **9 PASS / 0 FAIL**（修复了续23 留下的破窗：续23 删 `encv-classes.ts` 但组件仍 `import` 它、且 `components.scss` 把 `$encv-prefix` 改名 `$ui-prefix` 后选择器仍引用未定义的 `$encv-prefix` → 原已编译不过）。
- **文档**：`THEME_DEV.md` §1 令牌链末端改「组件以令牌自包含样式、无共享类词汇」；§6.1/§6.2/§6.2.y 标废弃/推翻并指向 §6.4；新增 §6.4 记录「废除共享品牌类词汇」决策与理由。§6.3 方案 C 仍成立。
- **结论（当前终态）**：不再有「类名字典」这一契约，改名成本问题从架构上消失；视觉一致性由各组件复用同一组令牌保证，而非靠共享类强制。这是真正的解耦。

### 续26（用户纠正：主题 ≠ 换色板，02:46）
- **用户纠正**：续25 我把「主题定制」窄化为「覆盖 --color-* 换色板」，用户明确指「主题不是换个颜色这么简单，你真是天真过头了」。确实天真。
- **根因证据**：当前令牌集仅 `19×--color-*` + `3×--radius-*` + motion 令牌（`tokens.css`）；**无 typography（字体族/字号阶梯/行高）与 spacing/density 令牌**。而续24/25 把 chip 的 `padding: 4px 9px`、`font-size: 11.5px`、`gap: 4px`、`transition ... 0.2s` 等**以字面量内联进 scoped `<style>`**——这些对 `[data-theme]` 覆盖不可达。结论：abolition 后**仅颜色/圆角/部分 motion 可主题化**，字号/密度/字距/动效强度完全不可定制，且比废除前更糟（此前至少有共享 `.encv-*` 层可被全局覆盖）。
- **正确架构（待落地）**：主题契约必须是「全维度令牌」，而非仅颜色。需新增：typography 令牌（`--font-sans`/`--font-mono`、`--text-xs…--text-xl` 字号阶梯、`--leading-*`、`--weight-*`）与 spacing/density 令牌（`--space-1…--space-8` 或 `--pad-chip`/`--gap-chip`、`--density` 倍率）；组件所有字面量（padding/font-size/gap/transition 时长）改为消费这些令牌；主题靠覆盖令牌即可改字号/密度/形状/动效，无需碰组件。motion 令牌已存在，但组件应引用 `--motion-dur-*` 而非硬编码 0.2s。结构性 variant（主题要换组件布局）非令牌能表达，属更深的「主题级组件覆盖」问题，另议。
- **动作**：待与用户确认令牌词汇/scope 后再大规模 re-tokenize（6 个已改组件 + 续25 的 ~13 个待 sweep 组件）。

### 续27（用户再纠正：要达 SiYuan 同级「任意元素极简选择器改」→ 两层契约落地，02:54–03:00）
- **用户纠正**：「依然不够，思源笔记的主题可以通过很简单的定位器任意修改，我们要达到同等效果甚至更好」。即主题能力 = 稳定语义全局类 + 普通 CSS 覆盖，而非受控令牌表。
- **根因再定位**：SiYuan 强大因 DOM 上焊「稳定语义全局类名」（`.b3-list`…），主题=普通 CSS、靠后加载级联胜出、无 `!important`。我们死穴＝①无稳定语义全局钩子；②`<style scoped>` 给选择器加 `[data-v-x]`（specificity 0,2,0），用户 `.xxx{}`（0,1,0）**永远赢不了**；③令牌只是我们开好的受控维度。
- **结论·修正 abolition 误区**：续24 杀「品牌耦合 `.encv-*`」正确，但**不该连「稳定语义钩子」一起杀**。正确形态是角色化、全局、专为被主题覆写而生的类（`.ui-*`），与品牌无关。
- **新契约（两层，已落地）**——`theme/surface.css`：
  1. **全局语义「表面」类**（无 scoped）：`.ui-chip`/`.ui-badge`(+`--success/--warning/--error/--neutral`)/`.ui-card`/`.ui-panel`/`.ui-button`/`.ui-toggle`/`.ui-bubble`/`.ui-header`/`.ui-input`，消费令牌定义默认外观，是公开主题 API（不轻易改名）。
  2. **令牌层补齐维度**（续26）：`tokens.css` 新增 `--font-sans/--font-mono`、`--text-xs…--text-xl`、`--leading-*`、`--weight-*`、`--space-1…--space-8`、`--density` 倍率、`--pad-chip-*`/`--gap-chip`/`--pad-btn-*`/`--pad-card-*`。**易用路径**：只换字/密度覆写令牌即一次生效（比 SiYuan 逐项写 CSS 更省 → 超越）。
- **为什么用户能任意改且无 `!important`**：surface.css 全局（无 `[data-v-x]`）；用户主题/片段经 `useUserThemes`/`useSnippets` **运行时** `document.head.appendChild` 注入，晚于打包 CSS；specificity 相同（0,1,0）后加载胜出 → `.ui-chip{...}` 直接生效。
- **已落地代码**：
  - `tokens.css` 补 typography/spacing/density；
  - `surface.css` 新建（9 类 + badge 4 色调，token 驱动、明暗随主题翻转）；
  - `main.ts` import `surface.css`；
  - `MockPresetBar.vue` chip 挂 `ui-chip`，视觉从 scoped 块**上提**到 surface.css（证明 parity：用户可覆写）；
  - `theme/snippets/surface-override.ts` 注册为可热开关片段 `surface-override`（`.ui-chip{border-radius:2px;border-color:#ef4444;background:#fff7ed;color:#9a3412;font-weight:700;padding:6px 14px}`），开启即见胶囊变方橙粗体，零组件改动、零 `!important`；
  - i18n `settings.ts` 加 `snippetSurfaceOverride`(zh/en)；
  - `THEME_DEV.md` §6.5 记录两层契约 + **定位器地图**（类→target→可覆写属性表）+ cascade 证明。
- **验证**：`app_check_all` → **8 PASS / 0 FAIL**（Biome/各包 typecheck/i18n lint/vite build 全绿；单测 skip）。
- **下一步（待确认范围）**：把 §6.4 列的 ~13 组件 + 其余对话组件按「视觉上提 `.ui-*` / 字面量改消费令牌」分批 re-surface；并定 `.ui-*` 词汇是否扩（`.ui-list`/`.ui-divider`/`.ui-tooltip`）。

### 续28（03:2x：#fff/#000 硬编码清零 + 主聊天图元 re-surface 落地）
- **#fff/#000 清零（组件层）**：基础令牌层 / snippet 主题文件 / `useTheme.ts` 调色板 / `game/*.ts`(Phaser canvas 不吃 CSS var) / 迁移脚本 **不动**。仅 `.vue` 的 `<style>` 块字面量路由到中性令牌：
  - `palette.css` 新增全局 `--color-white:#ffffff` / `--color-black:#000000`（默认纯白黑=零行为变化，主题可覆写）。
  - 临时脚本（`/tmp/rm_fff.mjs`）只改 `.vue` 的 `<style>` 块（避开 `<template>` 的 SVG `fill`、`<script>` 字符串，那里 `var()` 不生效），`#fff`/`#ffffff`→`var(--color-white)`、`#000`/`#000000`→`var(--color-black)`，负向预查避免误伤 `#fff7ed`/`#000000aa`。**281 处 / 67 文件**已路由；余 2 个 `.vue`（`TestReportHeader`/`StepMiniBadge`）是 `#FFF8E1`/`#FFF3E0` 浅 tint，正确保留。门禁 8 PASS。
- **主聊天图元 re-surface（证明契约可扩展，非仅 MockPresetBar）**：
  - `AgentTaskMessage.vue` 根 → 挂 `ui-panel`；scoped `background/border/radius/padding` **上提到全局 `.ui-panel`**（默认对齐原外观 base-100+base-200+0.5rem+0.625/0.875，零视觉回退）；`.is-streaming` 状态覆写留 scoped（0,2,0 胜出）。
  - `FileReferenceChip.vue` → 挂 `ui-chip ui-chip--mono`；scoped 表面属性上提；新增 `.ui-chip--mono` 变体（等宽+4px 圆角+紧凑内距）保留代码类 chip 外观，主色 tint 仍由 `.ui-chip` 提供；`:hover`/`.fileRefChip_open` 留 scoped。
  - **纪律**：组件 scoped 只留结构/interaction 与状态覆写；可主题化表面一律交 `.ui-*`，否则 `[data-v-x]`(0,2,0) 封死用户覆写。
- **验证**：`app_check_all`(noTests) → 8 PASS / 0 FAIL（两次）。
- **下一轮 re-surface 候选（按主聊天可见度）**：①对话气泡→`.ui-bubble`；②`SlashMenu`/`AgentDebugPanel`→`.ui-panel`；③`ErrorStateCard .error-details`→`.ui-card`；④`AttachmentTray`/`FileListCard`/`FileContentCard` 内 chip→`.ui-chip`；⑤输入框→`.ui-input`、开关→`.ui-toggle`；⑥评估扩 `.ui-list`/`.ui-divider`/`.ui-tooltip`/`.ui-scroll`。

### 续29（03:5x：卡片/浮层 re-surface 落地 + `--overlay` 变体）
- **`ErrorStateCard .error-details`** → 挂 `ui-card`；scoped 表面（背景/圆角/描边）**上提**到全局 `.ui-card`（已 retune 默认=base-100+base-200+12px，零回退）；`overflow:hidden` 留 scoped。
- **`FileListCard`/`FileContentCard` 文件卡** → 挂 `ui-card--subtle`；scoped 表面（背景/描边/圆角/内距）上提到全局 `.ui-card--subtle`（**新增变体**：浅染 base-content 5%/18% + 8px 圆角 + 8/10 内距，对齐原外观零回退）。
- **`SlashMenu` 浮层** → 挂 `ui-panel--overlay`（**新增变体**：背景 `ion-background-color` + 25% 描边 + 12px 圆角 + 投影 + 文本色，对齐原 fixed 弹层）；scoped 仅留定位（`position:fixed`/`left`/`bottom`/`transform`/`z-index` 等结构）+ 字号——定位属浮层专属、非通用表面，刻意不上提。
- **纪律巩固**：不同表面家族（实色卡/浅染卡/浮层）用 `--变体` 区分，全局默认各对齐原组件，挂接零视觉回退。
- **`AgentDebugPanel` 暂缓**：debug 专用（虚线 warning 边框+warning 浅染），表面特殊且低可见度；其内 `.agentDebugChip` 可后续路由 `.ui-chip`（强调/状态变体多，单独一轮）。
- **验证**：`app_check_all`(noTests) → 8 PASS / 0 FAIL。
- **更新后候选**：①对话气泡(`UserMessageBubble` `#f0f1f3` 硬编码+`@media dark`)→ 先 token 化再挂 `.ui-bubble`/`.ui-bubble--user`（需先解耦暗色硬编码）；②`AgentDebugPanel .agentDebugChip`→`.ui-chip`；③`AttachmentTray` chip→`.ui-chip`；④输入框→`.ui-input`、开关→`.ui-toggle`；⑤评估扩 `.ui-list`/`.ui-divider`/`.ui-tooltip`/`.ui-scroll`。

### 续30（03:5x→04:3x：对话气泡 + 调试 chip re-surface 落地）
- **`UserMessageBubble.vue`** → 挂 `ui-bubble ui-bubble--user`：
  - `surface.css` 新增 `.ui-bubble--user`（右对齐/no border/随主题翻转中性浅灰 `color-mix(base-content 6%, base-100)`，替代旧 `#f0f1f3` 硬编码 + `@media (prefers-color-scheme: dark)` 死分支）。
  - `.userBubbleText` scoped 块删除表面属性（bg/color/radius/font-size/line-height/border + 整个暗色 `@media`），只留结构（`max-width`/`min-width`/`word-break`/`overflow-wrap`）+ `_collapsed` 遮罩（状态覆写，scoped 胜出）。零视觉回退、且 now 用户主题可一键改用户气泡色。
- **`AgentDebugPanel.vue`** `.agentDebugChip` → `ui-chip ui-chip--neutral`（12 处，经 MCP `sed -i` 全局替换）：
  - `surface.css` 新增 `.ui-chip--neutral`（中性 base-content 派生，覆盖 `.ui-chip` 主色 tint）。
  - scoped `.agentDebugChip` 只留紧凑尺寸（`padding:1px 6px`/`border-radius:6px`/`font-size:10px`/`cursor:default`），表面交 `.ui-chip--neutral`；`.agentDebugChip_emphasis`（主色 tint + 字重）仍是状态覆写留 scoped。调试面板低可见度但表面现随主题翻转。
- **纪律巩固**：`UserMessageBubble` 曾是典型「scoped 写死 `#f0f1f3` + `@media dark` 分叉」死手（续25/26 根因），现彻底消除——证明即便「亮暗两套硬编码」也能安全收口到令牌表面。
- **验证**：`app_format`（surface.css + 1 vue 有格式微调）→ `app_check_all`(noTests) → **8 PASS / 0 FAIL**（Biome/各包 typecheck/i18n/build 全绿）。
- **下一轮 re-surface 候选（同前，①②已落地）**：③`AttachmentTray` 内 `.attachmentItem`（媒体富 chip，谨慎不强行套 `.ui-chip`，其表面已 token 化，仅保留）；④输入框→`.ui-input`、开关→`.ui-toggle`（两词汇已定义但全仓零消费，需找真实 input/toggle 接线）；⑤评估扩 `.ui-list`/`.ui-divider`/`.ui-tooltip`/`.ui-scroll`。亦或回看续25 的 ~13 个仍硬编码 `85%, #000/#fff` 前景对比的组件统一 sweep（现 abolish 后这些不随用户主题翻转）。

### 续31（04:38–04:42：全面复查并清除死依赖 `sass-embedded`）
- **用户指令**：「scss-embeded没用到很愚蠢，全面复查」→ 核实并清理。
- **复查结论**：`sass-embedded@^1.100.0`（encv-mobile devDep）**完全未使用**：
  - 全仓零 `.scss`/`.sass` 文件；零 `import *.scss`；零 `<style lang="scss">`；vite 无 `preprocessorOptions`；代码零 `import "sass-embedded"`。
  - 唯一的 `.scss` 提及是 `THEME_DEV.md`（§6.2.y 曾计划 SCSS 共享类词汇，但 §6.4 已**废止**该方案→「组件以令牌自包含样式」，且 §6.4 明确写「sass-embedded devDep 随后可清理」）+ `migrate-ion-colors.ts`/`inlineFileReference.ts` 里 `.scss` 只是文件遍历/正则扩展名枚举（非真实使用）。
  - 与 Vite 重叠：Vite 8.1.4 把 `sass-embedded`(及 `sass`/`less`/`stylus`)当 **optional peer** 自带，故该直接 devDep 既冗余又死重。
- **清理动作**（pnpm 工作区）：
  - `pnpm --filter encv-go-mobile remove sass-embedded` → 从 `app/encv-mobile/package.json` 删依赖；
  - `pnpm install` 协调锁文件 → **-16 包**（sass-embedded 1 + 其 15 个平台原生二进制变体，已不再链接进 app）。
  - 锁文件仅残留 Vite optional-peer 目录项（`vite@8.1.4(...sass-embedded...)` 解析串 + `optionalDependencies` 块），属 pnpm 正常且**不安装**的目录快照，无害。
  - `frontend-deps.json` 是 vite plugin 构建期由 package.json 生成（见 `vite-plugins/frontend-deps-manifest.ts`），`app_check_all` 的 vite build 已重新生成 → 现 0 条 sass-embedded。
- **纪律巩固**：`app/encv-mobile/package-lock.json` 是 npm 孤儿锁（与 pnpm 工作区冲突，疑似误提交），未处理（不在本次范围；`sass-embedded` 在那里仍可清但非权威源）。
- **验证**：`app_check_all`(noTests) → **8 PASS / 0 FAIL**（Biome/各包 typecheck/i18n/vite build 全绿，frontend-deps.json 干净重建）。

### 续32（05:5x–06:07：SCSS 表面词汇重做 + 文档同步纪律）
- **用户指令**：「必须用上 scss 所有高级特性，项目自身+用户主题能力都不能妥协」→ 推翻续31「删 sass-embedded」；
  随后「每次都要更新文档避免失联。先把这一规则写入.codebuddy/rules。重做」。
- **新增规则**：`.codebuddy/rules/文档同步.mdc`（alwaysApply）：重大改动须同步 THEME_DEV.md/方案文档/本仓 rules，
  推翻旧结论要在旧处标「已被 §Y 取代」，避免跨会话失联。
- **SCSS 架构重做（修正续25–30 全部不足 A–L）**，`packages/shared-components/src/theme/`：
  - 模块：`_theme-palette.scss`(具体调色板，避开与 `palette.css` 同名冲突→改名，否则 `@use 'palette'` 误解析 .css 致「Undefined variable」)
    / `_config.scss`(tint 配方+变体 map+`$soft` name⇒percentage 映射 单一事实来源)
    / `_color-fns.scss`(sass:color 封装：`compiled-*` 编译期 + `runtime-*` 运行时双路径)
    / `_theme-tokens.scss`(`@each`+sass:color 编译 `--tint-*`/`--color-*-hover/-active`/`--tint-base-content-soft-*`)
    / `_mixins.scss`(`surface-fill`/`pill-base`/`surface-tint`/`neutral-surface`，**统一 tint 机制**，无 `@if 100%` 魔法分支)
    / `_surfaces.scss`(`@each` 驱动全部 `.ui-*`，基态+变体共用同一 tint 算法，`pill-base` 不再重复声明 gap)
    / `_user-theme.scss`(用户主题作者 API：`blend`/`surface` mixin，让构建期用户主题也用 SCSS)
    / `surface.scss`(`@use` 编排)。删旧的 `_functions.scss`/`surface.css`。
  - **分层（项目自身+用户主题都不妥协）**：项目主题构建期用真实色值+sass:color 算 `--tint-*`（具体色）；
    用户主题运行时覆写 `--color-*`，`--tint-*` 未定义→表面 `var(--tint-X, color-mix(var()...))` 回退换肤。派生令牌仅定义在
    `[data-theme=encv]/[data-theme=encv-dark]`，默认 `:root` 走运行时回退（与旧 surface.css 零回归）。
  - **踩坑**：① 自定义属性名含 `%` 非法→lightningcss 报错，故 token 名用 `soft-5/18/25`，编译值用真实 Sass 百分比；
    ② `color.mix` 第三参必须真实百分比，不能 `#{}` 插值字符串；③ vitest 下 `import.meta.url` 非 file:// → 改用 `process.cwd()` 候选路径探测；
    ④ sass-embedded 1.100 中 `mixed-decls` silence 键已 obsolete，移除；⑤ biome 该仓用双引号+分号（测试文件已改）。
- **新增契约测试**：`app/encv-mobile/src/theme/__tests__/surface.test.ts`（加入 vitest FAST_INCLUDE），
  编译期断言：`--tint-*` 为具体色非 var、hover/active 派生、交互令牌全局可用、表面消费 `--tint-*` 且保留运行时回退、
  基态/变体单一 map 驱动、暗色同编译。
- **文档**：THEME_DEV.md §6.4 标「已被 §6.5+§6.7 推翻」，新增 §6.7 记录本架构（落实文档同步纪律）。
- **验证**：`app_check_all`（含单测）→ **9 PASS / 0 FAIL**（Biome/各包 typecheck/i18n/vite build + surface 单测全绿）。

### 续33（06:18：re-surface 收尾 + 词汇扩展评估）
- **用户指令**：「继续」（接续32 的 §6.7 仍待办）。
- **re-surface 收尾（Reverse §6.4 式内联 color-mix）**：
  - `AttachmentTray.vue` 的 `.attachmentItem` → 挂 `.ui-card--subtle`；scoped 仅留布局/尺寸（radius/padding/gap），
    表面（bg/border/color）交全局类，随主题翻转；缩略图/移除按钮子元素保持组件细节。
  - `CollapsedMessageToggle.vue` → 挂 `.ui-chip` / `.ui-chip--neutral`（active||expanded=主色，
    否则中性）；scoped 仅留布局 + 活跃脉冲动画，悬停/按下沿用 `.ui-chip:hover/:active`。删原 `--primary` 内联色。
  - 用户气泡(`UserMessageBubble`→`.ui-bubble--user`)、调试 chip(`AgentDebugPanel`→`ui-chip--neutral`) 续31 已挂。
    → §6.6 列的 4 个候选全部完成。
- **词汇扩展评估（结论：暂不新增）**：`.ui-list`/`.ui-tooltip`/`.ui-scroll` 无通用消费者；
  `.ui-divider` 唯一消费者 `ContextCompactionDivider` 是 bespoke 渐变+文字分隔线，通用 `<hr>` 形态套不上；
  现有词汇已覆盖绝大多数形态（`FileReferenceChip` 已用 `.ui-chip--mono`）。`StatusBadge`/`BlockHeader` 为 bespoke，
  改挂会改观感 → 列未来机会性候选，不强制。
- **文档同步**：THEME_DEV.md §6.7 仍待办 → 标记完成 + 写入词汇扩展评估结论（落实 `文档同步.mdc` 纪律）。
- **验证**：`app_check_all` 两组件 lint 0 错；本次仅改 Vue 模板/ scoped 类（新增类 `ui-chip`/`ui-chip--neutral`/`ui-card--subtle` 均已在 surface.scss 全局存在），不影响 surface.scss 编译与契约单测。

### 续34（06:33：实现多个官方主题）
- **用户指令**：「实现多个官方主题」。
- **架构**（沿用既有）：官方主题 = `useUserThemes.ts` 的 `USER_THEMES` 中 `builtIn:true` 项；
  调色板须**同时**进 `_theme-palette.scss`（→ `_theme-tokens` 编译 `--tint-*`/`--color-*-hover` 具体色）
  + `palette.css`（`[data-theme="<id>"]` 基色块）。示例用户主题（sunset/mint）仅 `user-themes.css` + 注册，走运行时回退。
- **新增 4 个官方主题**：`rose`（亮·玫瑰）/`ocean`（暗·海洋）/`forest`（亮·森林）/`midnight`（暗·午夜），
  与 `encv`/`encv-dark` 共 6 个官方。亮/暗由 `color-scheme` 声明。
- **四处同步**：① `_theme-palette.scss` `$palettes` 加 12 色项；② `palette.css` 加 `[data-theme]` 基色块；
  ③ `useUserThemes.ts` 注册 `builtIn:true`；④ `i18n/settings.ts` 的 zh-CN+en 加 `settings.theme<Id>`。
  外观页 `AppearanceDetail.vue` 已 `v-for themes` 自动展示，无需改。
- **契约锁**：`surface.test.ts` 新增用例——每个 `builtIn` 主题都必须编译出 `[data-theme=id]{--tint-primary-bg;--color-primary-hover}`，
  防「只注册不进 _theme-palette」导致 silent 退回运行时回退。
- **文档**：THEME_DEV.md 新增 §6.8 记录官方主题扩展 + 「四处同步」纪律 + 契约锁（落实 `文档同步.mdc`）。
- **踩坑/注意**：① 导入 `useUserThemes` 进单测——其模块级 `applyToDom` 用 `typeof document==='undefined'` 守卫，vitest node 环境安全；
  ② 暗色官方主题的「暗」由 `color-scheme` 驱动，Ionic 的 `body.dark` 由 `useTheme` 的 bgColor 控制（与 data-theme 解耦），与 encv-dark 既有行为一致，非本次引入；
  ③ `--tint-*` 派生令牌名不能含 `%`（续32 已解决），新增主题沿用 `$soft`/`$tint-config` 通用配方，无新百分比字面量。
- **验证**：`app_check_all` → **9 PASS / 0 FAIL**（含新增官方主题契约单测；i18n lint 两语言键齐备）。

### 续35（06:54：推翻编译期特权，官方 == 第三方）
- **用户纠正**：「主题不是调色板」「官方主题也得和第三方主题一个待遇，你全塞应用里让第三方主题怎么玩」。
  我才意识到续34 把 4 个官方主题塞进编译期 `_theme-palette.scss` + `palette.css`，赋予第三方**永远拿不到**的
  「编译期派生令牌」特权，违背主方案 `ENCV前端主题重构方案.md` §2.3/§2.7.6（主题=声明式数据资产，官方==第三方同形态）
  与续26「主题 ≠ 换色板」原则。文档里本有此决策，是我没遵守。
- **根因 bug（顺带暴露）**：§6.7 的 `--color-*-hover/-active` 只在 `[data-theme=encv/encv-dark]` 编译块定义；
  第三方主题（sunset/mint）未进 `_theme-palette`，`--color-primary-hover` 为 undefined → `.ui-button:hover` 背景失效。
- **修复（官方 == 第三方，零特权）**：
  1. **删除 `_theme-palette.scss`**（编译期调色板）；`_color-fns` 删 `compiled-tint/compiled-neutral/shade`，仅留运行时 `runtime-tint/runtime-neutral/css-var`。
  2. `_theme-tokens.scss`：删按主题编译的 `@each` 循环，改在 `:root` 对每个交互色用
     `color-mix(in srgb, var(--color-*), white/black)` 运行时派生 hover/active。因 `var(--color-*)` 在元素上
     解析为当前主题色，单个 `:root` 定义对官方+第三方同样生效 → 派生结果完全一致。
  3. 表面 tint 沿用 `_mixins` 的 `var(--tint-*, color-mix(var(--color-*)))` 回退，所有主题一致。
  4. 官方 6 主题（encv/encv-dark/rose/ocean/forest/midnight）的 `[data-theme]` 基色块留在 `palette.css`（随包数据），
     与 `user-themes.css` 的 sunset/mint 写法完全一致；`builtIn` 仅是「随包/不可卸载」分发标记，无颜色力差异。
- **契约锁（反方向，防复发）**：`surface.test.ts` 改为断言编译产物**不含**任何 `[data-theme=X]{--tint-*` / `{--color-*-hover:}` 声明，
  且 `:root` 提供 `color-mix(var(--color-primary))` 派生 hover/active。
- **文档**：THEME_DEV.md §6.7 分层处、§6.8 加「已废除/被 §6.9 推翻」标注，新增 §6.9 记录新架构 + 反方向契约锁 +
  「新增官方主题 = palette.css 基色块 + 注册 + i18n，❌不再进 _theme-palette」。（落实 `文档同步.mdc`。）
- **踩坑/注意**：① 删除 `_theme-palette.scss` 后需同步移除 `_theme-tokens`/`_color-fns` 的 `@use 'theme-palette'`，否则编译报错；
  ② 第三方主题按钮 hover 失效是 §6.7 既有缺陷，本次一并修复；③ 主方案文档早已写明「主题=声明式资产、官方==第三方」，
  下次动主题前应先查 `ENCV前端主题重构方案.md` 与 THEME_DEV.md，别再凭 §6.7 的旧「分层」假设行事。
- **验证**：`app_check_all` → **9 PASS / 0 FAIL**（含反转后的单测契约）。

### 续36（07:10：换色 ≠ 主题，官方主题重写为设计语言资产）
- **用户再次强调**：「换色不是主题，能懂吗」。续35 只消除了编译期特权，但官方主题仍只是「换色」——
  6 个主题半径全相同（`1rem/0.5rem/1rem`），表面层还有**字面量圆角**（`8px`/`12px`/`9999px`/`4px`）
  导致形状语言无法被主题改写。这才是「换色」的本质。
- **修正（让主题真成主题）**：
  1. **迁出 `palette.css`**：rose/ocean/forest/midnight 从 encv 品牌调色板移到
     `official-themes/<id>/theme.css` 独立主题资产（与 `user-themes/<id>/theme.css` 同形态、同 `@import` 加载，
     `theme-core.css` 引入），`palette.css` 仅留 encv/encv-dark 默认基态。
  2. **表面层字面量圆角 token 化**：`--radius-box`/`--radius-pill`/`--radius-mono` 基线定义在
     `tokens.css :root`，`_surfaces.scss`/`_mixins.scss` 改消费这些令牌（card/panel/overlay/chip/badge/mono）。
     主题从此可整体改写「形状语言」，不再被硬编码卡死。
  3. **每个官方主题定义独立设计语言（不止色相）**：
     - rose：圆润(1.5rem)+宽松(density 1.05)+柔彩阴影+回弹动效；
     - ocean：科技暗色+利落小圆角(0.75rem)+紧凑(0.9)+青色辉光+敏捷；
     - forest：圆角盒(1.25rem)+宽松(1.12)+柔绿影+缓入缓出；
     - midnight：极简暗色+锐利(0.375rem，胶囊近方)+近扁平(无抬升)+细锐环+沉稳。
- **契约锁（防复发，反向）**：新增 `official-themes.test.ts` 断言 4 个官方主题资产各自定义**不同**的
  `--radius-*`/`--density`/`--elevation-*`，且**不仅**含 `--color-*`；并断言 `palette.css` 不再含这 4 主题
  （防止回塞品牌调色板）。`surface.test.ts` 既有「无编译期烘焙」契约仍有效（主题资产是纯 CSS 数据，不进 surface.scss）。
- **踩坑/注意**：① 新增官方主题现走四处同步（official-themes/ 资产 + theme-core @import + 注册 + i18n），
  **不要**再塞 `palette.css`、**不要**塞回 `_theme-palette.scss`（已删）；② 凡表面层想让主题可改的维度，
  都必须先 token 化（加 `--radius-*`/密度/立体感令牌 + `:root` 基线），否则主题改不动；③ 用户的核心诉求是
  「主题 = 可改写任意元素的声明式资产（色+形状+密度+立体感+动效+…）」，不是调色板——下次动主题先看
  `ENCV前端主题重构方案.md` §2.3/§2.7.6 与 THEME_DEV.md §6.9/§6.9.1。
- **验证**：`app_check_all` → **9 PASS / 0 FAIL**（含新增 `official-themes.test.ts`；Biome 仅一处格式由 app_format/app_exec 修）。

### 续37（07:32：主题 = 运行时可加载「文件夹资产包」，官方==第三方，附性能指标与优化）
- **用户点破根因（第四次纠正）**：续34→35→36 我一直在「填令牌槽」里打转（换色→色+形状+密度+立体感），
  但本质仍是**编译进 `theme-core.css` 的 CSS 块**：①主题被构建期 `@import` 烤进产物，用户**无法加载/卸载/分发**
  （违背方案文档 §2.3「主题=可加载/可卸载/可分发的资产包」）；②主题带不了自有资源（图片/字体/贴纸）→
  **Hello Kitty 主题根本做不了**（用户原话）。真主题（思源集市式）= 能运行时加载、能用自己的选择器覆写真实组件、
  能 `url()` 自有资源 / `@font-face` / `theme.js` 钩子装饰的独立样式表；令牌只是「懒人快捷层」，不是主题本身。
- **新架构（落实方案文档 §2.3 / 对比表「theme.json + theme.css + 可选 theme.js，可加载/卸载/分发」）**：
  1. **主题 = 文件夹资产包**：`app/encv-mobile/public/themes/<id>/{theme.json, theme.css, 可选 theme.js/assets}`。
     官方 6（encv/encv-dark/rose/ocean/forest/midnight）+ 示例第三方（sunset/mint）**同形态**。
  2. **运行时加载，不再编译进产物**：`theme-core.css` **不再 @import 任何主题**；`palette.css` 仅留 `:root` 的
     encv 默认基态（含 `[data-theme="encv"]`，作为回落，无其它 `[data-theme]` 块）。主题由新建
     `themeLoader.ts`（`packages/shared-components/src/theme/`）运行时 `createElement('link')` 注入并切 `[data-theme]`。
  3. **官方 == 第三方，唯一区别 = 预装（builtIn）**：`builtIn:true`（官方）随包/预加载/不可卸载；
     `builtIn:false`（第三方）来自用户空间/集市(远程URL)/可卸载/LRU回收。两者走同一套加载代码，零特权差异。
  4. **资源包能力证明**：`rose/theme.css` 用 `url("data:image/svg+xml,...")` 给 body 加极淡点纹，演示「主题能自带装饰资源」。
- **themeLoader 性能指标（getThemePerf / window.__encvThemePerf）**：每主题 loadMs/cached/loadedAt；全局
  lastSwitchMs/cacheHits/loaded/active/firstPaintMs/thirdPartyLoaded。
- **themeLoader 优化**：①预加载（启动期并行注入官方 link，切换零等待）；②去重缓存（同主题只注入一次，
  重复激活命中缓存 loadMs=0）；③LRU 卸载（第三方超 `MAX_THIRD_PARTY_LINKS=4` 回收 link+unmount）；
  ④防 FOUC（先 await CSS onload 再置 `[data-theme]`，默认回落 `:root`）；⑤2s 超时安全网防挂起。
- **代码动作**：
  - 新建 `themeLoader.ts`；重写 `useUserThemes.ts`（派生 source、委托 loader、导出 `initUserThemes()`
    启动期预加载+水合）；`main.ts` 调 `void initUserThemes()`。
  - 8 个 `public/themes/<id>/{theme.json,theme.css}`；删旧 `official-themes/` 4 文件 + `user-themes.css`；
    `theme-core.css` 去 5 个 `@import`；`palette.css` 去 `[data-theme="encv-dark"]` 块（迁到资源包）。
  - 重写 `official-themes.test.ts` 为「资源包契约 + loader 注入/去重/卸载/默认回落/预加载」并加入 FAST_INCLUDE。
  - 文档：THEME_DEV.md §6.9/§6.9.1 标「加载方式被 §6.10 推翻」、新增 §6.10；方案文档 §2.7.6 补主应用运行时加载已落地。
- **踩坑**：① 测试资源包目录相对路径算错（应为 `app/encv-mobile/public`，我多退一级到 `app/public`）→ happy-dom 读不到；
  ② palette 断言误把默认基态 `[data-theme="encv"]` 也排除（它本就该保留）→ 改为只排除 rose/ocean/forest/midnight/encv-dark；
  ③ **`app_format`(MCP) 与本仓 `biome ci` 不一致**：`app_format` 报「已格式化」但门禁仍 FAIL，须改用
     `app_exec` 跑 `pnpm exec biome check --write <files>` 才真正写入（本次 3 个 TS/测试文件）。下次 Biome CI FAIL 优先用此路径。
- **验证**：`app_check_all` → **9 PASS / 0 FAIL**（Biome/各包 typecheck/i18n lint/单测/vite build 全绿）。

### 续38（07:54：主题系统对用户真正可用——可视预览 + 集市 + URL 分发 + 管理 + 性能可见）
- **用户第五次纠正「远远不够，继续」**：续37 只把**引擎**做对了（运行时资产包+loader+指标），但用户面仍是
  「一行纯文本列表+勾选」——看不出主题设计语言，也无法安装/卸载/分发。「可加载/可卸载/可分发」只是内部能力不是体验。
- **AppearanceDetail.vue 主题区重做**：
  1. **可视实时预览网格**：每张卡片 `:data-theme="<id>"` 让主题 CSS 作用域落到卡片，内渲真实语义样本
     （`.ui-bubble--user`/`.ui-chip`/主·辅·强调·底色色块），用户肉眼看到形状(圆角)+配色差异。
  2. **主题集市 Bazaar**：`BAZAAR` 注册表（示例 `neon`/`paper` 本地包），`installFromBazaar` 一键装进用户空间。
  3. **从链接安装（可分发）**：粘贴远程 `theme.css` URL → `installTheme({id,url})`，loader 按远程地址注入 `<link>`。
  4. **管理**：非官方主题显卸载按钮 → `unloadTheme` 移 link+删持久化；官方 `builtIn` 不可卸载。
  5. **性能可见**：外观页底部展示 `themeLoader` 实时指标（切换耗时/去重命中/已加载）。
- **useUserThemes 新能力**：`installTheme`/`installFromBazaar`/`uninstallTheme`；已安装持久化 `localStorage["encv-installed-themes"]`，
  `initUserThemes()` 启动期水合+预热全部 link（切换/预览零等待）；`ensureThemeLoaded(id)` 预览预热；响应式 `themePerf`。
  `themeLoader` 新增导出 `ensureCss`（供预热）。
- **新增资源包**：`public/themes/neon/`（暗色霓虹·直角·发光描边）、`public/themes/paper/`（暖纸·柔圆角·衬线），
  仅登记在 `BAZAAR`，安装前不出现在主网格，证明集市式分发。
- **i18n**：settings.ts zh/en 新增 bazaar/install/installed/uninstall/installFromUrl/themeUrlPlaceholder/themePerf*/bazaarNeon*/bazaarPaper*。
- **测试**：`official-themes.test.ts` 新增 `useUserThemes` 描述块，断言集市安装+持久化、卸载同步删 link+持久化、
  URL 安装按远程地址注入 link、官方 uninstall 静默忽略。已入 FAST_INCLUDE。
- **文档**：THEME_DEV.md 追加 §6.11（用户可用的主题系统）；§6.10 仍为运行时资产包基准。
- **验证**：`app_check_all` → **9 PASS / 0 FAIL**（Biome 用 `pnpm exec biome check --write` 修，非 app_format）。

### 续39（10:48：预览忠实化——渲染真实语义组件，主题「组件覆写」在预览中可见）
- **用户第六次纠正「继续」**：续38 把用户面搭起来了，但预览卡片用的是**占位** `.pv-bubble`/`.pv-chip`（只显示令牌颜色），
  **看不出主题对组件的覆写**（neon 发光描边、paper 衬线字体、`--radius-box` 圆角差异）——仍「看不出设计语言」。
- **先核验机制正确性（避免假改）**：确认所有主题(官方 6+neon/paper/kitty…)CSS 均作用域在 `[data-theme="id"]`，
  预览卡片 `data-theme="<id>"` 让后代正确继承该主题变量；官方主题文件齐备(`public/themes/*/theme.css`)；
  `surface.scss` 全局引入(`main.ts:44`)，`.ui-bubble--user/.ui-chip/.ui-panel` 可用；`UserMessageBubble.vue` 已用 `.ui-bubble--user`，主题真实影响 App。
- **改动 AppearanceDetail.vue 预览**：占位 `.pv-bubble`/`.pv-chip` 删除，改为直接渲染真实语义组件
  `.ui-bubble.ui-bubble--user` / `.ui-chip` / `.ui-panel.pv-panel` + 色板；卡片自身 `data-theme` 作用域内继承主题，
  **主题组件覆写原样呈现**（neon 发光、paper 衬线、圆角随 `--radius-box`）。卡片文字改 `color: var(--color-base-content)`
  （用主题自身令牌，暗/亮卡片都可读）；卸载图标色同改为主题令牌色。
- **文档**：THEME_DEV.md §6.11 第 1 点修订，标注「续39 占位→真实组件」，消除文档与代码不符。
- **验证**：`app_check_all` → **9 PASS / 0 FAIL**（全绿）。

### 续40（11:06：Ionic 半透明层跟随任意主题——换肤不再半成品）
- **用户第七次「继续」**：核验桥接时发现真正换肤缺口——`bridge.css` 实体色用 `var(--color-*)` 间接（任意主题跟随），
  但 `-rgb` 三元组硬编码成 encv 紫（lines 25/33/41…）。Ionic 半透明层（`rgba(var(--ion-color-*-rgb),a)`：进度条/焦点环/overlay/ripple）
  仍认 encv 紫，切 rose/ocean/neon 后实体色换、半透明层残留紫 → 换肤「半成品」。
- **修复**：themeLoader 新增 `syncIonicRgb()`，每次 `activateTheme` 置 `[data-theme]` 后读取当前主题 `--color-*`，
  用 `hexToRgbTriple()` 派生写回 `--ion-color-*-rgb`（primary/secondary/tertiary=accent/success/warning/danger/medium/light）
  + contrast/background/text -rgb；写内联 `<html>`（特异性高于桥接 `:root`，覆盖硬编码兜底）。主题无关（官方/第三方/远程 URL 均生效）。
- **bridge.css 注释更新**：-rgb 现由 syncIonicRgb 运行时派生，硬编码值降级为兜底。
- **测试**：`official-themes.test.ts` 新增 `themeLoader.syncIonicRgb` 描述块：rose 派生 `225,29,72`；
  即便硬编码 encv 紫存在也被 neon 绿 `57,255,20` 覆盖。已入 FAST_INCLUDE。
- **文档**：THEME_DEV.md §6.12。
- **验证**：`app_check_all` → **9 PASS / 0 FAIL**（修一处 `afterEach` 漏导入的 typecheck 失败）。

### 续42（16:39–16:46：theme.json 成为运行时活清单——清单驱动分发，非死数据）
- **用户第九次「继续」**：核验「可分发」发现根缺口——每个主题随包放 `theme.json`（架构文档也称主题为
  「theme.json + theme.css 文件夹资产包」），但运行时**从不 fetch/parse 它 = 死数据**。后果：① 元信息
  （名字/作者/版本/js/资源）全在 TS `USER_THEMES` 硬编码，与磁盘清单两处真相可静默漂移；② 从 URL 分发
  必须手填 id + **直连 theme.css**，不是「指向文件夹即安装」的真分发（Obsidian/思源集市是指向清单）。
- **代码动作**：
  - `themeLoader.ts` 新增 `ThemeManifest` + `fetchThemeManifest(folderOrJsonUrl)`：接受文件夹 URL 或
    theme.json URL，fetch+parse，相对 css/js 按清单目录解析为绝对 URL（绝对/data: 原样），校验 id、HTTP 失败抛错。
  - `useUserThemes.ts`：`UserThemeMeta` 增 `jsUrl`，`toSource` 优先用它（否则回退本地 `js:true` 推导）；
    新增 `installThemeFromUrl(url)`（async）读清单自动发现 id/名字/CSS/JS 安装；导出该函数。
  - `AppearanceDetail.vue`「从链接安装」：非 `.css` 走 `installThemeFromUrl`（清单路径），`.css` 保留旧直连回退；
    加安装中/错误态（`installing` i18n 键 zh/en + 错误 `ion-note`），placeholder 改「文件夹/theme.json 地址」。
    ⚠️ 该文件的 ion-* 是自定义元素（无 `@ionic/vue` import），`ion-note` 直接可用无需导入。
- **契约锁**：`official-themes.test.ts` 新增三块——① 防漂移：每个本地 theme.json 的 builtIn/js 与 `USER_THEMES` 一致；
  ② `fetchThemeManifest`（mock fetch：文件夹/theme.json/绝对 css/缺 id/404 各分支）；③ `installThemeFromUrl` 读远程清单派生 + 注入远程 link。
- **设计说明**：内置主题随包，其 TS 注册表仍是启动期零 fetch 的权威（避免 N 次启动请求），一致性靠防漂移测试保证；
  **远程/集市分发**才走 `fetchThemeManifest` 活清单路径。
- **验证**：`app_check_all` → **9 PASS / 0 FAIL**（Biome 用 `pnpm exec biome check --write` 修；test 文件 2 处 forEach 返回值 warning 属续41 遗留、非本次、非阻塞）。文档 THEME_DEV.md §6.14。

### 续43 修订（17:06–17:19：本地优先/云拉取落本地同一目录 —— 框架端口化，存储后端是 Go）
- **用户纠正**：上一版「继续」我假设 Capacitor、把 `themeStore.ts` 写成「@capacitor/filesystem + localStorage 存主题字节 + data: URL 注入」。用户明确：**后端是 Go 不是 Capacitor**，且**避免框架与应用耦合**。即主题「本地同一目录」= Go 后端托管的同源 `/themes/<id>/`，不是设备文件系统；「字节下载到哪」是应用层职责，不应进 shared-components。
- **重写(端口化)**：删 `themeStore.ts`，新建 `theme/themeStorage.ts` —— 只定义【端口 `ThemeStorage`】`pullToLocal(req)/removeLocal(id)` + `setThemeStorage/getThemeStorage` 注入点，默认 `sameOriginThemeStorage` 不下载不存字节(单测/后端已就绪用)，框架零 Capacitor/零后端依赖。
  - `useUserThemes`：`installThemeFromUrl` 改调 `getThemeStorage().pullToLocal(...)` 后仍从同源 `/themes/<id>/` 解析(本地优先，不热链 CDN)；新增 `installThemeFromCssLink`(裸 .css 直链同样走端口)；`toSource` 对 `meta.local` 统一从 `/themes/<id>/` 解析 css/js；`uninstallTheme` 调 `removeLocal`。删 `UserThemeMeta.jsUrl` 远程用途，仅留非本地兜底兼容。
  - **应用层适配器**：`encv-mobile/src/stores/registerSharedThemeStorage.ts` 注入 Go 后端实现 —— `pullToLocal`→`POST {getAgentApiBase()}/api/themes/pull`、`removeLocal`→`DELETE /api/themes/<id>`，沿用 `getAgentApiBase()` 三态拼装(dev 网关/native 相对/web SPA 绝对)，与现有 `setApiProxy` 解耦范式一致；`main.ts` 启动期 `registerSharedThemeStorage()`。
  - **单测修正**：`installThemeFromUrl` 断言改为注入同源 `/themes/remote-cool/theme.css`(本地优先)而非远程 CDN。
- **文档**：THEME_DEV.md §6.14 安装描述+契约锁措辞修正；新增 §6.15 记录端口化+Go 后端+本地优先，并标注续43 初稿 Capacitor 方案已废除。
- **验证**：`app_check_all` → **9 PASS / 0 FAIL**（修 1 处 typecheck：`ThemeManifest` 应从 `themeLoader` 而非 `themeStorage` 导入）。

### 续43 二次修订（17:52–17:58：主题数据目录与 servingDir 分离；Android 落 app 私有）
- **用户纠正**：我一度把用户主题写进 `servingDir/.encv/themes`（servingDir=静态 web 根）。用户明确：**应用数据不该放 servingDir**，**且安卓是私有路径**（servingDir 在 Android 上是打包的私有只读资产，写不进去也不该混用户数据）。
- **改法（对齐既有 `mountRegistryDataPath`/`kernelDataPath`/`simverseDataPath` 脉络）**：
  - 新增 `themeDataPath()`（`server.go`）：复用 `mountRegistryDataPath` 目录派生，取同级 `.encv/themes`；优先级 `ENCV_THEMES_DIR` > 派生。Android→`<ENCV_APP_FILES_DIR>/.encv/themes`(app 私有 files，可写)；桌面→XDG/标准 `encv(-dev)/themes`。
  - `Server` 增 `themesDir` 字段（`NewServer` 用 `themeDataPath()` 初始化）；`themesRoot()` 返回 `s.themesDir`（不再用 `servingDir`）。`mobile_themes.go` 的 pull/delete/static 全部基于 `themesDir`。
  - `HandleThemeStatic`(GET /themes/*) 改为 **local-first 双源**：先查 `themesDir`(用户主题)，再回退 `servingDir/themes`(内置)，两侧防穿越；用户主题优先覆盖同名内置。修复此前 `/themes/*` 显式路由会 shadow 掉内置主题的隐患。
- **Go 测试**：`internal/server/mobile_themes_test.go` 增 `TestThemeStatic_BuiltinFallback`(回退) + `TestThemeDataPath_NotServingDir`(锁契约：数据目录≠servingDir、Android 落 app 私有)；与原有 pull/static/delete/cssOnly/穿越防护共 **4 PASS**。`go vet` 干净。
- **文档**：THEME_DEV.md §6.15 补「存储根=数据目录(非 servingDir)」「Android app 私有」「内置回退」三节。

### 续41（11:25：切换主题卸载上一主题 JS 装饰——装饰不残留/不丢挂载）
- **用户第八次「继续」**：核验「真主题能力（theme.js 运行时装饰）」发现**切换生命周期 bug**——
  `activateTheme` 调 `ensureJs` 在激活时 `mount()` 一次，但【切走时不调上一主题 `unmount()`】（仅 `unloadTheme`/LRU 调）。
  故 kitty 挂 `<body>` 的角标贴纸切到 rose 后【永久残留】；更糟：`jsCache` 仍持 kitty，切回时 `ensureJs` 的
  `jsCache.has` 守卫使其【不再重新 mount】——不刷新拿不回装饰。
- **修复（activateTheme 内）**：置新主题前，若 `prevId` 存在且 ≠ 新主题，取 `jsCache` 中 `unmount()` 调用并 `jsCache.delete(prevId)`。
  效果：① 切走即卸载上一装饰（不残留/堆积）；② 删缓存使【切回能重挂】（不丢挂载）；同主题重复激活走 `prevId===src.id` 守卫，幂等。
- **测试桩 `__stubJsModule(id, mod)`**：为 jsdom 下对「动态 import」做确定性单测新增；`ensureJs` 优先用桩，
  `resetThemeLoaderForTest` 一并清桩。`official-themes.test.ts` 新增 `themeLoader 切换生命周期` 描述块：
  切到 kitty 装饰在 / 切到 rose 被卸载 / 再切回重挂 / 同主题仅挂一次。
- **文档**：THEME_DEV.md §6.13。
- **验证**：`app_check_all` → **9 PASS / 0 FAIL**（Biome 用 `pnpm exec biome check --write` 修，非 app_format——app_format 未实际改写）。
- **经验**：`app_format` MCP 在此环境只报告「Formatted 4 files / No fixes applied」却不真改，Biome 失败须用
  `pnpm exec biome check --write <file>` 才生效。

### codemogger-patch 多根检索 + 概念词典优化（19:xx）
- **痛点根因**：codemogger 只索引【单前端根】(`CODEMOGGER_ROOT=encv-mobile`) 且【无 Go 语法】，
  整个 Go 后端(`internal/server` 的 servingDir / SPA-WebView 服务 / themes)与受管项目(combolite/openlist)
  **完全不可见**。搜"SPA"/"servingDir"要 30+ 轮横跳，用户怒："搜个 SPA 都这么费劲"。
- **改造（全部在 `app/codemogger-patch/`）**：
  - 新增 `/workspace/.codemogger.json`（工作区维护）：`roots`(路径+weight，多根混合检索按权重降序标注) +
    `concepts`(架构概念→精准检索词，降噪，如 `SPA`→`Vite|WebView|loadUrl|file:///android_asset|encv-go`，
    避免裸 `SPA` 命中 `xcworkspace` 子串噪声)。
  - `codemogger-shim`：① 多根 `index`(遍历各根分别建 `<root>/.codemogger/index.db`，codemogger 无原生多根)；
    ② `search` 概念展开(命中概念先按精准词 grep，再各根 FTS 标注[根·w]，最后全仓 grep 补全)；
    ③ 新增 `grep` 子命令(全仓固定串 grep，覆盖 codemogger 不能解析的 go/md/kt)；
    ④ grep 补全排除 `.agents/.codebuddy/.trae/dist/build` 等降噪；`grep_root` 默认取 `.codemogger.json` 所在工作区。
  - `mcp-server.mjs`：`runShim` 支持 `{db:false}`（多根 search/index/grep 不挂 --db，改读配置）；
    新增 `codemogger_grep` + `codemogger_index` 工具，`codemogger_search` 改多根混合；
    `references/context/impact/leaks` 仍单根(--db)。`CODEMOGGER_CONFIG`/`CODEMOGGER_GREP_ROOT` env。
  - `README.md` §6 记录多根+概念词典设计；无 `.codemogger.json` 时自动回退原单根行为（向后兼容）。
- **验证**：`bash -n` 语法 OK；`search SPA` 概念展开命中 Vite / `file:///android_asset/.../index.html` WebView 加载；
  `search servingDir` 命中 Kotlin `EncvGoService`；一次查询跨全工作区，无需多轮横跳。
- **注意**：多根是"检索可见性"优化；`references/context/impact/leaks` 仍针对前端单根(需前端符号图)，架构事实靠 search/grep。

### codemogger 通识文件检索（docs+config，可配置 + per-root 标注）续优化（19:18）
- **用户进一步要求**：后端只做"检索可见性"太浪费；要支持【通识文件】检索(md/json/yaml/Dockerfile 等文档+配置)，且在 `.codemogger.json` 配置。
- **改造**：
  - `.codemogger.json` 新增 `grep` 块：`include`(glob 列表：go/md/json/jsonc/yaml/yml/kt/ts/vue/Dockerfile/toml/sh/gradle)、`exclude`(node_modules/.git/.codemogger/.agents/.codebuddy/.trae/dist/build 等)。省略则回退内置默认。
  - shim：`_load_config` 解析 `grep.include/exclude` 为数组；`_load_config` 后若无配置则用默认填充；`GREP_OPTS` 改为由配置动态构建。
  - **关键**：补全 grep 从"扁平全仓"改为 **`grep_roots()` per-root 标注**（按权重降序，每根独立 `[根 · w=…]` 块）。后端/配置根(`internal`/`openlist`/`combolite`)因此获得**一等标注归属**，
    不再是被浪费的扁平 grep——`search <q>` 的 grep 补全与 `search` 概念展开都走 `grep_roots`。
  - `codemogger_grep` 子命令保持扁平全仓（raw 工具），但同样用动态 GREP_OPTS。
  - 概念词典新增 `docker`(`Dockerfile|docker-compose|image:|container:`)、`config`(`ENCV_|CODEMOGGER_|vite.config|biome.jsonc|pnpm-workspace`)；**`docker` 去掉 `FROM `**（大小写不敏感 `-i` 会命中 `from "vitest"` 导入，噪声大）。
  - README §6 同步：新增 `grep` 配置说明、search 改为 per-root 通识补全、示例 JSON 同步；MCP `codemogger_search`/`codemogger_grep` 描述更新。
- **验证**：`bash -n` OK；`search ENCV_THEMES_DIR` 返回 `[shared-components · w=1]`(THEME_DEV.md) + `[internal · w=0.9]`(server.go) 标注块；
  `search docker` 概念展开命中 openlist 的 `Dockerfile.ci`(无 FROM 噪声)；`grep ENCV_` 命中 toml/yml 配置。后端/配置根确为一级可见。

### codemogger 通识检索增加 css/scss/less 样式文件（19:30）
- 用户要求：在通识文件检索里加上样式文件(css/scss/less)。
- 改动：`.codemogger.json` `grep.include` 增 `*.css *.scss *.less`；shim 默认 `GREP_INCLUDE` 同步增三项；README §6 工具表/示例 JSON/`include` 说明/同步。
- 验证：`bash -n` OK；`grep daisyui` 命中 `shared-components/src/styles/daisyui.css` 等样式文件，确认 css 已被索引。

### 续44（20:00：servingDir 应用数据全面迁出 + MCP 参数体验修复）
用户硬要求：**servingDir 是用户数据，任何应用数据都不该存在**；且此前"能通过测试就离谱"——缺防护测试。

#### A. servingDir 全面检查（用 codemogger_grep 实践定位）
活跃写入点（全部迁出）：
- `internal/server/db_init.go`：`encv-tasks.db` 原 `servingDir/encv-tasks.db` → 改 `tasksDataPath()/encv-tasks.db`（InitDatabase 去掉 servingDir 参数）。
- `internal/server/mobile_search_fulltext.go`：`fulltextDBPath(servingDir)` → `fulltextDBPath()`，落 `ftsDataPath()`（config.AppDataDir("fts")/.encv/fts5.db）。
- `internal/service/task_manager.go`：`NewTaskManager`/`NewTaskManagerWithStore` 的 `persistPath` 改 `config.AppDataDir("tasks")/.encv-tasks.json`。
- `internal/service/trash_manager.go`：`NewTrashManager` 的 `trashDir` 改 `config.AppDataDir("tasks")/.trash`（trash_manager.go 增 `config` 导入）。
- `pkg/tasksystem/store/sqlite/migrate.go`：`MigrateIfNeeded` 原只认 `servingDir/.encv-tasks.json`（死代码，无调用点）改同时认 `config.AppDataDir("tasks")` 旧 json（防漏迁），绝不写回 servingDir。

新增中枢：`internal/config/config.go` 加 `AppDataDir(subdir)` + `appDataParent()`，平台/env 矩阵与 `mountRegistryDataPath` 完全一致（复用 `.encv/<subdir>` 派生，Android 落 `ENCV_APP_FILES_DIR/.encv`），`ENCV_<SUBDIR>_DIR` 可覆盖。
`internal/server/server.go` 加 `tasksDataPath()`/`ftsDataPath()` 包装 + `migrateLegacyAppData(servingDir)`：启动期把遗留的 `encv-tasks.db`/`.encv-tasks.json`/`.trash`/`.encv/fts5.db` 从 servingDir best-effort 迁到数据目录（重命名、不覆盖新数据），在 `NewMobileService`/`InitDatabase` 之前调用。

#### B. 防护测试（锁死"app 数据不进 servingDir"）
- `internal/server/app_data_path_test.go`：`TestTasksDataPath_NotServingDir`/`TestFTSDataPath_NotServingDir`/`TestAppDataDir_NotServingDir`。
- `internal/service/app_data_guard_test.go`：`TestTaskManagerPersistPath_NotServingDir`/`TestTrashManagerDir_NotServingDir`（注释明示：谁把路径改回 `filepath.Join(servingDir,...)` 立即 FAIL）。
- `mobile_search_fulltext_test.go` 的 `TestFullTextDBPath` 改为断言 FTS 索引不进 servingDir 且等于 `ftsDataPath()/fts5.db`。

#### C. MCP 参数体验修复（用户要求：写错参数名无反馈本身就是问题，+ 参数别名）
三服务 `app/codemogger-patch/mcp-server.mjs`、`scripts/app-dev-mcp.mjs`、`scripts/web-fetch-mcp.mjs` 各加统一 `checkArgs(name,args,TOOLS,ARG_ALIASES,PASS_THROUGH)`：
- 别名归一到规范名（如 codemogger 的 `pattern|q|text|term → query`；app-dev 的 `cmd→command`、`dir→cwd|project`；web-fetch 的 `uri→url`、`link→url` 等，覆盖全部注册工具）。
- **未知参数直接报错**并给合法参数列表 + "did you mean" 拼写建议（不再静默当 undefined 退化为错误行为）。
- `app_skill_add`/`app_skill_update` 设 `PASS_THROUGH`（参数透传 skill-manager，不校验）。
- 实测：codemogger/web-fetch 管道验证（未知→error、别名生效）；app-dev 运行服务实测 `app_skill_list` 传 `xyz`→拒绝、`f`→filter 生效。

#### 验证
- `go build ./internal/... ./pkg/...` → 0 错。
- `go test` config/server/service/sqlite：guard 测试 + 受影响测试全 PASS（含 `TestFullTextDBPath`/`TestAppDataDir_NotServingDir`/`TestTaskManagerPersistPath_NotServingDir`/`TestTrashManagerDir_NotServingDir`）。
- 三 MCP `node --check` 全 OK。
- 结论：当前**无任何活跃应用数据写入点落在 servingDir**；剩余 servingDir 引用均为一次性遗留迁移（从 servingDir 迁出）：`server.go:345-348`、`registry_persist.go` 老 mounts.json 迁移、`migrate.go` 旧 json 读取。挂载注册表(kernel/simverse/theme/tasks/fts)全部已走 app data 目录（续43 脉络）。

### 续45（20:30：codemogger grep OR + gsap+daisyui 动效接入推进）
**codemogger 实践中完善**：grep 把 `|` 当字面量 → 多词查询（如 `MotionEngine|from "./internal"`）返回空，是实践中暴露的摩擦。修复：
- `codemogger-shim` 增 `_pat_opts`，把含 `|` 的 query 拆为多个 `-e` 固定串（OR）；`grep_roots` 与 `grep` 子命令都走 `_pat_opts`。保留单字面量语义。
- `mcp-server.mjs` 的 `codemogger_grep` 描述补 `|` = OR；README §工具表同步。
- 验证：`bash -n` OK；`grep 'MotionEngine|from "./internal"'` 命中（确认 ACL：仅 `internal/gsap-engine.ts` import gsap，各 composable 走 `./internal`）。

**gsap+daisyui 任务推进（Phase 3 动效全集开始接入真实 UI）**：
- `micro.ts` `useRipple` 自动把宿主设 `position:relative; overflow:hidden`，ripple 圆形无需额外 CSS 即被裁剪（`.encv-ripple` 由 JS 内联驱动）。
- `ExtensionsPage.vue`：页面进入转场 `usePageTransition`（作用 `ion-page.$el`）+ 既有列表 `useScrollReveal` stagger。
- `Settings.vue`：页面进入转场 `usePageTransition` + 设置分组 `useScrollReveal` stagger（作用 `ion-content.$el` 各 `ion-list`）。
- **契约测试** `encv-mobile/src/motion/__tests__/motion-guard.test.ts`（入 `FAST_INCLUDE`）：锁死 `guard` 闸门（reduced-motion / vivid-P3 intensity 1.3 / `setMotionDisabled` 总闸覆盖 reduced 探测）+ 设计令牌导出；是「换 gsap+daisyui 技术栈下游零改动」的运行时兜底。
- 接入原则：bespoke 交互组件（`ServerStatusCard` 3D 翻转/脉冲）保留专属动画不套共享层；通用列表/页面/卡片才接 `useScrollReveal`/`usePageTransition`/`useRipple`（与主题 re-surface 一致）。
- 文档：方案文档 §2.5.10 补落地进度。
- **验证**：`app_check_all` → **9 PASS / 0 FAIL**（Biome/各包 typecheck/i18n/单测含新契约测试/vite build 全绿；biome 格式用 `pnpm exec biome check --write` 实写，非 `app_format`）。

### 续45b（21:03：动效指令层——应用层自助接入，让 Phase 3 真正落地）
composables（usePageTransition/useScrollReveal/...）内部用 `onMounted/onUnmounted`，必须在组件 setup 上下文调用，无法直接用于 Vue 指令（指令 `mounted` 无活跃组件实例 → `onMounted` 失效）。
- 新增 `packages/shared-components/src/directives/motion.ts`：**指令层** 7 个指令 `v-reveal`(滚动揭示,`{stagger:true}`)/`v-page-transition`(页面进入淡入上移)/`v-ripple`(点击波纹)/`v-press`(按压回弹)/`v-hover`(悬浮抬升)/`v-magnetic`(磁性跟手,可传 strength)/`v-count-up`(数字滚动,绑 number)。
- 关键设计：指令内**直接驱动 `motion` 引擎 + `guard` 闸门**（用指令生命周期 `mounted/unmounted/updated`，cleanup 存 `WeakMap`），而非套 composables——因此仍受 ACL 约束、引擎可透明替换、换栈下游零改动。复用 `getComputedStyle` 设 `position:relative;overflow:hidden`（ripple 裁剪）。
- `main.ts` 增 `installMotionDirectives(app)` 全局注册（镜像既有 `v-longpress`），`Files.vue` 的 `<ion-page v-page-transition>` + `AgentChat.vue` 模态根 `<div v-page-transition>` 已接入高频页面/模态转场。
- 契约测试扩展：`motion-guard.test.ts` 增 describe 块，断言 7 指令导出且含 `mounted` 钩子 + `installMotionDirectives` 把它们注册到 app（锁 API，防死代码）。注意 `Directive` 联合类型可能是 `FunctionDirective`（无 `mounted`），测试用结构类型 `{ mounted?: unknown }` 断言。
- 文档：方案文档 §2.5.10 补「+ 指令层 7 指令注册与 API」断言；**新增 §2.5.11 动效指令层**说明设计/接入/已接入点。
- **验证**：`app_check_all` → **9 PASS / 0 FAIL**。Biome 仅 `cleanups.get(el)?.forEach(fn=>fn())` 返回值 warning，已改 `for...of` 消除；格式用 `pnpm exec biome check --write` 修。

### 续46（21:50：用户指令「别再用有问题的终端，MCP 不全就优化 MCP，强化 .codebuddy/rules」）
用户点名：停止坚持用本端 `execute_command`（裸终端，沙箱里易失联/卡死），若 MCP 能力不全应**先扩展 MCP 而非回退终端**，并把规矩写进 `.codebuddy/rules`。
- **强化 `.codebuddy/rules/环境保持.mdc`**（原仅讲 kill 红线 + 长命令走 MCP）：改为铁律——`execute_command` 仅限 `echo`/`ls`/`pwd`/`cat` 级极短探测，其余一律禁；实质命令改走 `app_exec`/`app_check_all`/`app_typecheck`/`app_i18n`/`app_format`/`codemogger_*`；**若某 MCP 能力不全，先扩展其服务文件**（app-dev→`scripts/app-dev-mcp.mjs`，codemogger→`app/codemogger-patch/mcp-server.mjs`），绝不回退终端；改完用 `app_exec` 跑 `node --check` 验证。
- **修 codemogger MCP（`app/codemogger-patch/mcp-server.mjs`）三处不全**：① 去掉死的重复 `codemogger_index` 分支（第二个 unreachable）；② `codemogger_list` 误带 `--db`（`list` 不接 --db）→ 显式 `db:false`；③ `codemogger_grep`/`codemogger_search` 新增 **`dir` 作用域参数**（注入 `CODEMOGGER_GREP_ROOT`），覆盖「按目录抓 grep」这一典型终端回退场景。`runShim` 增 `opts.extraEnv` 支持 per-call 环境变量。
- 验证：`app_exec` 跑 `node --check` 语法 OK；一次性拉起编辑后的 MCP 实测 `codemogger_grep dir=/workspace/app/encv-mobile/src` 正确限定范围、`codemogger_list` 不再因 --db 报错（返回 "No files indexed" 正常路径）。
- README：codemogger_grep 工具说明补 `dir` 作用域。
- **注意**：gsap 赋能主题（读 --motion-* 令牌）的代码+契约测试+方案文档 §2.5.x 已做，但 **THEME_DEV.md 的对应章节更新被本指令打断、仍待补**（下次续）。

### 续46-2（22:08：补齐 THEME_DEV.md §6.16 + 方案 §2.6 对齐）
- 在 `app/packages/shared-components/src/theme/THEME_DEV.md` 末尾补 **§6.16 续44/45：gsap 赋能主题（GSAP 运行时读取 --motion-* 令牌）**：说明旧短板（GSAP 时长/stagger/强度写死 JS 常量、主题改 --motion-* 对 GSAP 无效）→ 修法（新增 `motion/theme-read.ts` 运行时读取层，250ms 节流缓存，`DUR`/`getStagger()`/`intensity` 改走令牌；`invalidateMotionTokenCache()` 主题切换即时生效）→ 主题作者公开 API（--motion-dur-*/--motion-stagger/--motion-intensity/--motion-ease-*）→ 已落地样例（ocean/midnight 改 dur-fast、rose/kitty 改 ease-back、forest 改 ease-out）→ 契约锁（`motion-guard.test.ts` 该描述块）→ 验证 9 PASS。
- 同步修 **`ENCV前端主题重构方案.md` §2.6**：补一段「gsap 赋能主题（运行时读令牌）」说明 `theme-read.ts` 让 GSAP 与 CSS 同读 `theme/tokens.css` 的 --motion-* 单一真源，与代码/THEME_DEV 一致（文档同步纪律）。
- 验证：走 `app_exec` 跑 `pnpm exec vitest run src/motion/__tests__/motion-guard.test.ts` → **11 passed**（含 gsap 赋能主题描述块），未碰裸终端。

### 续47（22:51：修复 Settings 一级页面整页空白但可点击 + .ide/Dockerfile 加 Rust 工具链）
**新增规则**：`.codebuddy/rules/复现优先.mdc`（alwaysApply）：先复现再修复（UI 必须真实重现，不接受读码推断）；严禁猜测修改、严禁乐观测试（先红后绿）、严禁不固定经验（固化到记忆/文档/回归测试）。

**Bug：encv-mobile 设置页面一级页面显示空白（实际可点击，DOM 都在）**
- **根因（经源码假设→测试复现证实）**：`useScrollReveal`（及同类 `vReveal` 指令）把 `ion-content` 的每个直接子元素（各 `ion-list`）立即置 `opacity:0`，然后**仅靠 gsap ScrollTrigger 的 `onEnter` 揭示**。但 Ionic 的 `ion-content` 在内部 shadow DOM 的 `.inner-scroll` 滚动（**非 window**），gsap `ScrollTrigger` 默认以 window 为 scroller → 在 Ionic 页面内**永远收不到滚动事件** → `onEnter` 永不触发 → 所有 list 永久停在 `opacity:0` = **整页空白但可点击**（与用户症状逐字吻合）。
- **修复（IntersectionObserver，scroller-agnostic）**：`app/packages/shared-components/src/motion/reveal.ts` 的 `useScrollReveal` 与 `directives/motion.ts` 的 `vReveal` 均改为用 `IntersectionObserver`（root:null, rootMargin `0px 0px -10% 0px` 近似 `start:"top 90%"`，threshold:0）观察容器/元素；相交才揭示并 `io.disconnect()`（once 语义）；**无 `IntersectionObserver` 环境下直接 `motion.to(opacity:1)` 兜底，内容绝不永久隐藏**。删除此前（本规则前）未经验证的 `resolveIonScroller`/`whenIonScrollerReady`/`SAFETY_MS` 重试逻辑（续46 前的投机改），改走已验证的 IO 方案。`usePageTransition` 的 `clearProps:"opacity,transform"`（防残留隐藏）保留。
- **复现+防复发（先红后绿）**：
  - 新增 `app/encv-mobile/src/motion/__tests__/scroll-reveal.test.ts`（FAST_INCLUDE）：`vi.hoisted` 引擎 mock + `FakeIntersectionObserver`；断言「挂载走 IO（非 ScrollTrigger）、相交才揭示、不相交不揭示、无 IO 环境直接落可见态」。写时**先红**（确认旧 ScrollTrigger 代码不建 IO、`engine.to` 永不调用）→ 改后**转绿**。
  - 新增 `app/encv-mobile/src/motion/__tests__/directive-reveal.test.ts`（FAST_INCLUDE）：同逻辑锁死 `vReveal` 指令修复。
  - 两文件均须 `engine.to/from/fromTo` 带显式参数元组（否则 `vi.fn(()=>...)` 推断 `Args=[]` 致 `mock.calls[0][1]` typecheck 失败）；`vReveal` 经 `as unknown as ObjectDirective` + `mountFn` 收窄调 `mounted`（Directive 联合类型无 `mounted`）。
- **验证**：`app_check_all` → **9 PASS / 0 FAIL**（Biome/各包 typecheck/i18n/单测/vite build 全绿）；两 motion 测试 6 passed。
- **经验（长期）**：Ionic 内滚页面里**任何靠 window-scoped 滚动触发**的动效（ScrollTrigger 等）都会静默失效；揭示/入场类一律用 `IntersectionObserver`。`useScrollParallax`（仍用 ScrollTrigger）在 Ionic 下同样不会触发，但只是无视差效果、**不导致空白**，本次未改（非本 bug，留作待办）。

**.ide/Dockerfile 加 Rust 工具链（libsql 经 CGO 编译 Rust 核心所需）**
- 新增 `ARG RUST_VERSION=stable`；section 2.4 用 `rustup-init.sh`（`RUSTUP_DIST_SERVER/RUSTUP_UPDATE_ROOT` 走 rsproxy.cn 镜像，失败回退 sh.rustup.rs）装 stable `--profile minimal`；写 `${CARGO_HOME}/config.toml` 把 crates.io 换源 rsproxy.cn（sparse 协议 + `git-fetch-with-cli=true`）。
- 基础 apt 增 `pkg-config libssl-dev`（libsql/Rust CGO 常见依赖 openssl-sys）。
- `CARGO_HOME=/root/.cargo`、`RUSTUP_HOME=/root/.rustup` 注入 ENV PATH + `/etc/zsh/zshenv` + `/etc/profile.d/custom_tools.sh`；section 7 验收增 `rustc`/`cargo` 版本 echo。
- 镜像大、未在此沙箱实跑 `docker build`（无 docker）；改动是确定性的标准 rustup 安装，语法/路径自洽。

### 续48（23:37：臻彩显示（Vivid / P3）真正生效——修复三处历史失效）
用户纠正：外观里的「臻彩显示」此前形同虚设。经**先红后绿**复现（`vivid.test.ts` 修复前 5/6 RED）→ 三处根因：
1. **默认恒等滤镜**：旧 `syncVividFilter` 把 intensity 直接拼 `contrast(intensity/100) saturate(...)`，默认 100→`contrast(1) saturate(1)`=恒等，零变化。
2. **`.encv-vivid` 根类从未添加**：`applyVividMode` 只切状态、从未给 `documentElement` 加 `.encv-vivid` 类 → guard/tokens 里 `encv-vivid` 驱动的动效强度 1.3 boost 永不触发（业务侧没接上）。
3. **P3 写无效属性**：`color-gamut` 是**只读媒体特性、非作者可设 CSS 属性**，旧 `applyP3Mode` 写 `--encv-color-gamut:p3` 之类完全无效 → 宽色域从未生效。
**修复**（零回归）：
- `vividAmountFromIntensity`：50→0、100→**0.34**(默认即可见)、200→1；`applyVividMode('on')` 现加 `.encv-vivid` 根类；`syncVividFilter` 用 MotionEngine(gsap) 平滑过渡 `--encv-vivid-amount`（reduced-motion 走 `getMotionProfile()` 闸门，ACL 解耦）。
- P3 改「`.encv-p3` 根类 + `@media (color-gamut: p3)`」：`applyP3Mode` 只加/去 `.encv-p3` 类；`vivid.css` 在真 P3 屏把 `--color-primary/-secondary/-accent` 换成 `-p3` 令牌。`applyColor` 按 `P3_PRIMARY_MAP`(7 已知色→display-p3 坐标)写 `--color-primary-p3`，未知色回退 srgb。
- `palette.css` 补 `--color-secondary-p3`/`--color-accent-p3` 默认；`App.vue` 删失效 `ion-page{filter:var(--encv-vivid-filter)}` 与 `@media (color-gamut:p3)`/`.encv-force-p3` 无效块；`theme-core.css`/`daisyui.css` 均 `@import vivid.css`。
**关键认知（长期）**：`color-gamut`/`prefers-color-scheme` 是媒体特性、**只读**，绝不能作为作者属性写 `style`/`setProperty`；响应式宽色域只能靠 `@media (color-gamut: p3)` + 根类切换。
**契约锁**：`encv-mobile/src/motion/__tests__/vivid.test.ts`（FAST_INCLUDE，6 用例）先红后绿锁死。
**文档**：THEME_DEV.md §6.17（文档同步纪律）。**验证**：`app_check_all` → **9 PASS / 0 FAIL**。

### 续49（23:49：用户质疑"臻彩一个开关能否全自动"——P3 改为任意 hex 自动派生）
用户点破：① Vivid 滤镜确实全自动（根级 filter，对所有主题/背景）；② 但 P3 真宽色域原靠写死的 `P3_PRIMARY_MAP`（7 色坐标，逐一对过发现就是 hex 归一化值）。
**改动**：删除 `P3_PRIMARY_MAP`，`applyColor` 改调 `hexToP3Token(hex)`——任意有效 hex（含自定义取色/远程主题色）自动归一化派生 `color(display-p3 r g b)`，开关对全色域全自动；非法 hex `removeProperty` 回退 srgb。
**仍存真缺口（待办）**：背景/渐变（`applyBgColor` 设的 `--ion-background-color`/body 渐变）**未进 P3 交换**，P3 模式下只吃到 vivid 滤镜、无真·宽色域。需把背景色也塞入 `vivid.css` 交换才完整——更大改动，本次未做。
**契约锁**：`vivid.test.ts` 改"未知色自动派生 display-p3"+"非法色回退"两条（先红后绿）。**文档**：THEME_DEV.md §6.17 第4点 + 契约锁描述同步。**验证**：`app_check_all` → **9 PASS / 0 FAIL**（Biome 用 `pnpm exec biome check --write` 实写）。
