// encv_core.ts - 服务端 URL / 标识 / 持久化常量
// 实现已提升为共享基座 @/api/core（见 packages/shared-components/src/api/core）。
// 此处 re-export 以兼容现有 `import ... from '@/api/encv_core'`。
export * from "@encv/shared-components/api/core";
