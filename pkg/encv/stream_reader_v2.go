// pkg/encv/stream_reader_v2.go

package encv

// import (
// 	"io"

// 	"github.com/Soltus/encv-go/internal/v2/reader"
// )

// // GetReader 为给定的容器文件返回一个通用的解密流。
// func GetReader(filePath, password string) (io.ReadCloser, error) {
// 	return reader.GetReader(filePath, password)
// }

// // GetSeekableReader 为给定的容器文件返回一个可寻址的解密流。
// func GetSeekableReader(filePath, password string) (reader.ReadSeekCloser, error) {

// 	return reader.GetSeekableReader(filePath, password)
// }
